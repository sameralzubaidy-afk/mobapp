# Ledger — SUB Android Round 5 (2026-09-16)

Decision → reasoning → evidence log. Format follows the QA Task 25 Group K reference ledger.

| # | Action | Reasoning | Tool call | Outcome |
|---|---|---|---|---|
| 1 | Pre-read the playbook + 4 repo-memory files + the SUB guide scope block | §4 mandates read-before-first-run; SUB's guide now carries a committed "Round scope & priorities" block (FIX-Task-37 item 3) precisely so a brief cannot be assigned against retired cases | `read_file` ×4, memory ×4 | Group B confirmed RETIRED in-source before planning |
| 2 | R29 busy check | Two agents on one emulator ⇒ interleaved taps ⇒ false verdicts | `uptime`, `pgrep -fl`, `adb devices`, `dumpsys activity` | Clean: 1 emulator (`Medium_Phone_API_36.1`), 0 orchestrators, 1 Metro. A `playwright test-server` for the admin portal was idle. |
| 3 | FIX-Task-46 environment-health gate | A "never loads" claim must be quantified against an independent reference before it is filed as an app bug | qemu `ps`, `uptime`, device `ping`, host `curl`, `qa:start-state` timing | **HEALTHY** (qemu 9.5→5.0%, load 4.9–5.2, ICMP 21.6 ms, host→Supabase 256 ms, host-side same-path read 0.80 s) ⇒ all later load times reportable as app-side |
| 4 | Source pre-read for Group A | §4 flow-registry pre-read before blind navigation | `ls src/screens/subscription`, `grep AppNavigator` linking map | `SubscriptionPlans`/`KidsClubOverview` → `JoinKidsClubScreen`; `PlanComparison`/`UpgradePlan` are real. **A01's `SubscriptionPlansScreen` ref is dead** ⇒ decided to drive A01 on `UpgradePlanScreen` (the actual plans-cards surface) |
| 5 | Group F started on the **existing** Payout Settings state | The app was already on Payout Settings as `qa-payout-seller` from Round 4's left-state — no persona switch needed for F05/F04's hero | `batch[foreground, screen_size]`, `save_screenshot`, `state`, SQL | **F05 PASS** (empty state), **F04 hero 3/3 DB-exact** |
| 6 | DB-reconciled the `payout-action-required-summary` aggregate | Brief rule: any count/summary element must be DB-reconciled before PASS | `mcp_supabase_execute_sql` | UI "18 payouts need a payout method" = **DB 18 exact** ⇒ Round 2a's MED under-count (5 vs 17) is **fixed**; a new `payout-action-required-hint` now discloses the list window |
| 7 | Load-More drive — first tap at the button's geometric centre missed | §5.1 one-miss-full-re-derive | tap (540,2152) | **Opened the Sell tab** — `load-more-button`'s centre sits inside the elevated Sell pill's hit area (§5.38 R17 class). Re-derived to the unoccluded band → (200,2140) worked |
| 8 | Load More to exhaustion (3 successful taps + 1 no-op) | F08's pagination assertions need the end of the list | taps + `listElementsAtEnd` ×4 | 5→10→15→**19** (= DB total), then the button **still renders** and a 4th tap adds nothing ⇒ **F08 FAIL** |
| 9 | Pull-to-refresh after the list was long | F08's 3rd assertion | 5× down-swipe | List **reset to exactly 5 rows** (Load More back at y=2117, matching the initial state) ⇒ that limb **PASSES** |
| 10 | F07 toggle arm — first attempt failed | `&` was consumed by the device shell | `am start -d "...&..."` | Handler logged `Missing key/value params` → re-fired with `\&` (R77 #12) → `Armed payout_fetch_failure=fetch_failure (read-back: fetch_failure)` |
| 11 | F07 error leg verified; alert classified | §5.4 empirical dialog-type verification | `swipe down` → AX | **`GlobalAlertProvider`** (`global-alert-button-0`) → Option B **not** needed; copy **exact**: "Failed to load payout data. Please try again." |
| 12 | Modal OK tap missed at (540,1448) | Hand-read the downscaled screenshot | tap | Missed; true AX point was **(541,1350)** — **98 px** low. Reproduces R104/R105 exactly |
| 13 | `qa-clear-overlays` did not dismiss the modal | Registry says it clears stuck overlays | deep link + screenshot | No effect → the control was a `GlobalAlertProvider` modal, not a stuck overlay. Used the AX coordinate instead |
| 14 | OCR route unavailable | — | `npm run qa:ocr --img …` | **Swift OCR fails**: the host has not accepted the Xcode licence ⇒ switched to `view_image` as the sanctioned fallback (§5.9) |
| 15 | A03 admin write via `qa:admin-config-set` | §5.37a/§5.44 R37 sanctioned shared-RPC path (same RPC the portal uses); scope → verify → revert | `set` ×2 + 4 `get` | Writes confirmed by the helper's own DB read-back |
| 16 | A03 first app check showed **$5.99 (stale)** — did NOT file it | R96: a deep link into an already-mounted route re-focuses the retained screen; I had visited `upgrade-plan` earlier | deep link + AX | Descended instead of filing: checked the normalizer, then did a **cold relaunch** |
| 17 | Cold relaunch hung on a blank root view | R87/R101 recovery discipline | `terminate`→`launch`→tap Metro row | Blank for ~10 min. Metro proved healthy (28.16 MB bundle / 0.35 s), qemu 5% ⇒ client-side |
| 18 | **Nearly filed "dev client is dead"** from the empty AX tree | — | repeated `listElementsAtEnd` | **A screenshot showed the Dashboard fully rendered** while the tree reported only `action_bar_root`. Recovered via `am start` deep link. ~25 calls lost ⇒ rule proposal recorded in report §5 |
| 19 | A03 re-tested on the **fresh process** | Only a fresh process clears the config cache + route stack | deep link + AX | **$6.99** on Upgrade Plan and **$6.99/mo** on Compare Plans ⇒ **price limb PASSES**; revert → **$5.99** re-observed ⇒ revert confirmed in-app |
| 20 | A03 fee limb still "$1.49 flat" → named the writer | R100: name the WRITER before filing | `grep getActiveMemberFeeCents -A22` + SQL | `getActiveMemberFeeCents` reads **`buyer_fee_active_member_cents`** (149) with a **hardcoded 149 fallback** — not the guide's `transaction_fee_subscriber_cents`. 4 competing fee keys enumerated |
| 21 | C03 render → "Next Billing Date **July 27, 2026**" though the DB says 2026-09-27 | Cross-check every date against the row AND the app's own other screen | SQL (`trial_end_date`, `next_billing_date`) + `grep periodEndDate` | **Root cause**: `subscription_expires_at \|\| trial_ends_at` (omits `next_billing_date`) ⇒ stale trial date; `daysLeft` ≤ 0 ⇒ **no days-remaining row**. **C03 FAIL** |
| 22 | Cancel chain ordered C04(abort) → C05(confirm) → C06(read) → D05(reactivate) → C07 | The chain ends with test-buyer **restored**; the guide's actors are `test-buyer`; no disposable subscriber fixture exists (`qa:r41-sub` has no status reset) | taps + 4 SQL read-backs | C05 abort → DB unchanged; C05 confirm → `status=canceled` + `cancel_reason='too_expensive'` + "Cancellation Confirmed"; C06 read; D05 reactivate → **`active` again, `current_period_end` unchanged (no new charge)** |
| 23 | Reactivate control search | Guide expects **[Reactivate Membership]** | Manage scrolled ×2, My Subscription | **No such control** on Android — the affordance is the Auto-Renew switch (". . . unless you re-enable it"). Doc-drift + an affordance finding |
| 24 | C04 closed via the `cancel-subscription` deep link | The retention screen is its own registered route; Manage's button leads to the reason modal (C05's surface) on this build | deep link + tap "Keep My Benefits" | Retention copy exact ("We'll miss you!", red X list, $45/month line, end-of-cycle disclaimer); Keep My Benefits → back with plan still **ACTIVE MEMBER** ⇒ **C04 PASS** |
| 25 | Final fixture read-back | Leave the environment disclosed, not assumed | SQL | `active` / `cancel_at_period_end=false` / `auto_renew_enabled=true`. **Residue flagged:** `cancel_reason` + `cancelled_at` persist on the active row |
| 26 | Standing off-brand-hex sweep with the **full** BP-82 list (R62d) | R62b's 3-hex subset is blind to the system-blue family; a pass is not "clean" on the subset alone | `grep -rEn` (quoted `--include`) | 50 hits / 19 files; triaged → **1 live deviation** (`NotificationSetup.tsx` ×3 `#4CAF50`), 2 comment-only, rest mostly known-dead |
