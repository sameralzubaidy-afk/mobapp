## 2026-09-17 (apply-handoff) — §5.76 R79-1 extended (new limbs R79-1a/R79-1b) + §5.75 R94 extended (new item 9) — SUB Android Round 8 FINAL CLOSING apply-handoff

- **Source suggestion:** the SUB Android Round 8 §8.3 handoff "Suggested to Improve Agent Rules" — the ONLY handoff/suggestion in that chat (2 parts). Both landed as **in-place extensions of existing rules; no new R-numbers, no BP-N rules** (nothing app-code-specific).
- **PLAYBOOK** `.github/instructions/QA-Test-Agent.instructions.md`:
  - **§5.76 R79-1a** — a bundle can be FRESH for one module and STALE for a **newly-ADDED file**; when a change ADDS a file the R79-1 discriminator must be a behavior only the **new file** can produce, never a sibling module's string.
  - **§5.76 R79-1b** — ordering for a suspect deep link/enablement: **COLD RELOAD FIRST → R111 re-fire → sibling control test** (the reverse order cost ~10 calls this round). Also records that a custom-listener sibling (`qa-dev-toggle`) is the correct control for the *handler* channel while a navigator-linked sibling (`sp-history`) only exercises the *linking config*.
  - **§5.75 R94 item 9** — the **falsy sibling**: a tool's own FAIL line is not proof either. `qa:set-sp-balance` emits `❌ VERIFY FAILED: read-back does not match the requested state` on a fully successful, DB-verified update (reproduced both directions); **the DB read-back is authoritative**.
  - Both R94 and R79 evidence/origin lines extended with the new provenance.
- **AGENT FILE** `.github/agents/QA-Test-Agent.agent.md` §4: dated **2026-09-17** pointer bullet prepended to the dated list.
- **Duplicate check (before adding):** grepped `.github/**` — no existing rule covered "bundle fresh for one module / stale for a new file", and R94's items 1–8 covered only the *success*-signal direction. No dev-side (`BP-N`) rule covers Metro staleness for a newly-added file (nearest: BP-54 dynamic import; `mobile-client.instructions.md:~416` device-pass discipline) ⇒ **nothing added to the dev files**, per the "nothing app-code-specific" precedent (same call as the 2026-09-10 apply-handoff).
- **Why:** `qa-subscription-status` no-op'd 3× while `qa-dev-toggle` (alert), `qa-login-as` (persona switch) and navigator-linked `sp-history` all worked, device logs proved ReactNativeJS forwarding yet contained no handler line, and the matcher was byte-identical to a proven sibling — every signal pointed at a broken enablement. A **cold dev-client reload** fixed it. Evidence: `e2e-test-results/qa-sub-android-r8-final-2026-09-17/report.md` §7.1 + `ledger.md` step 40.
- **Scope note (flagged, not silently taken):** the prompt's step-3 destination list names the 4 app-code instruction files + the builder agent file; these QA-agent operating rules were applied to the **QA playbook + QA agent file**, which is this repo's established destination for QA-side suggestions (see the 2026-09-10 → 2026-09-13 dated entries). No new file or section was created.


## 2026-09-13 (FIX-Task-26 round-2 verification + bundle-offer chain + N2-C01 live) — Phase A 3/3, Phase B finding RETRACTED, 16 PASS / 1 PARTIAL, 3 new playbook rules (R103–R105)
- Run `e2e-test-results/qa-fix26r2-verify-bundlechain-n2mut-2026-09-13/` (report.md, ledger.md, 32 screenshots). **Android `Medium_Phone_API_36.1` only** (iOS booted NOT driven — R80); admin portal up, never reached. HEAD `a6ce831a` (round-2 delta; prior round drove `e94b8ab4`). R29 busy check ran first; **neither of the two Metros (`:8081`+`:8082`) was killed** (standing rule).
- **Fresh-bundle discriminator (R79-1) satisfied with ZERO extra cost:** the F1 copy fix is unreachable pre-`a6ce831a`, so driving the wrong-password login *doubles* as the fresh-bundle proof — no 4-min cold reload was needed. **Reusable technique.**
- **Phase A 3/3 PASS.** F1 wrong-password → "Login Failed" / **"Invalid email or password."**, no project ref / internal URL / `cf-ray` / cookie ⇒ **closes the prior round's F1-b (MED–HIGH)**. Cart removal SUCCESS path: DB 1→0 **first attempt**, badge removed, empty state, no error card. My Listings pill now reads **"My Trades"**.
- **🔴 Phase B RETRACTION (highest-value correction of the round):** the prior round's "Item Detail Seller Info block is ABSENT on a healthy backend" **plus its null-seller hypothesis are both WITHDRAWN**. The block renders — it sits **below the fold behind a sticky CTA band** (`view-cart-button`/`request-to-buy-button` pinned y≈1949) and needed **2 swipes** to reach (screenshot `14-…` top viewport vs `16-…` after scrolling). `contact-seller-button` + `view-seller-profile-button` present and correctly `disabled`. ⇒ **new R103.**
- **Phase C PASS:** **F7** (post-accept copy is status-aware: "You accepted this offer — the trade is now in progress.", NOT "expired") · **F8 both stages** (3-item banner "Bundle offer · 3 items" ⇄ `accept-bundle-button` "Accept all 3 items"; 2-item banner + "1 already accepted" ⇄ "Accept the other 2 items") · **F9 both roles** · **UX1** · **UX2** ("4 offers to review") · **UX3** · **F10**. **F4 🟡 PARTIAL** — resolved money exact and gating source-verified (`moneyReady`, `'—'`, `disabled={!moneyReady}`; the old `false` default = the $0.99 first-paint bug), but the pre-settle frame is **uncapturable** because the ORDER SUMMARY + submit CTA sit below the fold at entry. **F6** not inducible (no offer-load-stall toggle).
- **Phase D: N2-C01 ✅ PASS live** (was PARTIAL). Byte-identical re-POST of `create-trade-offer` with the same `submission_nonce` → **409 `DUPLICATE_OFFER`**; DB = **1 trade / 1 PI / 1 audit row per key** (`offer_`, `pi_`, `tax_quoted_`). Method = fixed `submission_nonce` + byte-identical replay + R24 separate-statement read-back — **generalises to C02/C03/C04/C05/C06/C09.** C09-adjacent deviation: the guide expects a duplicate key to *return the prior result*; the EF *rejects* the replay.
- **Money closure (reusable reference):** bundle $63.00 subtotal + **ONE** $1.49 fee (one-fee-per-bundle) + **$2.94** tax = **$67.43**; DB 6300+149+294=**6743**. Tax = 2×2100×6.99% with the **Books item tax-exempt** (`taxable_amount_cents` 0). Checkout label now plain **"Sales Tax"** (round-1's blended-label finding is fixed).
- **Findings:** **F3 MED (agent-enabler)** — `qa-login-as?persona=test-seller` failed **"User profile not found"** while `profiles.id = profiles.user_id = 14be337c` and the auth user both exist; retry logged nothing and left the client wedged on "Loading trade…"; the persona only took after force-stop + cold relaunch. **F2 LOW (layout)** — sticky CTA band **clips** the Seller Info footer note (renders 797×**4** px). **F4 observation** — `disclaimer_acknowledged` stamped on only **1 of 3** bundle trades (same shape as the one-off fee); writer not read ⇒ observation, not defect. **F6 harness** — `qa:create-bundle-fixture` needs an EMPTY active cart (`CART_ACTIVE_LIMIT`) and **orphans its created `items` rows** on failure.
- **Tooling:** AX coords are **1:1 with screenshot pixels on Android incl. with the IME up** (content does NOT shift) — 2 taps lost to *visual* y-estimates while the tree was right both times ⇒ **new R104**. AX-capture key shape depends on FORMAT (`format=json` vs text) and a JSON-key grep against a text capture silently returns 0 ⇒ **new R105**; `qa:ax-tree` does **not** parse the chat-session-resource capture. **Dev-client bundle-load wedge reproduced** (client waits on ":8082" while that Metro is idle at 0.1 % CPU) — **new workaround: force-stop → cold launch → pick the OTHER dev-server row (`:8081`)**, which loaded. **`Gateway Timeout` degradation recurred** (chat-count ×2 + `[listing] getListingSummary` LogBox) while **direct SQL stayed sub-second and `qa:ef-repro` EFs worked** ⇒ degraded RPC/gateway leg, not a backend outage; wedged Trade Timeline = environment artifact (R102 applied, BLOCKED-class).
- **R100 reconciliation (do not mis-file):** My Trades "Completed" tile **36** vs `status='completed'` **41** — reconciles exactly as `buyer_marked_completed_at` **32** + `auto_completed_at` **4** = 36 (buyer-completion definition, not raw status).
- **By design:** after a single accept the app navigates to My Listings (`ReviewOfferScreen` L265/299/332); the Trade-List bundle card CTAs carry **no count** (the counted label lives on the Review Offer screen ⇒ score F8 there). `cart-item-open-<id>` uses **`cart_items.id`** while `qa:create-bundle-fixture` prints **`items.id`**.
- **Tracker (R52):** `TRD-TC-N2` Remaining-row **note updated** with the live C01 result + the still-owed limbs (C02/C03/C04/C05/C06/C09). **No row-status flips** — the N2 aggregate is not fully verified, so it correctly stays Remaining.
- **Playbook:** added **§5.82 (R103–R105)** to `QA-Test-Agent.instructions.md` via `apply-handoff-rule-suggestion.prompt.md`.
- **Phase E NOT STARTED** (N remainder, T03/T12–T14, S, M, V/X/Y all owed; **T03's low-SP persona still not built** — test-buyer holds 459 SP vs 8–15 caps).

## 2026-09-13 (2nd round) — Group N/N2 + T03/S/M + V/X/Y leftovers: **Phase 0 (a)/(b)/(d) VERIFIED PASS**, (c)/(e) env-blocked; X12 PASS, X14 PASS, X16 DOC-DRIFT; Phases 1–3 NOT started
- Run `e2e-test-results/qa-groupn-n2-phase0-2026-09-13/` (report.md, ledger.md, screenshots). **Android `Medium_Phone_API_36.1`** (iOS booted NOT driven — R80); admin portal up, never reached. HEAD `5aad09d7`. ≈144 calls / 7 verdict items ≈21/verdict (18% pure outage handling).
- **FIX-Task-25 mobile fixes ARE real on-device.** **(a) PASS** — Confirm-All-N → **"Done! All 3 items marked as completed."** → My Trades self-updated to **0/0/0/42** with no `qa-refresh`/no pull-to-refresh (the old frozen "IN PROGRESS · N items" card is gone). **(b) PASS** — after an *individual* accept, re-entry showed **"Accept All 2 Items"** + modal **"Accept all 2 items?"** (pending-only) and the accepted sibling was no longer actionable. **(d) PASS** — banner sub-line **"Auto-completes in 71h 57m"**. **(c)/(e) BLOCKED (env)** — see the `listing.seller` gate below.
- **🔴 NEW HIGH FINDING F1 — the login-failure modal dumps the raw serialized fetch `Response`** (reproduced 2×): project ref, internal Supabase URLs, Cloudflare ray/request IDs, and a **live `__cf_bm` cookie value**, instead of friendly copy. Both a §6.3 raw-developer-string leak and an information-disclosure defect.
- **🔴 NEW FINDING F7 (MED–HIGH) — a just-accepted offer is labelled "expired".** `ReviewOfferScreen.tsx:658` is a **two-way** ternary on `offer.status === 'pending'`; the else-arm hard-codes *"This offer has expired and can no longer be accepted."* so `in_progress`/`completed`/`disputed` all read as expired. **Make the else-arm status-aware** (the screen already does expiry correctly at L428 via `cancellation_reason === 'Offer expired'`).
- **F8 (LOW–MED)** — same screen: banner **"Bundle offer · 3 items"** beside **"Accept All 2 Items"**. Banner uses `bundleSiblings.length + 1` (L446) and the siblings query (L171-193) has **no status filter**; FIX-Task-25 fixed the BUTTON count (L776/782) but not the banner. **F9 (LOW)** — My Trades summary tiles lag the list within one frame after an external accept (tiles 3/0 while the card read "2 items"), reconciled only by pull-to-refresh. **F10 (LOW design)** — My Listings' selected "All" chip renders **system blue**. **F11 (LOW–MED)** — `ItemDetailScreen.tsx:838` gates the WHOLE Seller Info block on `{listing.seller && …}`, so a failed seller join silently deletes the card, `contact-seller-button`, `view-seller-profile-button` **and** the more-from-seller CTA (no error, no skeleton).
- **⚠️ STAGING OUTAGE SIGNATURE (cost ~40 calls; recognise it fast):** `POST /auth/v1/token?grant_type=password` → **504** (Supabase gateway headers `sb-gateway-version`/`sb-request-id`/`x-envoy-attempt-count`) · app LogBox **`57014` statement timeout** · direct SQL **"Connection terminated due to connection timeout"** · fixture scripts **"Gateway Timeout"**. The **edge stays healthy** (unauthenticated POST → `401` in ~0.1 s), so it is the upstream auth/DB leg. **The app's own log names the culprit** — check logcat before theorising.
- **TOOL/FACT (new):** **`qa-login-as` does NOT fire on the Landing screen** — the handler is mounted in the authenticated stack only, so a fully logged-out app cannot be recovered by deep link and needs a **UI login**. Fixture credentials live in `p2p-kids-marketplace/scripts/seed-staging-data.ts` (`TEST_USERS`) — read them, never guess. Also: **Discover search works** (tap `discover-search-input` → type → **press BACK to dismiss the IME, screenshot to confirm, then tap the result** — tapping with the IME up hits keys).
- **Tracker drift CORRECTED (R56):** the file header (270/35/0/**6**/3/**19**) and the TRD section header (270/35/0/**7**/3/**18**) disagreed, and X11/X12 had been **counted into the Remaining header without rows ever being added**. Now reconciled: **271 PASS / 35 PARTIAL / 0 OPEN / 7 DOC-DRIFT / 3 SKIPPED / 17 Remaining = 333 ✓** (X12 → PASS; X11 row added). Verified by counting `^| TRD-TC-` row lines = **333**.
- Residue: bundle `15b77972` **fully completed** (3 real Stripe captures); 3 fixture listings `sold`; app left **logged in as test-buyer**; test-seller Basket badge `4` = known stale 2026-08-29 `cart_items` (R100: real count, not a leak). No config/admin writes.

## 2026-09-13 — Group L final legs + M/N/S/T remainder + Round 3 (Android): 33 verdict items, P1 3/3 PASS, 2 tracker flips
- Run `e2e-test-results/qa-groupl-final-mnst-round3-2026-09-13/` (report.md, ledger.md, screenshots). **Android only** (iOS booted NOT driven — R80); admin portal up but not reached. HEAD `94c8fb79`. ≈108 calls / ~33 verdicts ≈ **3.3 blended (best of recent rounds)**.
- **L02 caveat CLOSED** — full 3-step Confirm-All-N incl. the shortcut **reappearing as "Confirm all 3 items received?"** on a different sibling of a partially-completed bundle (first real end-to-end verdict since FIX-Task-18/19). **L08 individual leg now driven** (only that trade advanced, DB-verified). **O2-C09 ✅** — fast-clock + **POST the `process-auto-complete` EF (never the bare RPC** — the EF does the capture + `rpc_mark_tax_collected`).
- **Tracker: 2 flips → TRD 268 PASS / 35 PARTIAL / 0 OPEN / 6 DRIFT / 3 SKIPPED / 16 Remaining = 328 ✓.** O2-C09 PARTIAL→PASS; **V01 OPEN→PASS** (was the *single* TRD OPEN row; its own note already said "superseded verdict: PASS" but the cell was never flipped). **Teaching: when a Notes cell supersedes a Status cell, flip the cell in the same pass — a stale FAIL silently inflates OPEN for rounds.**
- **P2/P3 scope truth (R78-2):** M/N/S/T/U/V/X/Y/N2 contain **zero NEVER RUN rows** — those blocks are Android-coverage re-verification, not first verdicts. Session stopped **inside Priority 2**; N remainder, T03/T12–T14, most of S, and **all of N2** were not reached.
- **New findings:** F1 stale Trade List after Confirm-All-N + F2 stale Review Offer after an individual accept (both R59) · F3 contradictory Seller-Info affordance (Hidden copy + enabled Contact Seller/View Profile) · F7 Contact Seller/View Profile lack testIDs · **F6 the TRD X-group INDEX descriptions ≠ the guide's X BODIES — score X rows against the BODY** · F8 the EF's `items:[…]` batch path only stamps `bundle_id` when the caller supplies it.
- **F4 env:** a **warm `qa-login-as` persona switch can wedge the app** (perpetual spinners + `[trade] Error counting active trades: {"message":""}`); an **empty** message = fetch-layer stall, not PostgREST → force-stop + cold relaunch (R92); `qa-logout` alone does not clear it.
- **R100 near-misses killed:** test-seller's Basket badge 4 (real stale 2026-08-29 cart rows) · Trades badge 4 (correct — `472ef43a`'s seller is test-seller-3) · Books trade missing a `tax_records` row (correct for `tax_exempt_goods`) · the Trade Basket's "back button" (an **empty spacer** — AppHeader `variant='tab'`; the compressed thumbnail fooled me ⇒ **use `view_image` at full resolution before filing a header-pattern finding**).
- **Technique:** completing a bundle leaves `cart_items` `active` against now-`sold` listings ⇒ free M13 + S07 coverage with zero setup (§5.33). "Replace Trade Basket" is the cheapest cart rebuild for cart-cluster cases.

## 2026-09-12 (3rd round) — P2 remainder → Group L / O-1 C07: 7 PASS, O-1 C07 + O-7 CLOSED, 1 doc-drift; two near-miss false positives caught
- Run: `e2e-test-results/qa-p2-remainder-groupL-2026-09-12/` (report.md, ledger.md, screenshots/ 6). **Android `Medium_Phone_API_36.1` + live admin :3001 + read-only DB** (iOS booted, NOT driven — R80). HEAD `da131a69`. ≈130 executions / 12 verdict-items ≈10.8 blended (device-only ≈15.6).
- **TRD-TC-O2-C11 ✅** — mark_tax_collected ×2 → `action:'idempotent'` (guide-exact); refund 100¢ ×2 → `REFUND_EXCEEDS_COLLECTED` (cap guard, **not** an idempotent ack); `refunded_tax_cents` stayed 100; sp_ledger = ONE entry per op; **0 trades with >1 seller_payouts row**.
- **TRD-TC-O2-C04 ✅** — real-UI SP offer (test-buyer, $25 item, 15 SP): trade `472ef43a` `taxable_amount_cents`=**2500 = FULL price (BP-37)** / tax 175 / cash 1000 / fee 149; `payments.total_charged_cents`=**1324** with `status='requires_capture'`.
- **TRD-TC-O2-C05 ✅** — accept via the real `transactions-update` EF as test-seller-3 → in_progress, tax still `quoted`, `tax_records.captured_at` NULL.
- **TRD-TC-O1-C07 ✅ (was STILL OPEN)** — FIRST REAL bulk session: Sell→Bulk Upload→**real system photo picker (1 image + Done) creates the session**→dev-fill→Submit 1 Item→"Thanks for submitting!" ⇒ item `bef903a8` pending, Books → **`tax_exempt_goods`** (mapped, not the flat default). Caveat: 1 item not 2+.
- **TRD-TC-O07 ✅ (was PARTIAL)** — buyer refund card live: $26.75 / **$1.75 tax** / MASTERCARD 4444 / date, all matching DB.
- **TRD-TC-O3-C05 → 📄 DOC-DRIFT** — `open-dispute` rejects non-`in_progress` ⇒ "complete a trade then dispute" is UNREACHABLE; not a product gap. **O3-C06** stays PARTIAL (3-layer guard: RPC cap + `ALREADY_RESOLVED` + Stripe already-refunded reconciliation; the guide's `action:'idempotent'` expectation is wrong for that path).
- **🔴 TWO NEAR-MISS FALSE POSITIVES (highest-value lesson):** (1) `payments.status='captured'` + `captured_at` 97 ms after accept LOOKED like a HIGH early-capture defect — but **`fn_payments_sync_from_trade`** (trigger `trg_payments_sync_from_trade`) maps `in_progress → 'captured'` by design: it is a **derived mirror, NOT the Stripe state**, so O2-C05's "PI in requires_capture" limb is **not DB-checkable**. (2) the Refund card's "Sep 11" vs `created_at` 2026-09-12T01:46Z = **local-time rendering**, not a mismatch. ⇒ **standing rule: name the WRITER of a column value before filing any finding.** `admin_health_strip` only counts `failed`, so no metric is corrupted (severity bounded).
- **F2 (MED, QA drive-recipe):** `rpc_process_expired_offers` (bare RPC) cancels the trade but **never voids tax / never cancels the PI** — both live in the `process-expired-offers` **EF**, which pg_cron `*/2` calls. Driving the RPC directly (the standard fast-clock recipe, used by R03) **bypasses both legs and permanently leaks `tax_status='quoted'`** (3 live rows). **Fix the recipe: POST the EF.**
- **F3 (MED env):** the picker fix is PARTIAL — `qa:android-seed-media` registers 24 MediaStore rows but the **system picker shows only 1 image** and "From this device" says **"No photos yet"**; the script's guard is a false negative when assets are already registered; it also falls back to `assets/` (app icons) since `assets/qa-media/` does not exist. Single-select + Done **works** (Android picker IS AX-drivable).
- **F8 (LOW, harness hygiene):** 25 cancelled trades carry `tax_status='quoted'` with reason `buyer_cancelled` — writers are ALL QA/seed harnesses (`cleanup-test-trades.ts`, `qa/reset-offer-fixtures.mjs`, `seed-staging-data.ts`) ⇒ **not a product defect**; inflates report `pending_tax_cents`.
- **FIX-Task-23 verification: item 1 ✅ FIXED** (`qa:r41-first-trade -- create` provisions at `…017`); **item 2 ✅ FIXED** — reproducible lock-hold test: gate settles at 516 ms into the new freshness UI then **redirects to /auth/login at ~4 s** (`AUTH_TIMEOUT`, AdminShell.tsx:69); **item 6 ✅ NOT A DEFECT** — Price Breakdown renders on scroll ($40+$1.49+$2.80 = $44.29 exact).
- **Dev-build friction (reproduced ×2):** the **LogBox notification overlay eats taps** on the bottom CTA band ("Submit for Review", "AI auto-fill") until its ✕ is dismissed. Dev fixtures also **grey out** in the Bulk "Group" step once a real photo is added — fixture and real routes are not freely composable.
- **Cold dev-client start ≈ 4 min** (terminate→launch→splash→Dev Launcher→Metro row→blank frame→Home). `adb logcat`/`dumpsys` diagnosis beats screenshot-polling.
- Tracker (R52): **TRD 262/37/2/5/3/19 → 267 PASS / 35 PARTIAL / 1 OPEN / 6 DOC-DRIFT / 3 SKIPPED / 16 Remaining = 328 ✓** (verified: §1 roll-up ⇄ section header ⇄ never-run header/body = 16). Residue: trade `472ef43a` in_progress (auto-completes 2026-09-16), item `bef903a8` pending, `3265ec84` tax record partially_refunded, `qa-first-trade` persona created, admin page signed out at /auth/login, Android left as test-buyer on the Bulk Upload success screen. **NOT reached: Group L's 2 open sub-legs (L02 Confirm-All-N, L08 individual-action) — its 11 rows already carry PASS verdicts, so the brief's "11/11 untested" is a session-scope statement, not a status statement (R78-2).**

## 2026-09-12 (2nd round) — FIX-Task-22 VERIFICATION: N1 fixed + prior HIGH retracted; Q19 re-driven; R04 promoted to PASS; R03 PARTIAL; K02 first-trade leg closed
- Run: `e2e-test-results/qa-fix22-verify-p2p3-2026-09-12/` (report.md, screenshots/ 6). **Android `Medium_Phone_API_36.1` + live admin :3001 + read-only DB** (iOS booted, NOT driven — R80). ≈160-170 executions / 13 verdict-items ≈6.3 blended.
- **N1 ✅ FIXED — the previous round's HIGH is RETRACTED (R99).** Drove the real admin UI on an ACTIVE page after `page.unrouteAll()`+`page.context().unrouteAll()`: response body = the route's true 4-key shape (`success,message,review_status,reporters_notified:1`), queue **14→13**, DB `is_hidden=false`/`review_status='reviewed'`/`report_count=0`/`has_been_reported=false`/0 `review_reports`, `user_notifications.review_report_kept` @22:43:53Z. Review **`44f5662f` independently confirmed restored** (dev's keep @22:12:52Z). Confirm copy matches the guide exactly.
- **NEW MED-HIGH TOOLING DEFECT (F1): `npm run qa:r41-first-trade -- create` CANNOT run** — `scripts/qa/lib/r41-common.mjs:49` maps `qa-first-trade` → id `a1234567-…-014`, **already occupied by the DEV-TASK-96 `test-noconvo` persona** (`a1234567-…-014`), so `createUser` fails "Database error creating new user". Use standing **`test-noconvo`** (`fee_state='no_completed_trade'`, 0 completed trades, `status='free'`) as the first-trade persona instead; K02's first-trade leg then PASSES ($23.00 + **$1.49 flat** + 6.99% $1.61 = **$26.10** exact).
- **NEW ADMIN-PORTAL ROOT CAUSE (F2): a stuck Web Lock hangs the auth gate forever.** `navigator.locks.query()` on a wedged page → `held: ["lock:sb-<ref>-auth-token"]` + **19 pending**; `ProtectedLayout.getUser()` never settles ⇒ **"/reviews" shows "Loading…" indefinitely** (no timeout/redirect). Closing the holder page releases all waiters instantly. Precondition: "Multiple GoTrueClient instances in the same browser context" (each page makes its own client). Recovery when an admin page is wedged: **close/replace the page** (R81 fresh active page), then reload.
- **NEW adb FIX (F7 — supersedes "craft single-param fragments" as the only option): escape the `&` FOR THE DEVICE SHELL.** `adb -s emulator-5554 shell am start -W -a android.intent.action.VIEW -d "p2pkidsmarketplace://qa-dev-toggle?key=card_decline\&value=hold_decline" <pkg>` — the local shell keeps it quoted against zsh globbing while the backslash survives into the device command line. Without it: `sh: com.<pkg> inaccessible or not found` + the toggle silently never arms (R77 #12). The handler's own alert is the arming VERIFICATION: "[QA] Toggle Applied — card_decline = hold_decline (verified read-back: hold_decline)".
- **R04 → ✅ PASS (promoted out of NEVER-RUN).** Toggle `card_decline=hold_decline` + `qa:ensure-cards --persona test-buyer` (test-buyer had **no** saved card; script restored `pm_1UEzkz4I…`) → **R92 applies: the running client still showed "Add New Card" until ONE terminate+relaunch** → Send Offer → **"Checkout Failed / Payment method declined. Please update your card."**; DB `buyer_pending_trades 0`, `sp_wallets.reserved_sp 0`, available 474 unchanged. Disarm verified by read-back `none`.
- **R03 → 🟡 PARTIAL.** `qa:ef-repro --ef create-trade-offer` → pending `575056d1…`; fast-clock + `rpc_process_expired_offers()` → `expired_offers_processed:1` + 2 notifications; DB `cancelled` / **`cancellation_reason='Offer expired'`** (friendly string, not `cancelled_expired` — doc-drift) / `cancelled_at` stamped. **Missing:** competing-offers limb; SP-restore N/A by state (`sp_amount=0`). ⚠️ **R24 reconfirmed:** an RPC called in a CTE + the row read in the SAME statement returns PRE-update state ("pending") — always re-query.
- **Q19/Q20/Q15 re-driven on Android** (one flow, persona test-seller-3, review `44f5662f`): Q15 report (4 reasons, "Review reported. Thank you!", `report_count` 0→1) → Q20 Hide (row stays badged "Hidden", "Restore review", "↺ Undo Hide" — FIX-Task-22 item 6 live; total stays 14; `review_report_hidden`) → Q19 Keep (queue 14→13; full DB closure).
- **N2 🟡 PARTIAL:** the mapping is source-verified + unit-covered, but a **network cut is absorbed by the app's global `offline-screen` gate** ("No Internet Connection"), so the 504 transient branch was NOT re-induced live. N3 ✅ (live `0/500 characters` + prose placeholder). N4 🟡 — it IS a documented **scripted recipe** (`qa:android-seed-media`, bash-3.2-safe); MediaStore verified at **24 registered images** via `content query`; the picker re-entry leg remains the missing piece (R98 step 4) and still blocks Group L's photo paths + O-1 C07. N7 ✅ (guide now ties the tax category to the product-category mapping). Tracker materialisation ✅ (43 sub-case rows present).
- **Tracker (R52):** TRD **262 PASS / 37 PARTIAL / 2 OPEN / 5 DRIFT / 3 SKIPPED / 19 Remaining = 328 ✓** (R04/R03 left the never-run table: 21→19); K02/Q15/Q19/Q20 notes refreshed; §1 roll-up + section header + never-run header reconciled.
- **Residue:** new review `4a87da53` (test-seller-3 → test-buyer, 5★); trade `575056d1` cancelled by the R03 drive (`b9e8918e` still available); `44f5662f` restored; test-buyer has a valid saved card again; Android left logged in as test-buyer; admin page `730f3326` open at /reviews (a `window.confirm` auto-accept override remains in that page's realm — gone on reload); **no config writes, no stubs registered**. **NOT reached: O-2 C04/C05/C11 · O03/O04/O06–O08 · Group L (11) · O-1 C07.**

## 2026-09-12 — TRD FINAL WRAP-UP (Android): P1 discharged except C07; 10 PASS / 1 BLOCKED / 1 FAIL; **admin Keep is a silent no-op (N1, HIGH)**
- Run: `e2e-test-results/qa-trd-final-wrapup-2026-09-12/` (report.md, ledger.md, screenshots/ 26). **Android `Medium_Phone_API_36.1` only** (iOS booted, NOT driven — R80). Manual tally ≈210 executions / 13 verdict-items ≈16 blended (device-only ≈9/verdict), desk work isolated (R71-fallback).
- **O-1 CLOSED except C07:** **C06 ✅** (real `CategorySelectModal` — IS AX-drivable on Android — picked **Games** → new item `591053d3` → `tax_category_id`=`general_tangible_goods` = the guide's expected key). **C15 ✅ all 7 steps** (pre-flip Books item `c1b3cd63`=`tax_exempt_goods` → admin flip Books→General Tangible Goods (DB @21:35:04.543Z + `category_tax_mapping_changed` audit row) → post-flip Books item `ba961cae`=`general_tangible_goods` **51 s later, no deploy** → first item unchanged → **restore DB-verified**). **C07 🟡 BLOCKED** — the dev-fixture bulk path reaches the confirm sheet but Submit = **"Cannot submit for review / Missing bulk session or draft session."** (fixtures make items+grouping, not a bulk session), and the real picker path was blocked by an **empty emulator gallery**. Not a new defect (43g K05 class).
- **Q20 ✅ PASS (full shipped Hide semantics)** — reviewee-only `review-menu-button` (own profile only; NEVER on the public SellerProfile ⇒ Q17) → 4 reasons → report row + `report_count 1` → admin Hide → row **stays in queue badged "Hidden"**, total stays 14, DB `is_hidden=true`/reports retained, public profile **Reviews (2)→(1)**, `user_notifications` **`review_report_hidden`** written.
- **🔴 Q19 FAIL + NEW HIGH FINDING N1: admin Keep reports success but does NOT persist.** `POST /api/reviews/<id>/keep` → **200 `{"success":true}`** while DB stays `is_hidden=true`/`report_count=1`, reports survive, **no `review_report_kept` notification**, row never leaves the queue. **Reproduced 4× incl. after restarting the admin dev server.** Two anomalies: the body ≠ `keep/route.ts` on disk; the dev-server log shows **no POST for that path and no compiled `keep` route** (`hide` compiles+persists fine); no catch-all route, no SW/MSW. ⇒ **a hidden review cannot be restored via the UI** (Q20's Hide is irreversible today). **Residue: review `44f5662f` left hidden.**
- **Group Q Android legs (after an R79-1 cold reload — the client had a STALE bundle): Q01 ✅ · Q04 ✅ · Q05 ✅ · Q06 ✅ both branches · Q15 ✅ · Q17 ✅. FIX-Task-21 items 2 (CTA a11y label + counterparty-name title) + 17 (`0/500` placeholder) re-verified on the fresh bundle.**
- **NEW TOOLING FACTS:** (a) **an AX-tree read OR screenshot taken inside a `mobile_batch_commands` batch is PRE-render** — a click+screenshot batch reliably returns the pre-click UI (cause of several false "nothing happened" readings and one mistaken navigation); read post-action state in a SEPARATE call. (b) **a `create-item` deep link RE-FOCUSES the retained ItemCreate screen** — previously filled photo/title/price/**category** survive into the "new" listing, and a still-open success modal stays on top. (c) **emulator gallery is empty by default**; `adb push <assets> /sdcard/Pictures/QA` + `content call --uri content://media/external/file --method scan_file --arg <path>` DOES register media (`content query` showed 12 rows) but the picker did not surface it on in-place re-entry — enter the picker from a freshly mounted screen. (d) `qa-login-as` fired immediately before a navigation deep link RACES the handler's nav-reset (the reset wins and returns you Home) — settle the persona first. (e) `public.notifications` does NOT exist — the table is **`user_notifications`**. (f) the admin taxonomy `tax_categories` keys = general_tangible_goods / clothing_footwear / tax_exempt_goods / review_required; `categories` display order starts **Books** (so `dev-set-category` always picks Books → `tax_exempt_goods`). (g) review-submit failure surfaces a raw **"Gateway Timeout"** to the user (§6.3 class, N2).
- Tracker: dated round note added at the top of the TRD block + header Android-coverage map refreshed → **O-1 16/17 · Q 14/20 · K 10/11**; **no row-status flips** (totals stay 288 · 239/25/1/5/2/16). **P3/P4 NOT started** — session ran out inside P2; P2's O-2 C04/C05/C11 · O03/O04/O06–O08 · R03/R04 · K02 leg still owed.

## 2026-09-12 — TRD Phase 2b/2c wrap-up (Android) — 28 PASS / 5 PARTIAL / 1 FAIL-reconfirmed; the R-group's missing SP-restore + level-3 escalation legs CLOSED
- Run: `e2e-test-results/qa-trd-phase2bc-wrapup-2026-09-11/` (report.md, ledger.md, screenshots/ **22**). **Android `Medium_Phone_API_36.1` only** (iOS booted, not driven — R80). **Manual call tally ≈110 / 33 verdict-class items ≈ 3.3 blended (device-only ≈ 2.1/verdict)** — R71-fallback (transcript not mineable), desk/work isolated.
- **Fixtures:** `qa:create-bundle-fixture -- --buyer test-buyer --seller test-seller --count 3` → cart `6e06aa28-…`, items Toys/Sports/**Books (tax_exempt_goods)** all $23, all Accept-SP ⇒ a tax-exempt limb inside a taxable cart (the round's best fixture). Reused bundle `73d5eb11` (seller **test-seller-3**) for the seller-cancel chain. **`qa-first-trade` persona DOES NOT EXIST on staging** (K02 first-trade leg = fixture gap; provision `npm run qa:r41-first-trade -- create`).
- **Tier-1 money/state CLOSED:** **R02 PASS** (seller declines pending SP offer → buyer reserved 16→0 / available 458→474, DB-verified — the genuine SP-restore limb the R group never had) · **R05 PASS** (seller cancels in_progress `0788cc0e` → count 2→3 + `admin_review_flagged_at` SET = level 3; persona restored 2/NULL after, R89) · **R08 PASS** (0 payouts) · **R06/R07 PARTIAL** (tax record quoted→**voided**, 0 `trade_refunds` — correct for an uncaptured auth; refund *breakdown* + the post-completion released-SP limb unverified).
- **Tier-1 tax/points:** O01 PASS · O02 PASS (tax flat **$3.22** at SP=5 AND SP=16 while subtotal stayed $69.00 — BP-37 on Android) · O05 PARTIAL (exempt Books = $0.00 tax inside the cart; the green "Tax Free" badge NOT rendered on the cart-checkout surface) · T04/T06/T07/T08/T09/T10 PASS · T01 PARTIAL. **T07 math closed to the cent:** UI $57.71 = 69.00−16.00+1.49+3.22; DB 700+2300+2300+149(**one fee per bundle**, K05)+322. **T10's exact surface confirmed:** `includes-points-redemption-tag` on the **seller's** Needs-Action bundle card.
- **Tier 2/3 sampled:** U01/U02/U05 · X01/X03/X04/X09/X10/X12/X13 · Y01/Y03/Y04/Y09 · V02/V03/V10/V11 (**V01 FAIL re-confirmed** — tab still reads "Basket") · S07/S10/S17 · M12 · N2-C01/C03/C05 (guards + live dedup counts; a suspected double-payout was **retracted** — it was the 21-row NULL `trade_id` bucket).
- **NOT reached (honest):** every admin-portal group (O-1 17 / O-2 12 / O-3 14 / P 8 / W 12 / K07–K09) despite the portal being up · Group Q 20 · R03/R04 · O03/O04/O06/O07/O08 UI (O07 unreachable via seller-cancel — an uncaptured auth **voids**, it does not refund) · M08/M09/M10 · L02 Confirm-All-N · remaining U/V/X/Y/T/S/N2-C02/C04/C06. **≈95+ TRD rows still owe an Android leg.**
- **NEW TOOLING FACT (highest-value of the round): the Android soft keyboard is INVISIBLE to the uiautomator tree.** With the numeric IME up, `mobile_list_elements_on_screen` returns no IME nodes and content coords as if the keyboard were absent → a tap at a tree coordinate landed on a keypad key and typed into the still-focused SP field (5→16). **On Android the §5.19 Rule-1 keyboard gate can only be honoured from a SCREENSHOT, never the tree**; treat a field value that changed without a deliberate keystroke as an IME-interception signature, not an app bug. (Proposed §5.19 sharpening — see the run's "Suggested to Improve Agent Rules".)
- **Other tooling facts:** (a) cold dev-client start ≈ several minutes / ~20 polls with **Metro idle at 0.7 % CPU** ⇒ native-init/first-frame harness cost, NOT bundling (confirms FIX-Task-17 F9); (b) `qa-login-as` fired while the app is **cold** can exit the app to the launcher (warm switches fine); (c) **`qa-set-sp` is unusable via a zsh `adb shell am start`** — the `&amount=` separator is dropped (the `&` is swallowed by the shell/tool layer); use single-param deep links only; (d) **3 schema 42703s → cheat sheet corrected:** `items` has NO `deleted_at` and NO `item_payment_preference` (SP eligibility = `accepts_swap_points` BOOLEAN; the string lives on `cart_items`), and `tax_rules` thresholds are `min_item_price_cents`/`max_item_price_cents`; (e) `seller_payouts` has 21 rows with `trade_id IS NULL` — filter NULLs in any payouts-per-trade duplicate query or it reports a false duplicate group.
- **New product findings (dev-side):** F1 MEDIUM stale Basket badge after checkout (badge still "3" on the success screen + My Trades + Home while `cart_items`=0; clears only after visiting Basket) · F2 MEDIUM contradictory cart loading copy ("Accepts Points" **and** "Points unavailable for this item" on one row) · F3 LOW–MED success-screen "Consider using SP on your next purchase" shown right after a **16-SP** order · F4 LOW "Sales Tax (4.67%)" blended-rate label on a mixed-exemption cart (applicable category rate 6.99%; guide expects plain "Sales Tax") · F5 LOW pinned `seller-cancel-inprogress-button` overlaps the `next-steps-cta` "Got it" pill (~76 px).
- Session end: Android left **logged in as test-seller** on My Trades; test-seller-3 restored (2/NULL); buyer wallet net-zero 474/0; bundle `6e06aa28` left with 1 available item + 2 pending trades + 1 cancelled trade; `73d5eb11`'s in_progress trade now cancelled. No config writes. Metro `:8081` single instance + admin `:3001` left running. Tracker: dated baseline note added, **no status flips** (TRD 239/25/1/5/2/16 unchanged); Android-coverage map updated (O 3/8 · R 5/13 · T 11/14 · U 3/5 · V 5/14 · X 7/16 · Y 4/9 · S 5/24 · K 7/11 · M 5/20 · N2 3/10).

## 2026-09-11 — FIX-Task-17 verification (12 PASS/1 PARTIAL) + TRD Stage-4 wrap-up ATTEMPT (Android; Phase 2 mostly NOT reached)
- Run: `e2e-test-results/qa-fix17-verify-trd-stage4-2026-09-11/` (report.md, ledger.md, screenshots/ 10). **Android `Medium_Phone_API_36.1` only** (iOS booted, not driven). HEAD `9d995b77`. **Manual call tally ~131 / ~22 verdict-items ≈ 6.0 blended (device-only ≈ 4.2/verdict)** — R71 fallback (transcript not mined), desk/work isolated (see ledger.md).
- **PHASE 1 = discharged, all 13 items.** PASS: MCP restored (hard precondition); cold-start My Trades first paint correct (1/0/0/32 + cards, no false empty); "Offer · 1 item" singular; "Checkout Failed / Payment method declined. Please update your card." (one surface, no payout vocab, WARN not error → no LogBox); disclaimer pre-ticked on the 2nd same-session checkout; 0.55-scrim alert legible & non-bleeding; Review Offer "Safety & Platform Fee"; guide L07 ids correct + **both ids seen live**; `single-item-cta-button` present in source; single Metro enforced (killed 2 strays) with cold-connect **20–30 s of which Metro bundling = 2 013 ms** (warm bundle 261 ms) ⇒ F9's 90 s = dev-harness native init + blank first frame, NOT bundling; tracker header/§1/section all read 16/17 consistently. **PARTIAL: item 12** — `charge_one_fee_per_bundle` column IS `boolean`/`true` but `supabase_migrations` has **0 rows for 20260911%** (migration file exists, unrecorded — BP-81 out-of-band apply; do NOT re-apply).
- **PHASE 2a harvest = 8 case verdicts (7 PASS · 1 PARTIAL) on a FRESH QA-owned bundle `73d5eb11-…`** (cart_id; items under test-seller-3), NOT the Stage-3 bundle `330427dc` (its buyer `d84bcc68…` is still not a persona): L01/L03/L05/L06/L07/L09 PASS (buyer+seller views, live "Bundle offer · 2 items" count after partial completion); **L02 PARTIAL** — `I Got It` → "Confirm all 3 items received?" (`confirm-all-cancel-button`/`confirm-all-trades-button`) → **Just This One** → 2nd confirm (`complete-trade-confirm-button`) → **exactly 1 of 3 completed, DB-verified**; but re-entering the **partially-completed** bundle gave **NO confirm-all prompt** (straight to the single Complete dialog) ⇒ the Confirm-All-N leg unverified + **new LOW finding N1**; **R01 PASS (promoted out of NEVER-RUN)** — buyer cancelled pending `91a76811…`: DB `status=cancelled`, `cancellation_reason='Changed mind'`, `cancelled_at` stamped; EF `{"consequence_level":null,"sp_refunded":0,"success":true}` (SP leg N/A by state: offer reserved 0 SP). By-products: K04/K05 steps 7–10 totals ($75 subtotal / $1.49 one-fee-per-bundle / Sales Tax (4.67%) $3.50 / $79.99 cash total), M12 per-item caps, T06-family "Points remaining: 474" counter. **NOT reached (explicit R40): all of Phase 2b + all of Phase 2c — zero cases.** R62b off-brand-hex sweep RAN → **0 hits** (owed item closed).
- **Post-run environment event (same day, after my session closed):** my `:8081` Metro was **SIGKILLed (exit 137)** under multi-session load while a **concurrent round was driving the same instance**; that session then started **its own Metro on `:8082 --clear`** (`pgrep` shows only 8082). ⇒ **the F9 double-Metro ambiguity re-appears across sessions**: `:8081` is free but `:8082` is held, so a device connecting now may bind the wrong bundle. Standing response (per the 2026-09-10 precedent): **do NOT blind-restart and do NOT kill the other session's Metro** — run `pgrep -l -f "expo start"` first, and treat "a colleague session owns `:8082`" as the expected state. Note also that the concurrent log showed an **SP-carrying bundle** (`bundle_id 6e06aa28-…`, `sp_amount_cents` 500→1600, `cash_portion_cents 5300`) and a seller cancel returning `consequence_level: 3` — i.e. **T08–T11 / R05-class legs may already be being covered by that round**; do not double-count its evidence into this round's verdicts (R29/R80), but check its report before scheduling them as a gap.
- **Tooling facts for future rounds:** (a) `trades` has **NO** `price_cents`/`accepted_at`/`cancelled_by`/`escrow_status` columns and `items` has **no `price_cents`** (it's `items.price` numeric); `cart_items` carries `item_price_cents`/`item_payment_preference`/`bundle_id`. (b) **`trades.bundle_id` is stamped with the CART id at checkout**, not the `cart_items.bundle_id` the `qa:create-bundle-fixture` helper writes — assert on the cart id. (c) **Read-back ordering:** a DB read issued in the same parallel batch as the post-tap tree read can return PRE-write state (cancellation read `pending`, true ~1 s later) — read the Metro/EF line or allow one poll before the DB verdict. (d) `mobile_swipe_on_screen` still no-ops when started over item cards (R77 #10) — start on a label band. (e) Grepping a **stale** AX resource file wastes calls; re-list the tree instead (R77 #8 is only for below-fold rows of the *current* file).
- Tracker: **TRD PASS 238→239, Remaining 17→16** (R01 promoted to a real PASS row), never-run header 17→16 + R01 row deleted (16 body rows verified), L01/L02/L03/L05/L06/L07/L09 notes refreshed, new dated baseline note added; §1 + section header + table header reconciled in the same pass (239+25+1+5+2+16 = 288 ✓).
- Session end: app **logged out** (Landing); Metro `:8081` single instance left running; test-buyer restored to a valid 4444 card (`pm_1UEdUX4I…`; old card permanently detached); 2 of 3 new trades completed (real Stripe captures), 1 left `in_progress` (auto-completes ~72 h); the 3 fixture listings consumed; Stage-3 bundle untouched; no `admin_config` writes.

## 2026-09-10 (later same day) — FIX-Task-15 desk observation: my items 1–7 have fixes in the working tree; the Android `qa-scroll-to` no-scroll STILL reproduces but now fails honestly
- **Context:** while the round report was being finalised, `git status` showed `M` on `QaScrollToDeepLinkHandler.tsx`, `qaScrollRegistry.ts`, `TradeTimelineScreen.tsx`, `ChatScreen.tsx`, `NotificationCenterScreen.tsx`, `services/chat.ts`, `SafeMeetupCard.tsx`, `scripts/qa/r41-in-progress-trade.mjs` (+ tests, guide, tracker), with source labels *"FIX-Task-15 item 2 (2026-09-10): OUT_OF_VIEW was added after the Android no-op"* and *"FIX-Task-15 item 7"* (`computeTimelineBottomPadding({…, safeMeetupExpanded, safeMeetupHeight})`). A **FIX-Task-15 verification round is in flight** (`e2e-test-results/qa-fix-task15-verify-2026-09-10/` had only `screenshots/`; the Metro log showed it driving Android live) — so that round owns the new build's verdicts, and **no device interaction was performed for this observation (R29)**.
- **Evidence (Metro log of the new build, not a device drive):** `[QaScrollToDeepLink] RESULT message-button OUT_OF_VIEW NaN NaN` + `RESULT safe-meetup-toggle OUT_OF_VIEW NaN NaN`, each preceded by ~5× `measure cannot find view with tag #<N>` warnings; new metrics fields `safeMeetupExpanded` / `safeMeetupHeight` / `timelineBottomPadding` visible in `[TradeTimeline][FIX-Task-13] scroll metrics`. ⇒ **the Android scroll is still a no-op; only the reporting changed** (it no longer emits plausible-but-wrong coords). Read `OUT_OF_VIEW` / `NaN` / `measure cannot find view with tag #N` as "tool could not scroll → use the manual swipe", never as an app-side occlusion finding.
- **Artifacts updated (desk-only):** run `report.md` gained an `⚠️ ADDENDUM` block (my findings 1–7 = fix-in-flight, with the source labels + the log lines); playbook **§5.75 R94 point 2 + its evidence line** and the **§5.51b** `qa:scroll-to` caveat were extended with the post-fix signal (no new rule — extension per the duplicate-rule discipline); this memory entry. Tracker rows/baseline for my round are unchanged (I02/I05 PASS both platforms stands; the Android reachability evidence is the manual-swipe leg, unaffected by the tool change).
- **Rule for future rounds:** when a report's finding is fixed *while* the report is being written, add a dated ADDENDUM that (a) names the fix task + the changed files, (b) distinguishes desk-observed log evidence from a device drive, and (c) says explicitly which round owns the fix's verdicts — do not silently rewrite the round's own verdicts.
- **Cross-round confirmation (same day):** `e2e-test-results/qa-fix-task15-verify-2026-09-10/report.md` independently returns **V2 `qa-scroll-to` = FAIL (tooling) on Android** (*"Android still does not scroll; iOS scrolls and reports usable coords"*), reproduces `OUT_OF_VIEW NaN NaN`, and files it as *"a tooling defect (R94), not a product defect"* ⇒ my tool-leg verdict stands on the fixed build and **R94 is already being cited by the parallel round**.
- **Metro port hygiene (multi-session):** the 8081 instance started for this round was later SIGKILLed (exit 137) under multi-session load; by the time I checked, both sessions had already restarted Metro themselves (**8081 + a second instance on 8082**). ⇒ **check `pgrep -l -f "expo start"` before restarting Metro — a blind restart would have hit a port conflict.** Concurrent agent sessions on this machine each want their own dev server/port.


## 2026-09-10 — Apply-handoff codification: §5.75 R94 + §5.68 R77 #16 + §5.51b claim qualified (QA playbook + QA agent file only; no app code)
- **Source suggestion:** the QA Task Android Item-1 re-verify §8.3 handoff "Suggested to Improve Agent Rules" — the ONLY handoff/suggestion in that chat.
- **PLAYBOOK** `.github/instructions/QA-Test-Agent.instructions.md`: new **§5.75 R94** — a QA tool's own SUCCESS signal is not proof the UI changed (tool defect, never a product finding); proof protocol = screenshot before/after + read the tool's own `RESULT`/`NOT_FOUND` line; cross-platform control; manual-interaction fallback THEN re-answer reachability; viewport-check tool-reported coords (pointer R22 §5.39); never downward-swipe to "return to top" on a refresh-control scrollable (pull-to-refresh resets the scroll mid-A/B); surface the tool FAIL in the roll-up + a scoped dev fix. New **§5.68 R77 #16** — a stopped `run-suite.sh` run takes Metro (8081) down with it: check `pgrep -l -f "expo start"` inside the R29 busy check and restart `npx expo start --port 8081` before connecting a device ("Failed to connect to /10.0.2.2:8081" = dead Metro, not an app/emulator fault). **§5.51b** instrumentation line's "returns fresh mid-viewport coordinates in one call" claim qualified (iOS ✔ / Android ✖ + the manual-swipe fallback). **§5.68 R77 evidence line** extended with #16 provenance.
- **AGENT FILE** `.github/agents/QA-Test-Agent.agent.md` §4: dated **2026-09-10** pointer bullet added at the top of the dated list.
- **Duplicate check (before adding):** no existing rule covers a tool reporting SUCCESS while the UI is unchanged — §5.24 triggered checkpoint (b) covers *empty/unexpected* output, **R81** (§5.71) the admin background-tab false negative, **R22** (§5.39) the pre-tap viewport check; R94 points at all three instead of restating them. Grepped `Kids P2P App Builder.agent.md` + all 4 app-code instruction files: **no overlap ⇒ no BP-N rule added** (the suggestion contained nothing app-code-specific; the `qa-scroll-to` code fix stays a dev task in the run report's "What Needs To Be Fixed Next" item 1).
- **Why:** 4 Android `qa-scroll-to` fires logged valid-looking coords (`RESULT message-button 16 1214` dp vs an 825 dp viewport) while the timeline never moved, and the same link scrolled iOS in one call — `e2e-test-results/qa-task-i02i05-android-item1-reverify-2026-09-10/report.md` §2c/§7.


## 2026-09-10 — QA Task (Android Item-1 re-verify + I02 persistence + 5c chip) — I02 & I05 → PASS both platforms; 5 new findings (1 MEDIUM tooling)
- Run: `e2e-test-results/qa-task-i02i05-android-item1-reverify-2026-09-10/` (report.md, ledger.md, screenshots/ 9). Android `Medium_Phone_API_36.1` + iOS `iPhone 17 Pro Max`. **Exact ledger: 179 tool executions / 147 msgs ≈ ~18 calls/verdict (10 verdict-items)** — vs 43c 9.6 / R1b ~23; dominated by a 9-call NEGATIVE answer for the scroll-to A/B + source root-causing + dual-platform legs.
- **Part 0: the FIX-Task-13 Android trade-fetch stall did NOT reproduce** (cold reload → timeline rendered instantly) ⇒ one-off harness artifact, NOT an app defect. Live metrics: viewport 825dp / content 1748dp / **maxScrollOffset ≈ 923dp** (the earlier "≈105pt tiny range" figure was wrong for this fixture).
- **Part 1: the "permanent occlusion" is DISPROVEN on Android.** 3 real finger swipes (800px, start over non-interactive content) put `message-button` y1156–1282, `safe-meetup-toggle` y1324–1474, `report-problem-button` y1505–1631, `request-cancel-button` y1663–1789 — all clear of the pinned footer (y1921–2106) and tab bar (y2190+); content→footer 132px, footer→tab 84px. `message-button` tap → Chat (safety modal → composer) ⇒ **I05 positive leg CLOSED on Android**; toggle expands + cta collapses.
- **NEW FINDING 1 (MEDIUM, tooling): `qa-scroll-to` does NOT scroll on Android.** 4 fires logged `RESULT message-button 16 1214` / `RESULT safe-meetup-toggle 16 1116|1278` (dp) — handler + refs fine, ScrollView never moves, coords outside the 825dp viewport (unusable to tap). The SAME link DOES scroll on iOS in one call. Prime suspect: one-frame rAF measurement after `animated:true`. **Standing Android practice: reach tail elements with manual swipes, not the tool.**
- **NEW FINDING 2 (LOW–MEDIUM):** `ChatScreen.isTradeActive` (L756) is false while `trade` is null ⇒ on the FIRST mount of a never-messaged in-progress thread the chat briefly shows **"This chat is no longer active. The trade has ended."** (L839-844) + placeholder "Chat is no longer active" (L975); the frozen branch is not gated on `loadingTrade` (the header IS). Re-entry doesn't reproduce. Fix: gate on `!loadingTrade`.
- **NEW FINDING 3 (LOW, dev-only):** `console.error('[chat.getTotalUnreadMessageCount] Error:', …)` (`src/services/chat.ts:560`) fires on a normal background path → dev LogBox over the Notification Center (FIX-2 class). **4 (LOW):** expanded safe-meetup CTA can land under the floating tab bar mid-scroll on BOTH platforms (Android y2145–2272 vs tabs 2190+; iOS y852–898 vs 868+) — reachable after further scrolling. **5 (LOW):** Notification Center error state renders raw **"Gateway Timeout"** (§6.3 class).
- **Part 2 (iOS I02):** expand → collapse → navigate away + return ⇒ **stays collapsed (persistence PASS as asked)**. **DOC-DRIFT:** guide expects expanded-by-default + a different trade expanded; shipped design (FIX-Task-7 5b) is collapsed-for-every-trade and only ever persists `true` (`SafeMeetupCard.tsx` L66/70-80) ⇒ persistence indistinguishable from the default and the per-trade-expansion assertion is unachievable.
- **Part 3 (5c):** emoji chip **visually confirmed** on real rows (iOS): 🧸 "LEGO Star Wars Set" · 🎮 "Nintendo Switch Games Bundle" · 🚲 "Kids Bicycle - 20 inch" · deterministic fallback for "QA Bundle Fixture 1 of 1" · **no chip** on `new_message`/`offer_accepted`/`sp_spent`. AX exposes only the `notification-item-glyph-<id>` container (no glyph value) → screenshots required; recommend `accessibilityLabel={itemGlyph}`.
- **Tooling facts:** (a) a stopped `run-suite.sh` takes Metro 8081 down with it → restart `npx expo start --port 8081` before connecting a device; (b) a swipe DOWN on the timeline triggers pull-to-refresh ("Loading trade…", scroll→top) — don't use it to return to top; (c) `qa:ax-tree` can't parse the mobile-mcp session-resource file (rendered text, not JSON) → `grep -n -E`; (d) R29 conflict resolved up front: a live `run-suite.sh --group D --platform both` held the iOS device + (pending) the emulator + the QA personas — owner stopped it, `pgrep` verified release.
- Tracker: **I02 🟡→✅ PASS, I05 → PASS (both platforms)**; §1 roll-up + section header + baseline note all updated to **237 PASS / 27 PARTIAL / 2 OPEN / 3 DOC-DRIFT / 2 SKIPPED / 17 Remaining** (R52/R56 verified). Residue: fixture trade `b2413c54` + item deleted (0/0/0 rows); **1 unread `trade_request` notification left for test-seller** (the reset script doesn't clean fixture notifications); both sessions logged out.


## 2026-09-09 — Agent-Improvement-3 codification: §5.73 R92 (external-state fresh-process gate) + R28 disarm sharpening (no code behavior change; playbook only)
- **PLAYBOOK:** `.github/instructions/QA-Test-Agent.instructions.md` — new §5.73 (before §6) = **R92 external-state fresh-process gate** (from the TRD Part 3 §8.3 handoff): after a fixture/tool mutates SHARED server state the app caches at process start (saved pm, persona, config toggles), assume the RUNNING process is stale → ONE terminate+relaunch before retrying; never logout/relogin or an unbounded retry loop (B06 stale-pm chase ~30 calls; direct-EF SUCCEEDED proving client-stale; logout/relogin didn't clear; fresh process did). Extends R83 (DB-vs-app tiebreaker) with the client recovery step + R78-9 (§5.2) from field to process-cached state.
- **R28 EXTENDED** (not a new rule — duplicate-rule discipline): §5.40 R28 disarm-verify sharpening — a `qa-dev-toggle` disarm deep link is NOT proof the armed state cleared; verify DISARMED by observable effect / DB-log read (R24/§5.37); toggle that won't clear in the running process → R92 fresh process (TRD Part 3 card_decline stale-arm).
- **Why:** TRD Part 3 (run `e2e-test-results/qa-task-trd-part3-2026-09-09/`, HEAD 6ecc6c00 FIX-Task-8) — Part 0 FIX-Task-8 6/6 PASS both platforms, B/C/D genuine-Android verdicts (no flips), B12/B13 NEVER-RUN→PASS, TRD remaining 19→17. Findings feeding these rules: stale-pm cache after external card swap; card_decline disarm unreliable.

## 2026-09-09 — Agent-Improvement-2 codification: §5.72 R82-R91 + B1-B8 table + FIX-3 LANDED as ios:sim-build (no code behavior change; playbook + tooling)
- **PLAYBOOK:** `.github/instructions/QA-Test-Agent.instructions.md` new §5.72 (before §6). R82-R91 = the 10 TRD-R1b efficiency rules (R82 one-flow-many-verdicts; R83 DB-vs-app→direct EF/fixture tiebreaker; R84 full build-log capture; R85 source-verify deep-link param formats; R86 fixture drivability-by-action; R87 dev-tooling crash early-pivot (libart SIGSEGV); R88 persona counter/flag DB recon; R89 persona-state restore DB-verified; R90 EF/fixture-over-UI setup; R91 live per-episode call-cost tracking). + B1-B8 bottleneck quick-reference table + iOS-rebuild standing procedure + tooling backlog (FIX-8/FIX-9 BACKLOG-not-built; FIX-5 DEPRIORITIZED; FIX-1 landed as FIX-Task-8 HEAD 6ecc6c00; FIX-2 still open in referralNotifications.ts).
- **FIX-5 CORRECTION (flagged to owner):** the brief's "Group J is post-MVP" premise is NOT supported by the tracker — TRD Group J (seller-cancel) was live-executed 2026-09-08 (J01-J03 DOC-DRIFT per TFV2-023 alert removal, J04/J05 PASS); the R1-android report lists it separately from the "A03/A04/D05 post-MVP exclusions". Playbook note corrected to the verified facts (FIX-5 low-value-not-ruled-out, NOT post-MVP).
- **FIX-3 LANDED:** `p2p-kids-marketplace/scripts/build-ios-simulator.sh` now sets BOTH `SENTRY_COPY_OPTIONS_FILE=false` + `SENTRY_DISABLE_AUTO_UPLOAD=true` (defaults+export+comment) in both --fast and clean paths; package.json adds `"ios:sim-build"` alias. **TESTED once: `yarn ios:sim-build` → ** BUILD SUCCEEDED ** → installed + launched on booted iPhone 17 Pro Max, exit 0, fresh .app 06:20** (replaces the ad-hoc multi-var command rediscovered every iOS rebuild round).

## 2026-09-08 — QA Task TRD-R1b (FIX-Task-7 close-out + Group J first Android exec) — iOS env fix FOUND; 16 PASS / 1 PARTIAL / 0 FAIL
- Run: `e2e-test-results/qa-task-trd-r1b-2026-09-08/` (report.md, ledger.md, screenshots/). HEAD `62238ea4` (FIX-Task-7 committed). iOS iPhone 17 Pro Max + Android Medium_Phone_API_36.1 (became unstable ~19:45 — SIGSEGV in libart on Hermes/JVMTI agent attach + item-detail fetch stalls). **~400+ calls / ~17 verdict-items ≈ ~23/verdict** (R71 manual tally).
- **iOS NATIVE-BUILD FIX (unblocks the FIX-Task-7 iOS BLOCKED-ON-BUILD):** `npx expo run:ios` still fails (signing check). Working command: `SENTRY_COPY_OPTIONS_FILE=false SENTRY_DISABLE_AUTO_UPLOAD=true xcodebuild -workspace ios/PassItUp.xcworkspace -scheme PassItUp -configuration Debug -sdk iphonesimulator -destination 'platform=iOS Simulator,id=<UDID>' CODE_SIGNING_ALLOWED=NO build` → BUILD SUCCEEDED (SENTRY_DISABLE_AUTO_UPLOAD=true ALSO required — sentry-cli sourcemap org error). Fresh .app boots (no RNCNetInfo null).
- **Part 1a iOS ALL PASS:** E02/D02 (real-UI dispute on trade 1ba3745c stamps disputed_at → fast-clock rpc_process_auto_complete=0, stays in_progress, item not sold, 0 payout), E03 (buyer dispute banner + pinned I Got It DISABLED 50%-opacity), E04 (seller ROLE-AWARE banner at HEAD — Android reverify LOW finding RESOLVED in committed FIX-Task-7 copy reconciliation, L1262-1264), Item-3 ("15 SP releasing in 3 days after completion" on live SP offer — getSPReleaseDays pending_sp_release_days=3), UX5a/5b (pinned CTA + collapsed SafeMeetup). Admin resolve-refund cleanup (DOM click, sidebar intercept) — cancelled/resolved_buyer, PI cancelled, item relisted, SP restored 482/0.
- **Part 1b UX 5c PASS (iOS):** 3 pending offers (ef-repro) → 4th UI offer → cap modal → Cancel My Oldest Offer cancelled CORRECT oldest (RC Car bb544703, earliest offer_expires_at, reason "Buyer cancelled oldest offer to free a slot") + freed slot (3→2; fresh EF offer 7662e9a2 succeeded). DEV OBSERVATION: one-tap auto-resubmit transient "3 pending" rejection (stale client check — modal clears offerLimitPendingOffers via setState then awaits handleSendOffer) — recommend dev refetch-before-resend.
- **Part 2 Group J first Android exec — J01-J05 PASS/DOC-DRIFT** (persona test-seller-2, count 0/flag NULL; **test-seller untouched at count 6 + flagged 2026-08-30**). Backend consequence verified: cancel → count 0→1→2→3 + admin_review_flagged_at SET at 3. UI = generic "Trade Cancelled" notif — Level 1/2/3 seller alerts confirmed DEPRECATED (TFV2-023 performSingleCancel comment) → DOC-DRIFT cross-platform. J04 cancel button on in_progress only (absent on completed seller trade); J05 seller-only reasons. test-seller-2 RESTORED count 0/flag NULL after.
- **1c Android Item-3 leg PARTIAL** (Android instability blocked the on-device SP-offer screenshot; iOS-leg + source + config strong).
- Tracker: E02/D02 OPEN→PASS (roll-up PASS 232→234, OPEN 4→2); E03/E04/J01-J05 notes appended; R1b note added. Part 3 breadth deferred explicitly (Android crash + fixture needs + budget). No config writes.
- **APPLY-HANDOFF (2026-09-09):** §5.70 R80 extended in the QA playbook — R80's platform-specific no-verdict disclosure now applies to DEFERRAL/known-gap rows too (when a platform's tooling crashes mid-run, the deferral row must name the platform that already holds a verdict + the single missing leg, so a later session on the recovered platform reuses the completed result). QA agent file §4 dated pointer added (2026-09-09).

## 2026-09-08 — QA Task TRD-R1-FIX7 (FIX-Task-7 P1 dispute/auto-complete re-verify) — Android 8 PASS / iOS BLOCKED-ON-BUILD
- Run: `e2e-test-results/qa-task-trd-r1-fix7-reverify-android-2026-09-08/` (report.md, ledger.md, screenshots/ 8) + `...-ios-2026-09-08/report.md`. HEAD fac3d70b + UNCOMMITTED FIX-Task-7 working tree (Metro served fresh bundle — pinned-CTA discriminating check). Admin :3001 (samer).
- **P1 CONFIRMED FIXED (Android, both fixture-EF and REAL-UI paths):** open-dispute now stamps disputed_at (real UI Report-a-Problem → no_show on real accepted trade `9299d761` → disputed_at SET 23:07:37). rpc_process_auto_complete(100) after fast-clock returned 0 on BOTH a fixture trade (`8b467792`) and the real trade — stays in_progress, item not sold, **0 seller_payouts** (prior FAIL queued $14.40). E02/D02/F02 PASS. E03 buyer banner PASS (pinned I Got It CTA **disabled** during dispute — FIX 5a nuance vs guide "hidden"). E04 seller banner PASS + **LOW copy finding**: dispute banner is role-agnostic (TradeTimelineScreen L1259-1260) — seller reads buyer copy "Your issue has been reported…", guide expects "A buyer has reported an issue with this trade."
- **E05/E06 admin resolve PASS (live portal, DOM-click for sidebar intercept):** resolve-complete on `9299d761` → completed + seller_payouts `b9e3eae4` $12 requires_action + admin_audit_logs actor `1a546991` (dispute_resolved); resolve-refund on `0e33f356` (Skateboard $22) → cancelled/resolved_buyer + stripe_refund succeeded (authorization-cancel, uncaptured-PI path — no trade_refunds row) + item relisted + attribution. Buyer mobile reflection clean both (completed; cancelled + "no payment taken" + timeline-dispute-closed-summary).
- **Item-3:** DB both `pending_sp_release_days`=3 AND `sp_pending_days`=3 (reconciled 2→3); client getSPReleaseDays reads `[pending_sp_release_days, sp_pending_days]` first. UX 5a/5b PASS on fresh Android bundle (pinned CTA; collapsed SafeMeetupCard `safe-meetup-toggle` "Tips ›"); **5c source-verified only** (TradeOfferScreen L512-551 handleCancelOldestOffer + `offer-limit-cancel-oldest-button`).
- **iOS BLOCKED-ON-BUILD:** prebuilt .app stale (RNCNetInfo null → "App entry not found"); fresh rebuild fails at "Bundle React Native code and images" (Sentry options file → worked around; then export:embed errors) — needs dev-agent clean rebuild. Per-platform disclosure (R80) — no iOS closure claimed.
- **Residue left:** real trade `9299d761` completed + $12 requires_action payout (f5bac12c sold); `0e33f356` cancelled (Skateboard relisted). Fixture trade cleaned. No config writes.

## 2026-09-08 — QA Task Cleanup-1 apply-handoff: admin click-actionability root-caused + §5.71 R81 codified (no app defect)
- **INVESTIGATION RESULT — the admin click-actionability "issue" was NOT an admin-app defect.** A/B-verified: `click_element`/`hover_element` time out on EVERY element ("waiting for element to be visible, enabled and stable") ONLY on browser pages the harness marks "(not visible)" = background tabs. Background tabs throttle `requestAnimationFrame` → Playwright's element-stability precheck (2 consecutive rAF frames, no bbox change) never completes → every click/hover times out regardless of route/element/auth, and a reload does NOT restore it. Proof: same tool + same `/categories` route — Books-Edit click failed on the backgrounded shared page `4d82b6cd` but opened the edit modal on the fresh "(active)" page `8636b8df`; login-page + sidebar-toggle + example.com clicks all worked on active pages. The original shared page `4d82b6cd` stayed "(not visible)" even after reload.
- **FIX = agent rule, not code:** operate admin clicks on an "(active)" page instance (open_browser_page to the same origin; Supabase session shared per-origin via localStorage → fresh page is already authenticated). Don't retry a backgrounded tab. Last-resort fallback for config writes = the portal's OWN API route (`PATCH /api/admin/categories/[id]` etc., `x-admin-secret` from `p2p-kids-admin/.env.local`, never echoed) — same server-side validation + service-role write as the UI Save. (This supersedes Cleanup-1's PATCH workaround being described as "click-tool regression" — the real cause was the backgrounded page.)
- **CODIFIED:** playbook §5.71 **R81** (admin browser clicks require an ACTIVE page instance; + fresh-value config writes must respect the enforced range — Cleanup-1's 1.45 exceeded `SP_EARNING_MAX` 1.40; + `CategorySelectModal` IS AX-drivable on Android) + agent-file §4 dated pointer. Left an active logged-in admin page (`8636b8df`, at /categories) available for future QA admin clicks.

## 2026-09-08 — QA Task Cleanup-1 (E04 Android re-run + J14/J15 re-drive) — 3 PASS / 0 FAIL; closes FIX-Task-6 + 43a-exec's last config-pending
- Run: `e2e-test-results/qa-task-cleanup1-e04-j14j15-android-2026-09-08/` (report.md, ledger.md, screenshots/ 6). HEAD `49c63c0a` (includes FIX-Task-6). Android Medium_Phone_API_36.1. **Manual call tally ~95-105 / 3 verdicts ≈ ~32-35/verdict** (R71 fallback; NOT small — drivers: admin click-tool regression ~9-12 wasted calls + ItemCreate long-scroll driving + signup ScrollView friction + E04's 3 mandatory real 60s cooldowns ~10 poll calls; verdict content itself genuinely small).
- **E04 Android PASS (FIX-Task-6 last piece CLOSED):** fresh signup (throwaway qa.alice.17888977501027905, phone +12025550102311), 3 real 60s-cooldown sends (#1 auto mount, #2/#3 resend) → 3 phone_verification_codes rows (20:02:49/20:04:14/20:05:24Z) → 4th send rate-limited (NO 4th row) → **friendly inline `otp-rate-limit-message` "Too many attempts. Please try again in 3600 seconds." + disabled Resend `otp-resend-countdown` "Resend in 59m" (range-based band)** — the 43m raw-"Rate limit exceeded"-Alert copy finding is RESOLVED on Android. formatOtpCountdown bands confirmed (<120s "Ns", ≥120s "Nm" e.g. 59m, ≥3600 "Nh").
- **J14/J15 PASS (43a-exec PARTIAL RESOLVED):** Books multiplier 1.30→**1.35** (NOT 1.45 — exceeds SP_EARNING_MAX 1.40) → ItemCreate (test-seller) $20 → preview "~27 SP" (20×1.35, not cached 26) + "1.35x multiplier" + buyer-cap "~14 SP" (Books 70%) = LIVE propagation. **CategorySelectModal IS AX-drivable on Android** (fullScreen RN Modal NOT AX on iOS — record platform diff): picker shows ⭐ bonus badges on Books/Toys/Electronics/Art&Crafts, none on Games/Sports/Clothing/Other/Shoes/Bookies. Reverted 1.30 DB-verified; 0 residue (no items/drafts from the dev-fixture drive).
- **ADMIN TOOLING NOTE (session regression):** the shared-admin browser `click_element`/`hover` never passed actionability (never-stable on EVERY element incl. top-bar, 7+ attempts + clean reload) → /categories UI undrivable by clicks this session. Worked around via the portal's **own PATCH `/api/admin/categories/[id]`** (x-admin-secret from p2p-kids-admin/.env.local; same SP_EARNING 1.05–1.40 server validation + service-role write as the UI Save). Not an app defect; flag if the click regression persists (run_playwright_code DOM el.click() was the prior-session path).
- Session end: app logged OUT (Landing); no config left changed (Books 1.30); admin portal left at /categories; Metro 8081 running. Tracker AUTH E04/J14/J15 rows appended (Cleanup-1 notes).

## 2026-09-08 — QA Task 43a-exec (Config-Propagation Execution, Android cross-guide) — 4 genuine verdicts: O05/N01/F04 PASS + J14/J15 PARTIAL(env); ~15 families not-driven (env/fixture)
- Run: `e2e-test-results/qa-task43a-exec-config-prop-2026-09-08/` (report.md, ledger.md, screenshots/ 4). HEAD `fc6867a3` (43M commit; no app-src drift vs 43m `dc16f31d`). Android Medium_Phone_API_36.1 + real admin :3001 (samer creds). **Manual call tally ~230-260 executions / ~5 verdict-items ≈ high/verdict** (R71 fallback — transcript not mineable; dominated by repeated emulator session-drop/process-exit instability ~6+ incidents + admin browser actionability races).
- **O05 PASS:** radius default/min/max 10/5/25→**15/8/40** via `qa:admin-config-set` → Discover Filters modal showed 15-mi default + "8 mi"/"40 mi" slider bounds (Discover reads radius via a DIRECT fresh `admin_config` query at `loadInitialData` — no getAdminConfig 5-min cache) → live-on-remount. Reverted 10/5/25 (post-revert re-check showed 5/25 + "default is 10 mi").
- **N01 PASS:** Games `is_active` deactivated via admin `/categories` modal (`input-active` checkbox) → Discover filter category row dropped Games (Books·Toys·Sports·Electronics·Clothing). Reactivated + DB-verified. Categories fetched `.eq('is_active',true)` per Discover mount.
- **J14/J15 PARTIAL (env):** Books `sp_earning_multiplier` 1.30→**1.40** via admin `/categories` SP Config (`tab-sp`/`input-sp-earn`; max 1.40 per categoryService SP_EARNING_MAX) DB-verified; on-device ItemCreate preview capture BLOCKED — **emulator session-drop to Landing + process-exit to launcher 5+ times** during the long-form drive (list_crashes EMPTY — dev-client churn, not native crash); dev fixtures all applied correctly on-device (Books selected via dev-set-category, $20 via dev-fill-item) but the form-bottom SPEarningsPreview (expected ~28 SP = 20×1.40 vs 26 at 1.30) unreachable. Reverted 1.30 DB-verified. Re-drive owed in a stable session.
- **MSG-F04 PASS:** sp_config `referral_reward_referrer_sp` 25→**30** + `referral_reward_referee_sp` 10→**15** via admin `/referrals` (`ref-config-first-trade-*`) → Referrals screen on TRUE remount (pop + re-fire deep link) showed "+15 SP when they complete their first trade" + "30 SP per trade". **KEY: re-firing the referrals deep link over an already-mounted screen did NOT remount (React Navigation re-focus = stale); pop+re-push required.** Reverted 25/10 DB-verified (referee needed a 2nd admin save — verify DB after each admin save, R28). F07 paused-branch source-corroborated (same fresh per-mount RPC `get_referral_listing_config`; ReferralsScreen `isProgramPaused=!program_enabled`; DT97-prior-verified).
- **Config-cache model (verified from `src/services/adminConfig.ts`):** `getAdminConfig` = module-level 5-min TTL cache cleared on app restart. Screens using direct queries (Discover radius) / per-mount RPC (referral rewards) / categories fetch read FRESH per mount → propagation on this app is documented "live-on-remount" (R59 category b), NOT a defect. No HIGH systemic client-config-fetch defect found in exercised pairs.
- **EMULATOR INSTABILITY (HIGH env finding, root cause of most not-driven legs):** app drops session to Landing + exits to launcher ~6+ times (esp. during long-form/multi-step drives + repeated deep-link delivery); nav-state restores stale screens (ItemCreate) causing mis-taps; cold deep links crash to launcher. Recover: launch → Dev Launcher 8081 row → BACK-dismiss dev menu.
- **Not-driven (honest PARTIAL/known-gap, per-family reasons in report.md):** SUB H02/D01/A01-A03/R05/I04/I09/H06/H07 (A-group: in-app Plans/purchase RETIRED web-first per guide — A01-A03 reference dead SubscriptionPlansScreen aliased to JoinKidsClub; trial_enabled=false intentional D-001; H/D/I families need payout-method/grace/pending-SP fixtures) · MSG G06-G09 (flagged-listing appeal/moderation/recall surfaces, fixture-heavy) · TRD B05f-j/B02/C08/T04/K01/K02/K04/K05/D06/P-group/N01-cart (offer/trade/cart money surfaces, fixture-heavy + emulator instability). **TRD-Z01-Z08 EXISTS in guide index** (L347-354; bodies L6696-6825 "did not test") — cancel-request escalation config checks NOT attempted (multi-party "did not test" flows need a dedicated fixture session).
- Tracker: AUTH J14/J15/N01/O05 rows got 43a-exec notes (config-pending designation RESOLVED; no count flips); MSG F04/F07 notes say verdicts carry forward into MSG's full-round dispatch. Session end: mobile left logged in as test-buyer; admin left logged in at /referrals; NO config left changed (all reverted + DB-verified).

## 2026-09-08 — QA Task 43m (Group P + E04/F02-F04/H06 on Android) — 20 fresh PASS + 3 CREDITED; AUTH Android coverage DONE except S08/S11-Case2
- Run: `e2e-test-results/qa-task43m-auth-group-p-android-2026-09-08/` (report.md, ledger.md, screenshots/ 17). HEAD `dc16f31d`. Android Medium_Phone_API_36.1. **Manual call tally ~300-320 / ~23 verdicts ≈ 13-14/verdict** (R71 fallback; above 43c 9.6 — structural: 5 fresh UI signups, 2 §5.2 layout-shift relaunch cycles on the Create-Account form, E04 60s-cooldown waits; the pure test-buyer Group-P batch ran ~6-8/verdict).
- **Step 0 credit-check (FIX-Task-1 `17e8f4ef`, 2026-09-07): P04/P10/P16 CREDITED not re-driven.** Evidence folder `fix-task-1-sell-fab-color/` has android+ios home-fab-green + sell-sheet-open; ONLY PersistentTabBar/index.tsx changed; `git diff 17e8f4ef..HEAD` on PersistentTabBar/ComposerBar = EMPTY (no drift). Fresh bundle green-FAB confirmed R79-1.
- **Group P 16 fresh PASS (test-buyer 49243010):** P01 node chip ViewGroup non-button · P02 bell+chat+avatar right cluster no logout · P03 badge 1 → Messages → read (msg c4223318 sent by test-seller on pending d8e14d86) · P05 no Inbox tab · P06 Active 3 pending (Your Offers 3 = DB) · P07 History newest-first · P08 badge 3 = active-only (getActiveTradeCount excludes completed/cancelled) · P09 basket badge 1 (added Building Blocks Bucket 8193b91b) + Home active · P11 composer focus · P12 "+" → ItemCreate title prefilled · P13 empty "+" → empty title · P14 camera → ItemCreate camera auto-launch (native camera2 AX-drivable: permission→shutter→Done) + title prefilled · P15 AI never overwrites prefilled title (source guard titlePrefilledFromComposerRef L883/921 + on-device title preserved; AI "Photo analysis issue" env) · P17 Profile logout + Settings Sign Out both → Landing · P18 composer analytics DB (tapped x2, submit true x2/false x1) · P19 AX (tab-trades Button, header-chat-btn Messages, node chip not button).
- **H06 full PASS:** Skip leg on final carousel slide → Home (onboarding_skipped_at 16:40:58Z set; tab bar mounts immediately — Phase-24 bug absent). Get-Started leg was 43b.
- **F02/F03/F04 PASS (fresh Alice-autofill signups, ZIP 07999):** "We're Coming Soon!" modal (Little Falls Central fallback); F03 Join Waitlist → Confirmed → zip_waitlist 07999 pending + fallback node; F04 Continue Trading → fallback node no waitlist row.
- **E04 PASS (functional) + COPY FINDING (R58):** 4th OTP send blocked server-side (3 code rows only, no 4th); standalone signup `PhoneVerificationScreen` shows raw `Alert('Error','Rate limit exceeded')` + Resend stays enabled (no retry countdown) — source: handleResendCode catch L74-78 no OTPRateLimitError branch; guide/phone-gate expect friendly "Too many attempts… Try again in N seconds" + disabled resend. Dev fix recommended (mirror EditProfileScreen/usePhoneVerification friendly retryAfterSeconds copy).
- **Android signup runbook (R77-series addendum, session-memory'd):** Create-Account form shifts ~82px when a lower field focuses (R78-7: BACK + re-list + verify focus before every tap); DOB auto-advances Day→Month→Year once the Day box is genuinely focused; phone must be E.164 (+1), else "Invalid phone number format"; dev-fill Alice autofill (unique email/phone) collapses the whole signup form to one tap.
- **Residue:** test-buyer 2 drafts (P15 mock-upload + P14 camera) + cart 1 item + P03 msg read; 6 throwaways (incl. orphan auth e15e2c91 from a pre-fix submit whose phone-send failed); F03 pending zip_waitlist 07999. App left logged out (Landing). No config writes. Tracker AUTH 43m note added (P10/P16 explicitly credited).
- Run: `e2e-test-results/qa-task43l-group-l-auth-reconcile-2026-09-08/` (report.md, ledger.md, screenshots/ 12). HEAD `08cd458c` (FIX-Task-5 COMMITTED, clean). Android Medium_Phone_API_36.1 + real admin :3001. **Manual call tally ~155-165 executions / 7 verdict-class items ≈ ~22/verdict** (above 43c 9.6: Part-2 reconciliation desk-work + admin cold-start + Gboard-tutorial friction ~12 calls; pure device leg Group-L+J10 ≈ ~40/5 ≈ 8/verdict).
- **Group L (L01–L04) PASS — first Android execution** (fixture `73670a8f`, deferred since 43b–43g). L01: buyer search 0 results while pending + seller My Listings PENDING/Awaiting-approval. L02: real admin approve (native "Listing approved! Starter Pack awarded" alert) → DB available/approved_at → buyer "1 result" + seller Active 26. L03: `listing_approved` notif `1ecb29ce` (deep_link /listing/73670a8f) → bell → tap → Item Detail; read_at set; +10 SP starter pack (SP 2199→2209). L04: edit price 20→25 → Save → pending $25 approved_at NULL → buyer search 0 again.
- **J10 typed-OTP completion leg PASS at committed HEAD** — 43h used dev-autofill (F1 dup items); this round typed 123456 → single auto-verify → ONE item `356c46b5` → **F1 confirmed dev-button-only** (real users type = single fire). Gate prefilled real `+15551234004` (not placeholder); Send Code fires; test-free restored to baseline after.
- **S08/S11-Case2 deferred with CORRECTED reason (reconciliation finding D2):** the reset-link minting harness EXISTS + live (source-verified admin-trigger-password-reset EF; iOS-PASSed Phase 16 + 2026-08-23). 43c–43g "harness absent" reason STALE. Android closure needs harness mint + Android fragment-deep-link delivery proof + shared-persona reset-back = dedicated session.
- **RECONCILIATION (primary deliverable): 99/138 AUTH rows have a genuine Android verdict** (43b–43g + 43h-Android + 43L). 9 excluded (5 REMOVED H04/H05/I01–I03 + 4 config-pending J14/J15/N01/O05). **30 no-Android-verdict**: E04, E05-as-row (gate covered via J10), F02–F04, G01–G06 (admin-surface), P01/P02/P03/P05–P19 (18), S08. All have iOS/platform-agnostic PASS. **Discrepancies D1–D7** (D1: 43g "near-closure" vs 30-row no-Android set; D2 stale harness reason; D3 tracker §1 127/2 vs header 128/1 — NOW FIXED to 128/1; D4 43c 27/10 vs 29/9; D5 43b B01 ledger-PARTIAL vs report; D6 C03 tracker OPEN vs 43e PASS; D7 J10 trajectory).
- **NEW ANDROID TOOLING FIX:** Gboard "Try out your stylus" tutorial (recurs every field focus + swallows adb input text/mobile-mcp type_keys) eliminated via `adb shell settings put secure stylus_handwriting_enabled 0` — reversible; add as standing emulator fix. uiautomator no-XML transient after OTP transition (R77 #3) recovered on 3rd list.
- Session end: mobile logged OUT (Landing); admin :3001 left running. Residue: test-seller `73670a8f` pending $25 (L04 end-state, needs re-approval to relist); test-free `356c46b5` new pending + 43h's 4 F1-dups; no config writes; no code modified.

## 2026-09-08 — QA Task 43h (FIX-Task-5 on-device verification, dual-platform) — 3 PASS / 0 FAIL / 0 BLOCKED
- Run: `e2e-test-results/qa-task43h-fix-task5-verify-2026-09-08/` (report.md, ledger.md, screenshots/ 7). HEAD aa6fc601 + UNCOMMITTED FIX-Task-5 (4 M files: PhoneVerificationModal.tsx prefill + placeholder, phoneService.ts getCurrentUserPhone, 2 test files). Metro 8081 served the uncommitted JS on cold reload (JS-only). iOS iPhone 17 Pro Max + Android Medium_Phone_API_36.1.
- **All 3 legs PASS.** Leg 1 iOS + Leg 2 Android (AUTH-TC-J10 flow, persona test-free `5551234004` phone_verified_at NULL): Submit → gate modal fires → phone input **PREFILLED `+15551234004`** (AX-tree value + screenshot, NOT empty, NOT the old `+1 (555) 123-4567` placeholder) → Send Code **ENABLED + fires** (phone_verification_codes row ea535907 created — the 43g "never fired" gap CLOSED) → OTP step + **Dev autofill reachable** → verify → publish resumes (pending items). Placeholder now "Enter your phone number". DB: phone_verified_at SET each leg; **test-free RESTORED to baseline (phone_verified=true, phone_verified_at NULL, method NULL) after EACH leg** (brief-directed scoped UPDATE; final read-back verified). Leg 3 Group E spot-check (iOS): fresh signup → **standalone PhoneVerificationScreen** (separate component) code-sent + verified + DB-persisted (throwaway ca25a137) → **signup path confirmed UNAFFECTED** (source-isolated change).
- **Findings (2 LOW, both pre-existing/not-FIX-Task-5):** (F1) the `__DEV__`-only "Dev: Autofill & Verify (123456)" button double-invokes verify → onSuccess ×2 → handlePublish ×2 → **2 identical pending items per platform** (iOS d53c99a9+eb818d86, Android 672de115+6ccd02ce) — real users can't reach (typed code = single auto-verify fire); surfaced because the fix made the OTP step reachable. (F2) dev-bypass verifyPhoneCode `admin_audit_logs` insert uses stale columns (user_id/action/details vs actor_id/action_type/entity_type/entity_id/payload) → silently no-ops (0 rows, DB-verified).
- **Residue:** 4 pending "QA Dev Fixture Item" $20 under test-free (F1 dups) + throwaway ca25a137 mid-onboarding (disposable). Tracker: AUTH-TC-J10 row Date/Source/Notes refreshed (already PASS; no flip). Session end: both devices Landing/logged out; qa-logout on iOS showed transient "Logout failed" toast but session cleared server-side (relaunch → Landing) — tooling note only. Android Gboard "Try out your stylus" tutorial recurred on modal focus (43g-known, BACK-dismiss).

## 2026-09-08 — QA Task 43g (Group J completion + Group K, Android-only) — 10 PASS / 1 PARTIAL; Group L deferred
- Run: `e2e-test-results/qa-task43g-jk-l-2026-09-08/` (report.md, ledger.md, screenshots/ 28). HEAD 804b606c (43f commit; no app-src change vs 43f). Android Medium_Phone_API_36.1. **Call ledger: manual tally ~195-205 tool executions / 11 verdicts ≈ ~18/verdict** (transcript not cleanly mineable — pointer log only; R71 fallback). Above 43c 9.6: deep J-form + bulk-K driving + recurring Gboard handwriting-tutorial interference.
- **Part 1 Group J completion (Android) — J02/J07/J11/J12/J13 PASS + J10 PARTIAL.** J02: AI analyzing overlay + 7s Continue-Without-AI + error card "Photo analysis issue" + Try Again re-trigger (Apply All/per-field Use source-corroborated — no dev mock on any platform). J11: draft `0b0063e2` DB-autosaved + **Dashboard resume banner → Continue → title+photos restored** — RESOLVES 43f's flag (banner lives on Dashboard/My Listings, NOT ItemCreate; `create-item` starts fresh); ResumeDraftBanner junk-text bug confirmed FIXED (clean copy). J13 EXCEEDS iOS basis: **replace COMPLETED via the AX-drivable Android system Photo Picker + real storage upload** (draft photo_urls [real,dev]); remove 2→1; reorder moved cover; resume restores. J12: doc/source (10MB/MIME/400px) + valid real image picked+uploaded (no rejection). J07: test-free Payment Preference → "🌟 Subscribe…" + Upgrade Now (`sp-upgrade-button`), no sp-toggle. **J10 PARTIAL-Android**: phone gate VERIFIED (unverified test-free → Submit → "Verify Your Phone" modal blocks, no item created DB); completion leg gated — Send Code not firing (3 taps on enabled green pill, no `phone_verification_codes` row) + **modal prefilled +1 (555) 123-4567 vs test-free's actual profiles.phone 5551234004** (number mismatch likely defeats dev-bypass). Used test-free (DB phone_verified_at NULL) per R10 instead of a fresh signup — flagged deviation.
- **Part 2 Group K (Android) K01–K06 PASS — K05 tracker PARTIAL→PASS** (confirm sheet "Submit 4 Items"/4 summary rows opened+driven on-device + Confirm → "Missing bulk session or draft session." fixture-caveat alert — exceeds iOS where the sheet couldn't open). K01 dev-add-test-photos → 5 auto-grouped items (dup-hash fixture-limited). K02 long-press→select→Merge 2 photos into 1 item (Split + reorder controls appear); reorder-visual unobservable (identical fixture photos). K03 step indicator Photos→Group→Review "current" (Publish step 4 not fixture-reachable). K04 Apply-to-All bar + "Apply Condition new" chip (fills-blanks source). K06 subscriber bulk-sp-summary card renders (numeric total needs per-card Accept-SP — fixture-gated).
- **Part 3 Group L (L01-L04) DEFERRED** per Session Budget Discipline (own mixed-surface admin+mobile unit; needs dedicated budget). Fixture item `73670a8f` (pending, $20, accepts-SP) INTACT for the L run. If L + the J10 Android leg are run, AUTH Android coverage ≈ full closure (only S08/S11-Case2 + config-pending left).
- **NEW ANDROID TOOLING FACTS:** (1) **Gboard handwriting-IME tutorial ("Try out your stylus") recurs on EVERY field focus on this emulator** (only LatinIME+voice IMEs) — BACK-dismisses but intercepts taps/text until gone (extends R77 #2; different flavor than 43c's audio-permission prompt); (2) **Android system Photo Picker IS AX-drivable this build** (select photo + Done) — J13 replace completed end-to-end (updates §5.31 per-build note); (3) ItemCreate ScrollView on Android needs **anchored `adb shell input swipe`** (mobile-mcp center swipes often no-op — erratic/teleport scroll class); (4) J10 phone-gate modal prefills `+1 (555) 123-4567` regardless of the persona's actual number — dev look.
- Session end: all logged OUT (Landing). test-seller 2 active drafts (0b0063e2 mine + dd3dbdde 43f-leftover) = cleanup candidates. test-free untouched (no items/drafts, still phone-unverified). One real photo uploaded to storage inside draft 0b0063e2 (J13 replace leg). No config writes/toggles. Tracker: AUTH PASS 127→128, PARTIAL 2→1 (K05 flip), header+note reconciled.

## 2026-09-08 — QA Task 43f (AUTH Backlog Push, Android-only) — 15 PASS / 1 PARTIAL; O04-free + B01-incomplete legs CLOSED + Group Q full + Group J partial
- Run: `e2e-test-results/qa-task43f-auth-backlog-2026-09-08/` (report.md, ledger.md, screenshots/ 23). HEAD 3a1b297b (43e commit). Android Medium_Phone_API_36.1. **Call ledger: mineable this round — 356 tool executions / 323 msgs** (transcript 3604f3f1; R71 exact; ~22.3 calls/verdict vs 43c 9.6 — HIGH: recon ~35 + settings-detour ~15 + Metro 8082 stall/cold-reloads ~25 + schema-surprise re-queries + deep J-form driving).
- **O04 free-user leg CLOSED (PASS)**: test-free Discover → "Accepts SP" toggle ON → `discover-sp-upgrade-cta` "🌟 Subscribe to Kids Club+..." + `discover-sp-upgrade-button` (Upgrade Now→JoinKidsClub) + 50 SP results w/ gold `-sp-badge`s; OFF reverts (81, no CTA). Source DiscoverScreen Fix-4 `!canSpendSP && filters.spEligibleOnly`. (43c PARTIAL → full Android PASS.)
- **B01 incomplete-onboarding leg CLOSED (PASS)**: reused 43e throwaway `qa.alice.17888198382012019@` (was already mid-onboarding: profile complete, onboarding_completed_at+skipped_at NULL — R10 reuse) → login → **onboarding carousel step 1 of 5**, NOT Home. shouldShowOnboarding = both NULL → true.
- **Group Q full Android coverage (Q01–Q07 PASS)**: reachability = SpWallet (`sp-wallet`) → "How Trading Works" → Help education (no `Help:` deep link registered — R42). Q03 Books/$25→33SP=round(25×1.30) no hardcoded rates; Q04 Books/$25 free → Max17=floor 70% cap, cash $8, fee **$20.00**=cfg 2000, total $28 (fee from admin_config subscriber-aware); Q05 bonus ⭐ Books/Toys/Electronics/Art&Crafts (DB set); Q06 live education_analytics rows this session (help_view/calculator_use/section_expand) no throw; Q07 state machine source+DB. **Q02 tracker PARTIAL→PASS** (stale — calibration report already PASS; tracker header+§1 reconciled PASS 126→127, PARTIAL 3→2).
- **Group J partial (6 PASS + J05-PARTIAL, test-seller trial+grace = canSpendSP true)**: J01 photo-first gating · J03 publish disabled-until-valid (canPublish gate functional no-op) · J04 condition/age/gender/12-color options · J06 Accept-SP toggle → "✓ SP Eligible" · J08 SP preview "~26 SP"=20×1.30 subscriber branch · J09 submit → "Thanks for submitting!" + **DB item 73670a8f pending** (= staged Group-L fixture) + My Items PENDING "Awaiting approval" hint. **J05 PARTIAL**: Other + Custom Category Name * + admin-review helper render confirmed; blocked-submit **source-verified only** — Android adb field-clear (CTRL+A keycombination 113 29 + DEL keyevent 67) did NOT clear the RN TextInput (bounded attempts, R78 pivot).
- **Deferred (explicit):** Group J J02 (AI success path needs real AI), J07 (needs test-free), J10 (needs unverified persona), J11 (draft resume banner not surfaced on fresh deep-link mount), J12/J13 (need fixture images); Group K K01–K06; Group L L01–L04 (item 73670a8f staged). Standing exclusions unchanged.
- Session end: all personas logged OUT; no drafts created; no config writes/toggles; 23 screenshots.
- **NEW TOOLING FACT (Android):** adb CTRL+A (keycombination 113 29) + DEL (keyevent 67) does NOT clear every RN TextInput's value on this build — J05's custom-category-name kept its prefilled value; source-verify + explicit PARTIAL after bounded attempts is the working default (extend R77 #4).

## 2026-09-07 — QA Task 43e (FIX-Task-3 on-device verification, dual-platform) — 16 PASS / 2 spot-check FINDING; Part 2 deferred
- Run: `e2e-test-results/qa-task43e-fix-task3-verify-auth-cont-2026-09-07/` (report.md, ledger.md, screenshots/). HEAD b11c73ff (FIX-Task-3). iOS (iPhone 17 Pro Max) + Android (Medium_Phone_API_36.1) both mandatory, cold-reload-first (43d stale-bundle avoided; Item-1 discriminating check confirmed fresh bundle on both). Manual call tally ~395 (transcript still not cleanly mineable — only pointer main.jsonl; 43d/QA41 precedent).
- **All 6 numbered FIX-3 items PASS both:** Item1 (C03 Apple pre-check) — friendly banner, NO sheet/custom tab opens (43d C03 FAIL CLOSED — the real fix); Item2 (Discover default-state count) — Filters "Show 81 results" = grid "81 results · near CT" (43d default-leg FAIL CLOSED); Item5 "Your area: 06850" prefill label; Item3 button-text centering (A06 Continue-anyway + Remove-fav Cancel + Sign-Out Cancel, ~11-16% green #5DBB8E outline bands, centered, no regression); Item4 outage email-fallback CTA full-width solid #5DBB8E white-bold-centered (~94% band), armed→disarmed; Item6 staging SMTP-500 alert still shows "Open Supabase Docs" (dev-only branch correct), armed→disarmed.
- **NEW SPOT-CHECK FINDING (open Part-1 defect, BOTH platforms):** Linked-Accounts Apple-link calls the SAME initiateSocialLogin → pre-check fires (no sheet — primary goal holds) BUT renders generic `Alert('Error','Failed to link apple account. Please try again.')` — the friendly banner lives ONLY in SocialLoginButtons; LinkedAccountsScreen catch special-cases only EmailMismatchError → ProviderDisabledError falls to the generic branch. Root cause source-confirmed (LinkedAccountsScreen.performLinking L120-163). Recommend FIX-Task-4 (route ProviderDisabledError → shared friendly banner). C03 real-Apple-sign-in leg remains BLOCKED-on-env (Apple not enabled in staging GoTrue — now surfaces gracefully).
- **Part 2 (AUTH cont: O04-free/B01/Q/J/K/L/S08) DEFERRED per brief** — Part 1 must be fully clean (all 6 items + spot-check, no new FAILs) before Part 2; the spot-check is an open defect → honest-deferral (R40), O04-free remains the cheapest next.
- **3 toggles disarmed → none, DB-verified (22:06:41Z / 22:07:50Z; avatar untouched 19:18:54Z).** Personas: iOS qa.alice.17888183703295187@ "QA A06 iOS" + Android qa.alice.17888198382012019@ "QA A06 Droid" (both phone-verified, zip 06850) — both logged OUT at end. **NOTE: FIX-Task-3 already committed+pushed at b11c73ff at session start (brief said uncommitted) — nothing for QA to commit; 6-item on-device confirm = the confirmation the diff was waiting on.**
- **Android UX note for dev (not a blocker):** Linked-Accounts re-auth modal buttons sit UNDER the soft keyboard (KeyboardAvoidingView doesn't lift them) — needed the Gboard hide-chevron to reach Confirm.

## 2026-09-07 — QA Task 43d (FIX-Task-2 verification + AUTH continuation, dual-platform) — 20 PASS / 2 PARTIAL / 2 FAIL; toggles disarmed
- Run: `e2e-test-results/qa-task43d-fix-task2-verify-auth-cont-2026-09-07/` (report.md, ledger.md, screenshots/). HEAD 59a657c8 (FIX-Task-2). iOS (iPhone 17 Pro Max) + Android (Medium_Phone_API_36.1) both mandatory. Manual call tally ~430 (transcript not mineable — only pointer main.jsonl; QA41 precedent) ≈ 15.9 calls/platform-verdict vs 43c 9.6 — structurally higher: dual-platform mandatory + 2 fresh-signup persona blocks + Item-4 attribution disarm/re-arm + stale-bundle cold-reloads.
- **FIX-2 #1 (06850 no-waitlist) PASS both** (43c F1 CLOSED) · **#2 A06 green Secondary Outline PASS both** (F2 CLOSED; ~14% green band, Fix it solid) · **#3 cancel dialogs (Remove-fav + Settings Sign Out) PASS both** (all 4 Cancels green outline ~10-15%) · **#5 E02 Verify-≥1-digit PASS both** ("Please enter all 6 digits"; 999999 leg intact) · **#6 PARTIAL both**: applied-loc leg PASS (Show 97=grid) BUT default-state leg FAIL — modal 1154 vs grid 81 when home ZIP prefilled-not-applied (locationUnchanged='06850'==='' → false → global). **#4 C03 Apple FAIL both — ProviderDisabled classification NEVER fires**: oauthService signInWithOAuth(skipBrowserRedirect:true) returns authorize URL w/o erroring for disabled provider → 400 renders raw JSON in iOS SFSafari sheet / Android Chrome custom tab. Tier-0 PASS ≠ on-device (real finding).
- **Part-2 (43c-BLOCKED → CLOSED):** C05 outage banner PASS both · H03 avatar-failure non-blocking PASS+DB avatar_url=NULL both (personas qa.alice.17888071480089182@ iOS "QA H03 Alice" / qa.alice.1788808174560139@ Android "QA H03 Droid"; Android Complete Setup needed 2nd tap) · S03/S04/S05 PASS both (S05 bad_email NEW 400 value first-ever inducible). C07 stays BLOCKED (ticket #19). **3 toggles all disarmed → none, DB-verified 19:18Z (owner-mandated).**
- **Process lesson (apply-handoff):** after a new commit, launched dev-clients that RESUME old processes serve STALE JS bundles on BOTH platforms (iOS resumed pre-FIX process; Android adb "delivered to running top-most instance") — force a cold reload (iOS terminate→launch; Android Dev-Launcher → tap 10.0.2.2:8081 row) and confirm via a discriminating behavior test before trusting results. Recommend codifying as an agent rule. Part 3 (J/K/L/Q/B01/O04-free/S08) deferred with explicit reasons — O04-free is the cheapest next (personas qa.alice.17888028470414455@ / qa.a01.and.1788797905@ ready).
- **APPLY-HANDOFF DONE (same turn, 2026-09-07):** the 3 handoff rule suggestions codified — QA playbook §5.70 **R79-1** (force a cold dev-client reload after any code change + confirm the fresh bundle via a discriminating behavior test) and **R79-2** (Tier-0 mocked-error green ≠ reachable on-device — drive the real trigger; Item 4 ProviderDisabled dead-code object lesson), **R71 extended in place** (manual-tally fallback when the transcript is unavailable — only pointer `main.jsonl` exists); dev-side **mobile-client BP-88** (error/defensive branches need a real runtime trigger) + Kids P2P App Builder §9.2 symptom entry + appendix BP-88 pointer; QA-Test-Agent.agent.md §4 dated pointer added. Commit recommended per the 2026-08-18 lesson (QA reviews/pushes manually).

## 2026-09-07 — Agent Improvement #1 codified (QA Agent Efficiency Rules) — playbook §5.69 R78 + §5.68 R77 #2 corrected / #12–#13 added
- Codified the QA Task 43c friction-analysis rule-set (Agent Improvement #1, owner-framed) as playbook **§5.69 R78-1..10** in `.github/instructions/QA-Test-Agent.instructions.md` + a dated §4 pointer in `.github/agents/QA-Test-Agent.agent.md`. Source: `e2e-test-results/qa-task43c-auth-android-r2-2026-09-07/decision-and-outcome-log.md` (mined **412 executions / ~43 verdicts ≈ 9.6 calls/verdict**; ten friction episodes ≈ 25% of calls; target 6–7). Pure playbook/memory discipline — no dev work — applies from the next QA session.
- **R78-1** recon-first (read `/memories/session/*` + prior-round report facts before any prep; 43c recon collapsed to ~35 calls via the 43b→43c handoff). **R78-2** read-only fixture/simulation-toggle/persona feasibility gate before device time (extends R-NEW-6; 43c pre-flagged H03/C05/S03/S04 → 0 wasted device calls). **R78-3** source-read a form/modal's submit-disable/error-render/dialog-type before driving (43c: ~6–8 no-op taps on a disabled ResetPassword submit). **R78-4** never-TAB between RN fields (pointer R77 #11). **R78-5** one-tap dev fixtures (pointer §5.13/R75; `dev-fill-test-user-1` = ~25 calls → 1 tap). **R78-6** tap current-tree bbox centers, never row edges (O03 mis-tap). **R78-7** re-list before every tap on layout-unstable screens (ResetPassword ±327px oscillation, largest cluster ~45–55 calls). **R78-8** one screenshot+tree AX-drivability gate before text entry on unproven surfaces (custom-tab OAuth ~15 calls). **R78-9** relaunch-on-corrupt (pointer §5.2). **R78-10** mandatory evidence screenshots (pointer §5.6). Techniques: DB-target over scroll-hunt, batch by persona+surface, header-from-current-tree, single-param adb fragments, fixture/provider-gated surfaces excluded from general batches.
- **R77 in-place:** #2 corrected (IME presence per-emulator/per-IME — 43b none vs 43c Gboard + "Allow Gboard to record audio?" prompts, deny+back-key); #12 added (adb multi-param `&` fragments lossy — S09 `error_description` dropped; craft single-param fragments); #13 added (real external OAuth via Chrome custom tab = toolset-BLOCKED on Android — C01/C02/C04; C06 silent-return still drivable/PASS; C03 Apple = provider-config finding + raw-JSON UX note).
- **Forward-looking dev asks listed in §5.69 (route to FIX-Task-2 addendum, NOT behavioral):** (1) ResetPassword ScrollView oscillation fix (highest-value, ~45–55 calls); (2) AX-expose submit disabled state; (3) kill Android TAB traversal (queued FIX-Task-2 item 7); (4) Android-OAuth drivability mechanism; (5) `qa:ax-tree --coords`; (6) scroll-to-element primitive; (7) auto-screenshot-on-transition.
- Recommend committing these tracked-rule edits in the same turn they are applied (2026-08-18 playbook-loss lesson) — QA reviews/pushes manually per convention.

## 2026-09-07 — QA Task 43 v3 (AUTH + ACC closure, adjusted for 2 owner-pending items) — 8 batch items: 7 PASS / 1 PARTIAL; AUTH-S01 + C07-login correctly excluded
- Run: `e2e-test-results/qa-task43-v3-auth-acc-closure-2026-09-07/` (report.md, ledger.md, screenshots/). HEAD fe5294a3 (DT-128 v2). **Exact mined call count 357 tool executions / 330 msgs** (qa:mine-call-ledger on transcript bfa6ac72…).
- **Batch 1 — 6/6 PASS:** J07 + J12 (policy_failure=no_policy → TOS/Privacy Alert "not available"+auto-back; Liability inline error+Retry) · J08 (fetch_failure → TOS/Privacy Alert+inline no-Retry; Liability inline+Retry→disarm→Retry recovers) · J02 (test-payfail UI email/pw — NOT qa-login-as — gate fires → Decline 0 rows → relaunch re-prompt → Accept v1.1 row) · J05 (REAL admin /settings/policies publish QA TOS v1.2 88abfb6a + Privacy v1.1 49b8398b → test-buyer re-prompt showing new versions → accept both → **restored original v1.1/v1.0 active**; test-buyer's old acceptances stay valid → 0 residual re-prompt; QA versions left archived w/ acceptance rows — not deletable, harmless) · G02 (test-payfail PaymentFailureBanner "Retry 1 of 3"; test-trial "5 Days Left in Your Trial"; test-grace Action Items ID+Grace+hidden Draft — "Show 1 more action" reveals ResumeDraftBanner Continue/Maybe later; Home AX coords logical-vs-rendered after flingy scroll — R31 + view_image needed for show-all).
- **Batch 2 — 2/2 PASS:** P03 (ran seed:staging per brief → self-refresh unread fixture msg a85aabb3 on pending d8e14d86 → chat badge "1" visual → open chat → read_at 14:06:30 → badge cleared) · H03 (faq_failure=fetch_failure → 5-cat fallback + 10 fallback FAQs; disarm → real DB FAQs incl. DB-only QA rows).
- **Batch 3 — C07 PARTIAL** (as briefed): can_set_password RPC live true; check_account_exists_by_email has_password false; LinkedAccounts + SetPasswordModal wiring source-verified (DT-128 set-password-button opens modal !hasPassword; onSuccess reload flips row); LinkedAccounts on-device "Password ✓ set" branch confirmed. **Modal drive NOT drivable** — no session path for password-less qa-social-only without Google identity (no dev email-OTP bypass in auth.ts; email magic-link needs the pending SendGrid SMTP; setting a pw would consume the fixture blocking owner's Google attach). Recorded "modal + RPC verified; full closure pending Google identity attach."
- **Bookkeeping:** ACC-K01–K04 OPEN→NOT-SUPPORTED (NOT-IMPLEMENTED bucket — MFA deliberately absent); AUTH-Q04 guide confirmed $1.49; tracker notes S01 (still BLOCKED SendGrid) + C07 (PARTIAL). Roll-ups: AUTH 126 PASS/3 PARTIAL/2 OPEN; ACC 68 PASS/2 OPEN/4 NOT-SUPPORTED/1 SKIP. R56-reconciled.
- **Doc-vs-build nuance (not a defect):** J07/J08 guide says TOS/Privacy load-fail shows "inline not-available"; current build = in-app Alert + auto-goBack (TOS/Privacy), inline only for Liability. Functional guarantee (clear msg, no crash, navigable) met both ways.
- **Excluded (correctly, unchanged):** AUTH-S01 (owner SendGrid step — not re-attempted), AUTH-C03 (Apple), ACC-B03 real-SMS (F&F), ACC-G09 (won't-test), ADM 3 permanent, TRD 19 deferred. **Next (flagged as the very next action): after owner's 2 steps, a 2-case round (S01 full run + C07 login leg) closes AUTH entirely.**
- Session toggles all disarmed (policy_failure/faq_failure none); personas: last session test-buyer logged in; qa-social-only untouched (still password-less).

## 2026-09-06 — QA Task 41 (AUTH+ACC backlog B0–B4, first pass) — 11 genuine PASS / 0 FAIL / 0 BLOCKED-as-defect / 2 BLOCKED-on-fixture (P03, C07) + B0 handoffs + B3/B4 parked (R40-explicit)
- Run: `e2e-test-results/qa-task41-b0-b4-exec-2026-09-06/` (report.md, ledger.md, handoffs-filed.md, screenshots/). HEAD 48347297. ~148 tool executions (manual tally — exact qa:mine-call-ledger N/A: transcript unreachable). Tracker atomically updated (R56/R57): AUTH PASS 118→**125**/OPEN 11→**4**; ACC PASS 57→**61**/OPEN 17→**13**.
- **B0: 6 dev/ops handoff issues filed** → mobapp #14 (Apple/C03), #15 (staging SMTP/S01), #16 (Twilio A2P 10DLC/B03 real-SMS leg), #17 (`qa_local_faq_failure` toggle/ACC-H03), #18 (G09 dead-branch disposition), #19 (real Google identity on qa-social-only/C07). MFA K re-scope = bookkeeping note only.
- **B1 reconcile (9 rows, evidence-verified before each flip):** AUTH L02 (Group L 4/4 PASS 08-21), P18/P19 (group-p-reverify 08-23), Q06 (group-qs-fix-verify 08-23, 12-event CHECK), H03 (FRESH on-device spot-confirm 09-06: qa_avatar_upload_failure armed → Warning "Profile will be created without avatar" → profile created, DB avatar_url NULL on qa.alice.17887204905099267; disarmed 0 residue); ACC F02 (08-25), L02/L03/L04 (full-closure 08-26 crash_trigger once/persist + CDP).
- **B2: C05 PASS** (qa_provider_unavailable=google → Login Google → inline "Google is temporarily unavailable…" + provider-error-cta dismiss; disarmed); **Q04 PASS both personas** (Help→SP Calculator Books/$25: test-buyer sub fee **$1.49** = live cfg 149 / test-free **$20.00** = cfg 2000; guide fee example stale $1.00→$1.49 doc-drift; flipped + C05 flipped).
- **B2 blocks (fixture, not code):** **P03** — test-buyer 0 unread; seed unread fixture SKIPS (`seed-staging-data.ts:1080` guard — target oldest trade already has seller msgs), all buyer↔seller convos on terminal (frozen) trades, pending offers have no chat until accept. **C07** — **qa-social-only NOW HAS a bcrypt password ($2a$)** = standing fixture regression (dev to NULL encrypted_password) + no Google identity (#19).
- **B3 legal (J02/J05/J07/J08/J12) + B4 G02 NOT driven** this session — mechanisms ready (policy_failure toggle; J02 needs email/pw persona w/ unaccepted TOS not qa-login-as; J05 admin /policies publish; G02 needs payment-fail + ≤7d-trial personas; test-trial currently ABSENT — deleted in QA Task 40). Continuation round + handoff.
- Findings: (1) P03 seed unread-fixture skip; (2) qa-social-only password-bearing regression; (3) Q04 guide fee $1.00→$1.49 doc-drift.

## 2026-09-06 — QA Task 40 (DEV-TASK-127 Tier 1 re-verify, closes SUB + ADM) — 8 PASS / 0 FAIL / 0 BLOCKED
- Run: `e2e-test-results/qa-task40-dt127-tier1-verify-2026-09-06/` (report.md; screenshots/). HEAD 9db0b742 (= 708d01c7 + flow-registry docs). **Exact mined call count 152 tool executions / 134 msgs** (short single-purpose round). Tracker: SUB-TC-N06 ACTIVE→PASS (SUB 76→77 PASS, ACTIVE 2→1, genuinely-active 0); ADM-TC-L02 row Notes appended (no count flip; ADM 144 PASS/0 OPEN already).
- **Batch A SUB-TC-N06 PASS both legs (SUB's last real open case CLOSED):** test-trial ensure 5d → ≤7d urgency pill "5 days left in trial" (theme warning #FFF3E0/#FFA726, 0 legacy #4A7C59) + NO free-days pill (correct for trial). ensure 14d (DB trial_end 09-20) → first re-open rendered STALE non-trial branch — R59-class (fixture password re-sign invalidated the session mid-flow; clean relaunch fixed it, NOT an app defect — do not re-report). Clean relaunch → >7d: NO countdown badge, plain upsell. **DT-127 item 7 `trial-end-line` verified on BOTH branches with correct DB date binding** ("Trial ends September 11/20, 2026"). test-trial reset 0 residue (DB-verified).
- **Batch B F-1 payment-cache fix VERIFIED:** test-free (MASTERCARD 4444) → Payment Methods (cache populated) → Remove → detach EF → invalidatePaymentMethodCache()+setPaymentMethod(null) → "Removed" → empty; **same-session remount → TRUE empty (no stale card)** — pre-fix defect gone. Restored via `qa:ensure-cards --persona test-free` (pm_1UCkX54…, DB-verified).
- **Batch C ADM-TC-L02 INDEPENDENT live-admin PASS:** /sp-analytics (samer, :3001) renders velocity/gap/avg-cash/anomalies; 90d = "7 categories · 6 flagged"; range 7/30/90 (**guide's "365" = doc-drift** — DateRangePicker 7/30/90 by design); category click → `/categories?edit=<id>&tab=sp-config`; CSV export = client blob `sp-analytics-90days-*.csv`, 6 columns + 7 rows; R54: all metrics reconcile digit-for-digit with `get_category_sp_analytics(now()-90d, now())` (return = category_id,name,velocity,gap_percent,avg_cash_per_trade — no anomaly_flags col; flags client-derived).
- **Batch D:** (1) grace-footer-back-button AX-exposed (16,877,408,44) 44pt target, drivable (single tap → Home), **NO tab-bar occlusion — ManageKidsClub in TAB_BAR_HIDDEN_ROUTES resolves DT-124 item-9**; (2) expired badge "Expired" + **#E85D75 confirmed** (0.41% full-frame) — DESIGN FLAG: reverses DT-119 neutral-gray; one-line revert in badge_expired if a terminal reading is preferred; (3) trial-end-line first-look PASS (see Batch A).
- **Batch E LeaderboardScreen source-audited PASS** (latent screen, no nav/deep link): 9 swaps canonical (#5DBB8E/#6B6B6B/#1A1A1A); app-wide R62b grep = 0 legacy hexes.
- **R77 recommended (owner-requested):** destructive-path cache-invalidation checks verified by a same-session remount assertion (the F-1 class) as a standing re-verify rule. Board now = 3 permanently-tooling-blocked ADM (B03/B06/B07) + the 47-case TRD/AUTH/ACC backlog (next scoping decision — explicit per-case plan, not piecemeal).

## 2026-09-06 — QA Task 38 (DT124 on-device items 2/4/9 + first R63–R76 real run) — 2 PASS / 1 PARTIAL; apply-handoff
- Run: `e2e-test-results/qa-task38-dt124-items-249-2026-09-06/` (report.md; screenshots/). Mobile HEAD 8b1d1294 + DT124 mobile code. **Batch A (Item 2 sync-on-return) PASS:** real hosted Express drive on qa-payout-seller → verified (SSN `0000` via Cmd+A, phone re-touch, industry search "merchandise"→Other merchandise R68, bank Test (Non-OAuth)+Manually-verify-instead+Finish-without-saving, website **`https://www.passitup.com`** — `www.example.com` rejected "Not a valid URL" (scheme + non-placeholder domain required — NEW runbook value)); **cold terminate + `payout-settings?success=true` (DT123) return AUTO-SYNCED** — DB `97c4c217` flipped `is_verified/onboarding_complete/payouts/charges=true` at 15:41:20 mount, UI "Verified & Active" + no continue-onboarding CTA, **NO manual sync EF call** — QA Task 37's stale-method ~37-call class ELIMINATED (F-2 closed by DT124 item 2). **Batch B (Item 4 AX) PASS:** PayoutMethodBottomSheet rows NOW AX-exposed (`sheet-overlay/container/set-primary/edit-details/delete-method/cancel` — REVERSAL vs QA34/37 pixel-only; set-primary functional + DB); `add-payment-method-btn` (qa-payout-seller + test-buyer) + `update-payment-method-btn` (test-buyer Visa 4242 after CLEAN RELAUNCH — earlier no-PM was stale fetch R59) + `resubscribe-kids-club-button` all AX-exposed. **Batch C (Item 9 sticky footer) PARTIAL:** grace Manage sticky footer pinned + Re-subscribe CTA visible w/o scroll, BUT **"Go Back" NOT visible — occluded by floating PersistentTabBar** (`ManageKidsClub` NOT in TAB_BAR_HIDDEN_ROUTES [only ItemCreate+NotificationSetup]; graceFooter no tab-bar inset; resubscribe AX y827-878 overlaps pill y848-904; bottom-band OCR no Go Back) — NEW MODERATE (fix class: add ManageKidsClub to TAB_BAR_HIDDEN_ROUTES — QA Task 28 NotificationSetup precedent). **Batch D: exact mined count 234 tool executions / 220 msgs (run-in-progress) vs QA Task 37's 487** — hosted Express drive + AX batches well under half; item-2 sync = zero extra calls (was 37).
- **APPLY-HANDOFF (2026-09-06):** (1) NEW canonical hosted-Express value: business "Your website" needs `https://` scheme + non-placeholder domain (example.com → "Not a valid URL"); add to the SUB guide G01 runbook (R76). (2) Dev follow-up: add `ManageKidsClub` to `TAB_BAR_HIDDEN_ROUTES` (or pad graceFooter for the tab-bar band) to fix the item-9 Go Back occlusion; re-verify grace + expired branches on-device. (3) After item 9 fixes, DT124 item 8 (server-side Express completion fixture) is the next investment. (4) R63–R76 + qa:start-state + qa:mine-call-ledger + G01 runbook all worked as designed on first real use (no rule changes needed). Cleanup R41/BP-70: qa-payout-seller method 97c4c217 + Connect acct_1UCi0B3AdmJKsmxv deleted (API 200 + reset), 0 methods/0 balance/0 connect. Tracker: G01 Notes refreshed (no status flip).
- Run: `e2e-test-results/qa-task37-g01-redrive-2026-09-06/` (report.md; screenshots/). Mobile HEAD 8b1d1294 (DT123), admin :3001 real session. **Batch A PASS 2/2** (DT121-3 delete-only "Add a method" → opens modal; DT121-4 resume-path phone re-verify hint on AddPayoutMethodModal). **Batch B (SUB-TC-G01) FULL PASS — promoted out of the Remaining ACTIVE table:** fresh Connect `acct_1UCgIu4HHYZdHIok` one-tap via AX-exposed Add-modal (DT122 I-1: AddPayoutMethodModal + WithdrawModal buttons AX-exposed this build; the method bottom-sheet is a NATIVE RN Modal whose buttons do NOT surface — drivability build-dependent per §5.31); **Test/User identity prefilled + DOB 01/01/1990, NO gov-ID doc gate** (QA Task 36's `individual.verification.document` blocker GONE post-DT121 name fix; currently_due only phone/SSN/TOS); hosted SSN canonical `0000` needed (8888 prefill fails name precheck → "Personal details Incomplete"); phone needed re-touch; drop→terminate→cold `payout-settings?success=true` deep link (DT123) → `continue-onboarding-<id>` CTA → resumed → Agree+submit → details_submitted/payouts_enabled/charges_enabled=true, currently_due=[]; set primary; funded $5 → Withdraw → payout `8f085298` gross500/fee26/net474 processing + balance→0 + history $4.74/Processing. **Batch B tool-call count ~110-120 vs QA Task 36's ~130-160** (R63-R70 win; most cost = hosted-Express Safari form driving). **Batch C (billing dedupe, D05-style) PASS:** fresh disposable real Checkout → active → saved-card Set up (`pm_1UCgvY…` on sub) → admin :3001 cancel → grace_period/admin_override → cold relaunch grace UI (Home Re-subscribe Now + Manage Grace Period SP-frozen banner) → in-app Re-subscribe → "Subscription Renewed / Your Swap Points are now available." → ACTIVE (new Stripe sub) → **EXACTLY ONE billing_history row per invoice** (orig in_1UCgva4→1; renewal in_1UCh0p4→1; NO dup) — **QA Task 36's MED (two rows/same invoice) CONFIRMED FIXED by DT121 `onConflict: stripe_invoice_id`**.
- **APPLY-HANDOFF (2026-09-06):** (1) **R63–R70 are STILL NOT codified** in `.github/instructions/QA-Test-Agent.instructions.md` (playbook ends at R62/§5.63; file not committed since QA Task 35) — QA Task 37 applied QA Task 36 decision-log patterns as de-facto rules; codify R63-R70 (session checklist; instrumented one-tap modals; never long-press Safari fields; hosted flows disposable + Continue-Onboarding resume; currently_due-first; Node-only env reads; §8.3 template read before handoff). (2) **NEW architecture fact:** `request_seller_payout` (manual withdrawal, trade_id-NULL) rows are NEVER Stripe-dispatched — no dispatcher exists for them (trade-completion trigger only); row stays 'processing', "dispatched to payment provider" copy is aspirational. Recommend a dev follow-up (manual-withdrawal dispatcher) — not a G01/H03 regression, matches QA Task 34 H03 acceptance. (3) Hosted-Express SSN canonical `0000` (8888 prefill quirk). (4) App method-stale after hosted completion until direct sync-stripe-connect-status EF call with persona JWT (silent on-mount sync miss observed once). (5) Locator gaps: Manage Kids Club+ "Add Payment Method" + grace "Re-subscribe to Kids Club+" buttons not AX-exposed (coordinate taps). Cleanup R41/BP-70 complete: Stripe customer cus_VD79HhriarejMN + Connect acct_1UCgIu4 deleted via API (200), disposable DB rows + auth user deleted (read-back 0), qa-payout-seller method 24ddd76a + payout 8f085298 deleted (balance 0/0). Tracker SUB updated (G01 main-table PASS row added + remaining-row struck; D05 dedupe append; header refreshed).

## 2026-09-05/06 — QA Task 35 closes with an R62c apply-handoff (QA Track)
- **APPLY-HANDOFF (QA Track, 2026-09-05):** QA Task 35's `Suggested to improve agent rules` (R62b on-device method) wired into `.github/instructions/QA-Test-Agent.instructions.md` §5.63 as **R62c** (extending R62a/R62b — no duplicate): (1) `grep -rEn` substitute when `rg` is absent (confirmed absent on this machine); (2) **full-frame `qa:badge-scan` presence scan** per rendered screen vs a canonical-screen AA baseline (~0.01–0.06% gray = AA noise; real legacy fill ≥0.1%+) — **per-region badge-scan scans returned false zeros for known text bands (do not trust a region-scan zero)**; (3) for ContinueKidsClub (and any most-seen-branch-isn't-the-persona's screen) **always review the upsell/non-active branch** — where D1 hid — and source-audit + explicitly report any unrendered branch. §6.4 intro pointer updated. `.github/agents/QA-Test-Agent.agent.md` §4 dated pointer added (2026-09-05/06). Commit recommended (2026-08-18 lesson) — QA reviews/pushes manually.
- Run itself: `e2e-test-results/qa-task35-dt119-branches-sub-2026-09-05/` (report.md + ledger.md). 13 PASS / 0 FAIL / 0 BLOCKED / 2 PARTIAL (trial-branch source-audit + D05/G01 fixture-gated). D1 (ContinueKidsClub upsell `#4A7C59`) VERIFIED FIXED on the branch QA Task 34 missed; G06 CTA, G05 friendly copy, F08/G10 pagination, Batch E footnotes all live. Tracker SUB PASS 69→72, PARTIAL 5→2 (F08/G10/G06 flipped; C05/L05 remain PARTIAL).

## 2026-09-05/06 — DT-119 closes QA Task 34's S3/S4 apply-handoff with a NUMBERED QA-playbook rule
- §5.63 R62 now recorded in the QA playbook (`.github/instructions/QA-Test-Agent.instructions.md`): R62a = audit EVERY rendered branch of a multi-state screen (the D1 LESSON, now a rule); R62b = the standing app-wide off-brand-hex grep `rg -n "#4A7C59|#4D4D4D|#808080" p2p-kids-marketplace/src` is part of every design-compliance pass. §6.4 intro carries the mandate; agent-file §4 dated pointer added; dev-side BP-82 detection grep widened app-wide in `.github/instructions/mobile-client.instructions.md`. All 3 hexes swept out of every actively-navigated screen (see /memories/repo/dev-task-119 memory).

## 2026-09-05/06 — QA Task 34 (Payout E2E positive+negative on DT118 fixture) — the round that closes "can a seller withdraw + do failure paths work" → YES
- **POST-RUN OWNER-FLAGGED DESIGN DEVIATION (D1, HIGH, not caught in-run):** ContinueKidsClubScreen **upsell branch** ("Start Kids Club+", for trial/free/grace/expired users) is OFF-BRAND vs `docx/design-system-passitup.md`: pricingCard + primaryButton ("Join Kids Club+ on the web") + secondaryButton use `#4A7C59` (non-canonical dark green, legacy ds.primary[500] — same class flagged on Discover Phase 24) → should be `#5DBB8E`; greys `#4D4D4D`/`#808080` → `#6B6B6B`/`#999999`. The SAME file's DT118 item-8 active branch is already on-brand (`#5DBB8E`) — internal inconsistency; the most-seen branch is the leftover. Fix recommended (dev follow-up, QA execution-only) in report.md Finding D1 + the handoff's What-Needs-Fixing. LESSON: on a dual-branch screen verify BOTH variants against the design doc — the DT118 verify only checked the active branch's colors; the upsell branch's off-brand `#4A7C59` slipped through.
- **APPLY-HANDOFF (2026-09-05, user-confirmed destinations):** S1+S2 (QA tooling: qa:ax-tree `(no coords)` reality + `list_dir` `path`-schema) wired into QA playbook §5.1 + §4 dated pointer in `.github/agents/QA-Test-Agent.agent.md`. S3+S4 (design-audit-every-rendered-branch + app-wide off-brand-hex grep `#4A7C59`/`#4D4D4D`/`#808080`) wired as **BP-82 extension** in `.github/instructions/mobile-client.instructions.md` (new rule 6 + expanded family/grep/Problem) + §9.2 + appendix Rule-Index pointer updates in `.github/agents/Kids P2P App Builder.agent.md` (no new BP-N — dedupe discipline). Commit these tracked-rule edits same-turn (2026-08-18 lesson); QA reviews/pushes manually.


- Run: `e2e-test-results/qa-task34-payout-e2e-2026-09-05/` (report.md + ledger.md). Persona qa-payout-seller (a1234567…f2) via `qa:payout-fixture` (ensure/methods --scenario/balance/stage-trade/reset). Tracker SUB PASS 56→69, PARTIAL 4→5, ACTIVE 20→6 (G01/M01/M06/M07/N05/N06 remain).
- **H03 REAL positive E2E PASS** (verified method + $5 balance): Withdraw Now → WithdrawModal → Confirm → payout `899e70cf` (gross 500/fee 26/net 474/processing/stripe) + hero 5.00→0.00 + history row. Fee = Stripe $0.25+0.25%. **H01/H04/G11/G05/H06/H07/G04/G09/F05/F06(Pending-leg)/F07/K02 PASS; G06 PARTIAL.** H07 = min config 200→0 via `qa:admin-config-set` → below-floor withdrawal succeeds → reverted+verified.
- **Guard architecture fact:** withdraw CLIENT gate keys off `is_primary` (not is_verified) → an unverified auto-primary row opens WithdrawModal; the RPC `request_seller_payout` enforces `is_verified` at Confirm → "Withdrawal Failed / Primary payout method is not verified". Guide G05's NoMethodModal expectation = doc-drift for that state (server guard works, no money moves).
- **G06:** a genuine `requires_action` payout is stageable on the fixture (stage-trade with NO verified method → create_seller_payout_on_trade_completion returns status='requires_action'!). Row renders "Action Required" (red); NO "Set Up Payout Method" CTA on the live PayoutSettings history card (source-proven; guide targets dead SellerEarningsScreen) → PARTIAL + UX gap.
- **F06 semantics note:** hero Pending = reconcile GROSS (stage-trade $20.00) while history row = NET ($19.70, Stripe fee $0.30) — reconcile stores pending as gross; client derive path sums net. Dev decision wanted. Pending→Available needs real dispatch (synthetic boundary; runbook mandates reset before 2d buffer).
- **test-seller sub status = grace_period** (trial ended 2026-09-01, grace to 2028) NOT active → ContinueKidsClub active branch needs an active persona (qa-payout-seller is active). test-seller reconciled hero verified $140.40/$442.60/$583/25 trades (DT118 item 2, DB-verified). DT118 items 4/5/7/8/9 fixes all re-verified on-device.
- **D05 cross-ref'd from DT118 live-verify** (not independently re-driven): deployed renew-subscription EF unchanged at HEAD 5ef735fa with step-9 service-role sp_wallet unfreeze + loud-failure. Fresh re-drive = real-Stripe disposable (R41-class session).
- **Fixture/tooling facts:** WithdrawModal Confirm/Cancel NOT AX-exposed (pixel-scan); its method bottom-sheet IS AX-exposed (sheet-set-primary/edit-details/delete-method/cancel). PayoutSettings refetch-on-focus unreliable — guaranteed remount = Home tab (59,886) then action-tile-payouts (368,383). `payout_fetch_failure` toggle values fetch_failure|none (F07/K02). Admin-config helper sets number type w/ --data-type number. `qa:payout-fixture` runs from p2p-kids-marketplace (cwd). "No Balance"/"Withdrawal Requested" success alerts: Withdrawal-Requested is NATIVE alert (tree blanks, OCR OK ~y547); No-Balance/Cannot-Set-As-Primary/Withdrawal-Failed are in-app GlobalAlertProvider (AX-exposed).

## 2026-09-05 — QA Task 33 (SUB 30-closure partial + DT117 visual) — playbook extensions applied (apply-handoff)
- Run: `e2e-test-results/qa-task33-sub-30-closure-dt117-2026-09-05/`. DT117 items 6/7/8 + ADM M03/M04 audit re-verify 4/4 PASS; SUB D05/F01/F03/F04/G07/G08/H02/N04 PASS + F08/G10 PARTIAL (tracker PASS 47→56, ACTIVE remaining 30→20; I06 DOC-DRIFT→PASS; A05 dup pruned). NEW MED: renew-subscription EF leaves sp_wallets grace_period after in-app reactivation (bb862192 retained as repro). NEW MED: test-seller seller_balance inflated ($15,603 avail / 790 trades vs real 25/$583). Owner fix records: Load More occluded by floating nav bar; WithdrawModal "Payout Fee" label must say processor fee not platform fee. UX enhancement record: ContinueKidsClub active state blank dead-end.
- **Playbook extensions applied same-turn (QA reviews/pushes manually):** R24 extended (check EVERY money object a success message references, not just the written row — D05 wallet evidence); R13 extended (money action skipped for unsafe balance → name missing positive+negative legs in Known Gaps); §5.9 extended (Payout Settings scroll-resistant ~0 movement); §5.31 extended (PayoutSettings WithdrawModal buttons NOT AX-exposed / pixel-scan; its method sheet IS — re-check per build). Agent-file §4 dated pointer added. Recommend committing (2026-08-18 lesson).

## 2026-09-05 — QA Task 31-M Round 5 (mobile-owed closure, batches A–E) — 13 mobile legs driven across 11 PASS rows + 3 PARTIAL→PASS; 0 FAIL/0 BLOCKED
- Report/ledger: `e2e-test-results/qa-task31m-r5-mobile-owed-2026-09-05/`. ADM tracker: PASS 132→135, PARTIAL 23→20 (D08/Y08/N2-A01 flipped; roll-ups on both §1 + section header refreshed — R56). Admin dev server was STALE (1d5h across code changes → 500/404) — §5.21 restart (kill 19907/19936/19938, npm run dev) fixed it.
- **Batch A D-group ALL mobile legs PASS on disposable cats (A 1eee27c5 / B 95b36f9a, deleted at cleanup, 0 residue):** D08 = DT110 Move Down reorder → mobile CategorySelectModal B-before-A (PARTIAL→PASS); D02 = multiplier 1.15 → ItemCreate SP estimate "~23 SP" + "1.15x" on $20 + Bonus badge in picker; D05 = icon upload (favicon.png) → DB icon_url + mobile row renders image (no 📦 fallback) vs 📦 on no-icon rows; D06/D07 = Sports redemption cap 3 (admin, DB) → Make Offer "Max: 3 SP (75% of price)" + typing 10 clamps to 3 + "3 SP applied" → cap reverted NULL; D09 = bulk deactivate → picker A+B gone / reactivate → A back; D10 = delete B → gone / A present. Sports spend 75% displayed (D06).
- **Batch B:** E04 = min_user_radius_miles 5→10 (qa:admin-config-set) → Discover slider min "5 mi"→"10 mi" → reverted 5 (live propagation proven). E03 = deactivated-node ZIP 90210 → Discover filter "Not Available in Your Area / not live in ZIP 90210 yet" modal (cannot-join) → No-thanks → 0 waitlist residue. E02/E05 mobile legs = fresh-signup fixtures (R41) — not driven.
- **Batch C moderation mobile legs:** C08 = approve test-seller pending afd3384a via targeted /listings search (seller-email+Pending — R4 unreliable-nav note RESOLVED) → buyer Request to Buy (was "no longer available"); C06 = Force Delete same item → status deleted → buyer "no longer available" (item consumed); C10 = rejected 04662c2c → seller ListingSafetyReview "rejected by our safety team" + REJECTED + rejection reason (listing-safety deep link). C09/X05 demonstrated by shared surface/mechanism (no needs_edits/flagged item staged).
- **Batch D/E:** N03 = referral sp_config (40/20/10/25) → mobile Referrals values match; Y08 = palette Listings nav → /listings?q=<title> "Results (1 of 1)" — DT108 FIX VERIFIED (PARTIAL→PASS); N2-A01 = per-trade financial_audit_log (trade 01121468 6 rows) + /audit page (PARTIAL→PASS). X07 (needs dev-staged failed payout), Y05 (driver-limited) unchanged.
- Tool facts: mobile-mcp categories kept dropping mid-session (re-activate_* restores — R4 pattern confirmed); CategorySelectModal is AX-drivable for row reads but its × close was unreliable (row-selection closes reliably — stuck modal cleared via relaunch R-NEW-1); admin Playwright locator.click() actionability timeouts → DOM el.click() reliable; /listings status select option value for "Available" = `active` not `available`; financial_audit_log is per-entity (wallet AND trade); sp_config uses config_key/config_value columns.
- Staging residue flagged for dev: QA nodes (QA Auto G01/G02, QA T31 Disc, Test Node 17xxx×3 06850, Diag Test Node), ~50 dup "Cash-Only Item" $20 listings under test-seller, 5 pending zip_waitlist rows (07999×4/12355), orphaned category-icon storage on category delete.

## 2026-09-05 — QA Task 31-M Round 4 (DT113 on-device verify + F08 completion + Batch C partial) — 11 genuine PASS / 0 FAIL / 0 BLOCKED
- Report/ledger: `e2e-test-results/qa-task31m-r4-dt113-f08-batchc-2026-09-05/`. ADM tracker: NO count flips — Notes refreshed on C03/C04/L04/F03/F05/F08/I03/I04/X06 (all already PASS). Roll-up unchanged 132/23/1/1/3.
- **Batch A — DT113 fixes ALL verified on-device (R3 findings #1/#2/#3 CLOSED):** A1 friendly copy both parties (buyer uncaptured-refund "This trade was cancelled. No payment was taken." / seller "…by our support team." — never raw `dispute_resolved_refund`); A2 REAL live-realtime: buyer's OPEN untouched timeline updated in-place to "Dispute in progress" after the DB dispute-open, then to Cancelled+friendly copy after admin resolve (trades-in-realtime + AppState-active refetch proven); A5 buyer closing-summary card `timeline-dispute-closed-summary` on the uncaptured case; A7 countdown pill "Confirm pickup — auto-completes in 71h 27m" NO "left"; A3 seller timeline fee = STORED not config (trade 01121468 stored 600 → −$6.00 stable under platform_fee_seller_percentage 10→20, reverted); A6 /users "(20 on this page) of 5232 total" (5232 = non-deleted profiles EXACT) + /reviews "(10 on this page) of 13" — but /payments "first 100 of 827" + /trades "page 1 of 12 — 579 total" use DIFFERENT window phrasing (827 = trades EXACT; 579 = single non-bundle trades EXACT) — LOW consistency note.
- **Batch B — F08 CLOSED end-to-end (fixture-gated leg gone):** qa-first-trade free persona (…014) Leg1 real offer ($1.49) → accept → complete → trade 27f04815 COMPLETED fee 149 + fee_state no_completed_trade→first_trade_completed (count 1); Leg2 second offer fee $3.24 = 324 (buyer_fee_subsequent_fixed 199 + 5%×2500 — NOT active-member 149, that's trial/active tier; brief's parenthetical imprecise for free persona) → trade 37141792 pending 324; reset → 0 residue.
- **Batch C — partial-execution reality check:** the tracker shows most brief-listed IDs already ✅ PASS (QA29/30/31, several with prior mobile legs) — the round genuinely closed ADM-TC-L04's explicitly-noted residual (live credit→balance mobile reflection: admin +25 → mobile 515 → −25 → mobile 490, ledger admin rows 1a546991) + C03/C04 live re-verify with mobile legs. Remaining PARTIALs (G04/M03/O03/O04/P03/X07/Y05/Y08/N2-A01) unchanged w/ stated reasons. C08 approve-pending /listings navigation unreliable this session.
- Method notes: mobile-mcp tools briefly "disabled by user" mid-session → re-invoking activate_* category tools restored them. Mobile screenshot tools disabled → evidence via `xcrun simctl io booted screenshot`. /items/flagged Decision Note = React controlled input (page.fill needed); Reject disabled until note set (guard). /sp-wallet adjust via adj-amount-input/adj-reason-input/adj-submit-btn. Completed-trade deep link → TradeSuccess (tap View Trade Details for the timeline).

## 2026-09-05 — DEV-TASK-113 (owner-approved) closes QA31-M R3 findings #1/#2/#3 + UX items — QA-facing updates to expectations
- **FINDING #1 FIXED (raw-code leak):** shared `getFriendlyCancellationReason(reason, role, refund?)` in `src/utils/tradeCancellationCopy.ts` now backs BOTH `TradeTimelineScreen` (status-banner sub-line) and `TradeDetailScreen` (cancelled infoBox). NO raw `cancellation_reason` code ever renders; unknown codes → generic "This trade was cancelled." (sub-line dropped). Buyer `dispute_resolved_refund` copy branches on refund rows: captured+refund rows → "…your payment was refunded."; no rows (uncaptured, e.g. I04 fixture) → "No payment was taken." Seller → "…by our support team." Item 5 buyer closing-summary card (`timeline-dispute-closed-summary`) shows only on the uncaptured case (captured already covered by O07 refund card).
- **FINDING #2 FIXED (trade timeline stale-after-backend-change):** root cause was `trades` MISSING from the `supabase_realtime` publication — the timeline/detail live channels silently no-opped. Migration `20260905000002_dev_task_113_enable_trades_realtime` (APPLIED to staging, verified `pg_publication_tables`) + AppState-`active` refetch added to the timeline; pull-to-refresh + refetch-on-focus already existed. **QA: an open/refocused Trade Timeline now reflects an admin dispute-resolve/force-cancel WITHOUT relaunch** — no longer a known-stale screen (playbook §5.9 + R59 updated). Public Profile (after review hide/keep), seller listing states, DB-derived counts remain stale-until-fresh-fetch.
- **FINDING #3 FIXED (seller fee display):** seller timeline fee no longer re-derives from current `admin_config`; when the stored trade fee is 0/missing it reads `seller_payouts.platform_fee_cents` for the trade (seller-only). Fixture/synthetic trades with a real payout row now show the stored fee (I03 fixture case: payout $0 → display -$0.00, not config-derived -$3.60).
- **UX items:** item 7 countdown pill now "Auto-completes in 48h" (no "left") via `formatCountdownLabel(model, { omitSuffix: true })`. Item 6 admin headers (`/users`, `/reviews`, `/payments`, `/trades` bundles) get the "(N on this page) of M" window disclosure (DT113 phase — verify on-device/admin). Item 4 = first-trade free-persona fixture `qa:r41-first-trade` (Phase-1 code; staging run = separate owner-approved session).

## 2026-09-05 — QA Task 31-M Round 3 (F-group/dispute execution + C12/L07 re-verify + Q-group clarification) — 15 PASS / 0 FAIL / 0 BLOCKED
- Report/ledger: `e2e-test-results/qa-task31m-r3-adm-fg-dispute-q-2026-09-05/`. Tracker ADM: PASS 130→132, PARTIAL 25→23, Remaining 3 (unchanged), OPEN 1 (L02). PARTIAL pool shrank by exactly 2 (C12+L07→PASS).
- **DT112 fixes all verified LIVE on-device:** (1) C12 — `admin_search_listings_v2` back to 7-arg (p_category/p_seller_email restored) → /listings Category Toys header "of 1077 matching" = DB 1077 EXACT (was 9 vs 1078); seller test-seller@ → 273 = DB EXACT (was 20 vs 274); header discloses "20 on this page". (2) L07 — TradeOfferScreen `sp-amount-input` GENUINELY ABSENT when wallet frozen (was enabled "Max 12 SP") + ⚠️ Frozen banner + inline notice; canSpendSPNow gates on wallet_state active|grace. (3) hasFlag double-dash bug fixed → `qa:r41-in-progress-trade --with-auto-complete` sets auto_complete_at. (4) r41-dispute reset handles under_review (QA30 stranded residue class resolved). (5) QA31T orphan item 0253b2cb gone.
- **F-group changed-value enforcement (F03/F05/F06/F08) driven against LIVE in-progress trade:** pickup_window_hours 72→48 → fixture trade auto_complete_at = now+48h (DB) → mobile timeline "Confirm pickup — auto-completes in 48h left". buyer_fee_active_member_cents 149→199 → Make Offer fee $1.49→$1.99 (fn_get_buyer_fee_for_checkout, live). F06 guardrail HARD-BLOCK with live fixture (168h attempt → no write). F08 first-trade-tier leg STILL fixture-gated (needs genuinely-first-trade free persona — R41 session).
- **Dispute mobile reflection (I03/I04/X06):** both parties' timelines reflect resolution on fresh fetch; timeline does NOT live-refresh (stale until relaunch — add to known-stale list). **[P1 — OWNER-CONFIRMED 2026-09-05, no UX fix exists — source-proven]** cancelled-trade reason leaks raw machine code to users: `TradeTimelineScreen.tsx` L1077–1082 + `TradeDetailScreen.tsx` L559–560 render `Reason: {trade.cancellation_reason}` verbatim; NO friendly map anywhere (only ReviewOfferScreen special-cases "Offer expired"). Code family: `dispute_resolved_refund`/`dispute_resolved_refund_uncaptured`, `changed_mind`/`found_elsewhere`/`other`, `requested_by_customer`, `payment_hold_failed`, `authorization_expired`, `seller_declined`, `offer_expired_competing`, `extension_denied`. Fix = shared `getFriendlyCancellationReason(reason, role)` used by BOTH timeline screens (buyer refunded-copy + seller outcome + "This trade was cancelled." fallback). Bundled in report.md finding #1 + What-Needs-Fixing #1 + UX-enhancement #1.
- **Q-group clarification:** only Q02 (Hide) + Q03 (Keep) got genuine commit+mobile execution in QA31-T Batch 0 item 4. Q01/Q04/Q05/Q06 (QA29 PASSes were empty-queue UI-only) now genuinely executed this round against 3 live reported reviews (reason/status filters, sort reorder, search hit+no-match) + mobile display reflection (profile 8 reviews → 7 after hide → restored). All six Q-IDs now genuinely closed.
- **LOW observation:** seller timeline fee display re-derives from current config and diverges from the real seller_payouts row on synthetic non-fee-bearing fixture trades (fixture stores 0 fee) — dev sanity check recommended.
- Method notes: in-app confirm modal then native window.confirm (dispute resolve, review hide/keep) → override window.confirm + click in-app Confirm in ONE batched script (deferred split cost 2 retries). run_playwright_code sandbox: no require/dynamic-import (page.screenshot to existing dir works). /reviews filter selects use coded option values (spam/false_info/pending_review/newest).

- **RULES ADDED 2026-09-05 (owner-directed):** playbook **§5.61 R58–R60** + `.github/agents/QA-Test-Agent.agent.md` §4 dated pointer. R58 = every user-facing message surface audited for machine/system content (raw codes, DB/EF strings, untranslated enums) on status sub-lines/reason lines/banners/toasts — any non-user-friendly message MUST be reported in the handoff (Critical Findings + Design & Copy DEVIATION + concrete rewrite in What-Needs-Fixing); enumerate the writer code-family via grep for a systemic fix. R59 = backend/admin-side change to a screen's content ⇒ open screen is STALE until fresh fetch (never assert/report contradiction from stale state; force remount/relaunch); Trade Timeline + Profile are known-stale-after-backend-change; flag no-live-refresh as a product note. R60 = dialog chain (in-app confirm modal → native window.confirm) handled in ONE batched script (window.confirm override + click in-app Confirm + accept native) — never split across deferred scripts. Recommend committing these tracked-rule edits same-turn (2026-08-18 lesson) — QA reviews/pushes manually.


## 2026-09-04 — ADM QA Standing Rules v3 file created at repo root `ADM-QA-Standing-Rules-v3.md` (closes the QA Task 30/31 "referenced v2 file does not exist" flags)
- Owner ratified v3 = single source of truth for ADM-R1–R6; every future ADM QA task brief references the rules by name — resolve full text from that file, never inference. Playbook pointer added §5.58 (extends the R55 §5.57 operating detail).
- ADM-R1–R4 unchanged from v2 (full E2E UI→DB→backend; mobile-impacting config cross-surface E2E forward-looking; `prompt()`-reason-entry B03/B06/B07 BLOCKED-on-tooling; single-field-edit discipline on settings forms). ADM-R5 = Mobile-Impact E2E mandatory for a PASS verdict (same-session mobile leg or PARTIAL "mobile leg owed") — ADM-task-facing form of R55/§5.57. ADM-R6 = native dialog technique: override window.confirm/window.prompt via page.evaluate BEFORE triggering so the app's UI handler runs the real mutation (category-delete proven, QA Task 31) — default for native-dialog-gated admin actions, except ADM-R3 reason prompts.


## 2026-09-04 — QA Task 30 (ADM moderation + MSG G05/G08/G09 + Y-group) + STANDING RULE R54 (admin-displayed numbers must be DB-reconciled)
- Run: `e2e-test-results/qa-task30-adm-moderation-msg-y-2026-09-04/report.md` + `ledger.md`. C03–C10 8/8 PASS + X05/X06 PASS live on disposable fixtures (all force-deleted post-run); X07 PARTIAL fixture-gated (0 failed payouts). Y01–Y12 (10 PASS / Y05+Y08 PARTIAL). MSG G05/G08/G09 closed → MSG never-run pool = 0. DT106 spot-checks hold (F06 batch atomic save; K02 data; Z05). B04 DOC-DRIFT (SP adjust on /sp-wallet). Tracker: ADM PASS 94/PARTIAL 11/OPEN 7/DRIFT 1/SKIP 1/Rem 46; MSG PASS 64/PARTIAL 4/Rem 0.
- FINDINGS: (1) MED /config SMS-stats API 401 (BP-49 header class, page.tsx:993); (2) LOW Y08 listings palette nav lands empty (search RPC doesn't match item UUID); (3) LOW C05 /items/[id] no appeal data + load hang; (4) LOW r41-dispute reset won't reset under_review (X06 residue on fe3924ee).
- RESIDUE: trade fe3924ee left dispute_status=under_review (r41-dispute reset gap) — dev reset or accept as fixture. Config all reverted DB-verified; disposables deleted; test-buyer cart/offers reset 0.
- **STANDING RULE R54 (owner-mandated 2026-09-04):** any admin page that DISPLAYS a number/count/stat/aggregate must be DB-reconciled via read-only query (global figures = full-table count; window-scoped figures verified + flagged if label doesn't disclose window). Wired into playbook §5.56. Post-run reconciliation proved every Action Center figure exact (badge 28 = 2+1+24+1+0+0+0) and exposed the /payouts window-scoped stat cards. Recommend committing the playbook edit (2026-08-18 lesson) — QA reviews/pushes manually.

## 2026-09-04 — QA Task 28 — Mobile-toolset closure round (SUB D01 + MSG D02/D04/D05/D10/I01/I02) — 3 PASS / 2 PARTIAL / 1 BLOCKED-on-device / 1 NOT SUPPORTED
- Report `e2e-test-results/qa-task28-mobile-closure-sub-msg-2026-09-04/report.md` + `ledger.md` + full dated file `/memories/repo/qa-task28-2026-09-04.md`. Mobile toolset + SQL both enabled (last round's blocker cleared).
- **NEW HIGH app defect:** NotificationSetup "Enable Notifications" CTA occluded by the floating PersistentTabBar (y868-905 in the pill band; TAB_BAR_HIDDEN_ROUTES has only ItemCreate). Fix: add NotificationSetup to TAB_BAR_HIDDEN_ROUTES. Blocks MSG I01 interaction + I02 on-device; real Settings→Enable-Push flow breaker.
- **SUB D01 PASS** (grace banner on Manage Kids Club+, test-grace; deadline framing vs guide copy diff). **MSG D05 PASS, D04 PASS** (pending guard = Pending-state screen; guide "Pending Request" alert is dead code — doc-drift), **D02 PARTIAL** (sim no-camera graceful error; capture device-gated), **D10 NOT SUPPORTED confirmed** (no id_verification category; enum + prefs verified), **I01 PARTIAL** (prompt renders; success device-gated), **I02 BLOCKED-on-device** by the CTA defect.
- Tracker: MSG 63/2/2/2/0 + 3 remaining (G05/G08/G09 config-gated); SUB 45/2/3/0/0 + 50 remaining. Both guides clear of tooling-queued items → move to ADM next.
- Tooling: stale orphaned `mobilecli screenrecord` (QA Task 26 leftover) wedged WDA (SpringBoard AX reads + WDA timeouts); kill + fresh launch fixed it — add orphaned-mobilecli scan to §5.41 R29 heuristics. Fixture: test-seller now ID-pending (28846a18).


## 2026-09-04 — STANDING RULE R53 (owner-mandated): the COMPLETE formal QA Session Handoff is the MANDATORY final-chat deliverable of EVERY QA run
- **Owner standing request (2026-09-04):** "for future requests I want the QA test agent to send the formal handoff with all the points in it. This is a mandatory request." Prompted by QA Task 26's final reply, which delivered a rich prose summary but omitted most §8.3 fields.
- **R53 — Every QA run MUST close with the full §8.3 formal QA Session Handoff block — every field label verbatim, every field populated (explicit "None"/"not applicable" phrasing where empty) — emitted verbatim as the closing block of the agent's FINAL chat-visible reply.** No condensed summary, no "see report.md" pointer, no partial field set, no file-only emission. Applies per-case (§8.1), per-batch (§8.2), and at every session close. A run is not complete until the complete block appears in the final chat message.
- Full field set (never merge/omit): Test Scope · Design-System Compliance · Perceived Load-Time Verdict · Design & Copy Compliance Confirmation · Verdict Summary · Coverage Tracker Updated · Critical Findings · App State Left Behind · Why It Matters · How to Verify/Reproduce · Known Gaps / Not Tested · What Needs To Be Fixed Next · UX Enhancement Ideas (optional, not defects) · Suggested Next Session · Suggested to Improve Agent Rules.
- **Wired (2026-09-04):** playbook §5.55 R53 added + §8.4 hardened (no condensed substitute); `.github/agents/QA-Test-Agent.agent.md` §4 dated pointer + §5 step 6 (mandatory final emission). Recommend committing these tracked-rule edits in the same turn they are applied (2026-08-18 playbook-loss lesson) — QA reviews/pushes manually.

## 2026-09-04 — QA Task 26 — Independent re-run MSG G-close + DEV-TASK-101 spot-checks — 8 PASS / 0 FAIL / 0 BLOCKED — report `e2e-test-results/qa-task26-msg-g-closing-2026-09-03/report.md` + `ledger.md`
- **MSG G01–G04, G06, G07 ALL PASS for real** (were 7 BLOCKED in QA Task 25). This **closes Dev Task 101 Item 1** (seller Safety Review unreachable — owner-scoped fetch fix live) and the last major unresolved block in MSG's guide. Full per-case detail + fixtures in the standalone dated file `/memories/repo/qa-task26-2026-09-04.md`.
- **Spot-checks also PASS:** MSG E04 (admin details page d148ee0f → Approved, holds on reload), SUB E03 (FAILED row error_message caption renders on Billing History), Item 2 (centered error-state layout), Item 7 (rejected ID-upload amber note renders), Item 6a (0 dangling `screenshot_path` of 76 rows).
- **Fixture states after run (test-seller):** `ccf97ae4` → flagged (G02 appeal, expected); `afd3384a` → pending (G03 resubmit, expected); **`ce322cd9` → deleted (G04 remove, terminal)**. Intact for re-runs: `ba6345ce` (flagged), `e2096de2` (rejected-backdated). Config baselines untouched (`moderation_appeal_max_attempts=3`, `moderation_appeal_window_days=14`, read-only verified).
- **G04 doc-drift note:** Remove Listing renders ONLY for `status='rejected'`; pure-`flagged` shows Edit+Back — guide's "Remove a flagged listing" wording maps to the reject surface.
- **Tool fact:** mobile-mcp `open_url` rejects non-http schemes → all QA deep links route through `xcrun simctl openurl booted`. App dropped to Landing 2× mid-run (no crash) — `qa-login-as?persona=` restores deterministically (R-NEW-1).
- **Item 5 NOT run** (explicitly out of scope — Dev Task 102 diagnostic pending).
- Tracker updated (MSG 54 PASS/5 PARTIAL/1 OPEN/12 NR; SUB 31 PASS/5 PARTIAL/6 OPEN/58 NR). Recommend committing tracked doc/playbook/memory edits per the 2026-08-18 lesson (QA reviews/pushes manually).

## 2026-09-03 — QA Task 25 consolidated round (MSG unblocked + SUB fixtures + DT97 visual) — report `e2e-test-results/qa-task25-consolidated-2026-09-03/report.md` + `ledger.md`
- **Batch 1 (MSG ID-verification, auth-unblocked) — E02/E03/E05/D07/D08 PASS, E04 PARTIAL, R04 covered.** The admin ID-badge auth fix works live (approve `d148ee0f` + reject `76592772` via the real admin UI; notifications + storage-object deletion verified). test-buyer (`34254b02`) + test-seller-3 (`d148ee0f`) now ID-approved; test-seller (`76592772`) ID-rejected (resubmittable) — standing-persona identity states added.
- **KEY HIGH APP BUG — MSG seller Safety Review unreachable:** `ListingSafetyReviewScreen` loads via `getListingById`, which **returns null for any `status !== 'available'` item** (listing.ts direct L1249-54 + fallback L1408). Every flagged/rejected/needs-edits listing (incl. the 5 R41 fixtures on test-seller) renders "Unable to open safety review / Listing not found". MyListings routes to the same broken screen. Blocks MSG G01-G04/G06/G07. Deep link `p2pkidsmarketplace://listing-safety/<listing_id>` IS registered but hits the same wall.
- **UX finding (user-flagged 2026-09-03) — Safety Review error state misaligned:** the error branch renders 3 bare children under ScreenLayout with no centering container (title/subtitle top-pinned y≈127/159, title left-aligned while subtitle centered, full-bleed Back button, Tailwind grays `#111827`/`#6B7280` not canonical). Fix: centered flex wrapper + centered text + max-width Back pill + canonical tokens.
- **KEY MED BUG — admin ID-badge `/details` shows "Pending" for a just-approved request** (`d148ee0f`), while DB + queue say Approved; older approved `34254b02` renders correctly. Persists after cache-bust.
- **KEY MED GAP — SUB TransactionHistoryScreen never renders `billing_history.error_message`**: red FAILED badge shows but no reason under a failed charge (SUB E03 FAIL-finding).
- **MED — public-profile Verified self-view staleness:** test-seller-3's own public profile showed Not-Verified ~1-2 min post-approval while other-view + test-buyer self-view showed Verified.
- **DT97 visual pass:** 5-3 PASS (pending Back-to-Profile = secondary-outline white+green-outline+green), 5-4 PASS (referral rows dim `rowDimmed opacity 0.5` when paused; two-state on-device verified; config reverted `referral_program_enabled=true`), 5-1 + 5-2 source-confirmed (Fix it `primary:true`; badgeShowcaseRefresh bump on celebration close).
- **SUB fixtures live-verified:** test-expired → D03 Subscription Expired screen now shows dated copy "ended on July 25, 2026" (DT100 expired-date fix live — resolves R41 flagged gap); C09 Manage Kids Club+ expired info box + Re-subscribe CTA (reachable ONLY via `manage-kids-club` deep link — the expired gate blocks Profile nav). E04 SUB Subscription Status PASS via `sub_status` fixture notification tap. **L02 PASS** (cross-ref DT99 independent QA grace/freeze leg). **D06/D07 GATED** — no `notif-sub-event` rows staged on test-buyer (only E04 sub_status); staging is a dev-run service-role step.
- **H05/H06 PASS** on the R41 reported dispute (trade `943097a5` → under_review → resolve-complete → `completed`/`resolved_seller`). Fixture consumed.
- **Efficiency fact (recurring):** the Profile ScrollView is flingy (~950pt/swipe regardless of requested distance) → use DEEP LINKS for profile sub-screens (`billing-history` → TransactionHistory, `id-verification-upload` → ID screen, `manage-kids-club`, `referrals`, `listing-safety/:id` all registered + work) instead of swipe-and-relist. The E04 admin "View" click can mis-hit (embed-browser) — navigate to the details URL directly.
- **Playbook addition (2026-09-03 apply-handoff pass):** the QA Task 25 "Suggested to improve agent rules" (Profile flingy-scroll → deep-link-first) was added as **§5.53 R51** to `.github/instructions/QA-Test-Agent.instructions.md` (extends R42/R-NEW-2 with the Profile-scroll trigger + verified deep-link map). Recommend committing these tracked-rule edits in the same turn they are applied (2026-08-18 playbook-loss lesson) — QA reviews/pushes manually per convention.
- **Doc-drift:** E05 confirmation copy is "✓ Saved successfully" (guide "Message saved"); Transaction History shows FAILED in caps (guide "Failed").

## 2026-09-03 — MSG first-live round → QA Test Agent Efficiency Playbook v1 — playbook §5.51 (R42–R49) + §5.51b instrumentation note added (zero dev cost)
- Added §5.51 "Standing rules — MSG first-live round efficiency (QA Test Agent Efficiency Playbook v1, 2026-09-03) — R42–R49 + forward-looking instrumentation" to `.github/instructions/QA-Test-Agent.instructions.md` (after §5.50, before §6), plus unnumbered §5.51b. Evidence source: `e2e-test-results/qa-msg-first-live-2026-09-03/decision-outcome-log.md` (+ report.md; 23 PASS / 0 FAIL / 1 BLOCKED (B05 screen-render, fixture-gated) / 1 NOT-SUPPORTED (J05); ~235–250 calls, ~30–40% projected reducible). Pure playbook/memory discipline — no dev work. Intro follows the §5.47/§5.48 pattern: where a rule restates an existing section (R19/§5.26, §5.33, §5.47b dedupe fact, §6.1 two-source, R-NEW-2), the earlier section stays canonical + this adds the sharper trigger/evidence. Applies from the very next QA session.
- R42 — Deep-link-first, verify `AppNavigator.linking.config.screens` BEFORE firing (extends R-NEW-2/§5.25; B05 `/leaderboard` ~9-call dead-end → 30s grep; `/trade/<id>` + `/seller-profile/<userId>` ARE registered/working on this build; `/leaderboard` is not — only reachable via a `leaderboard_rank_up` notification, none exists → render leg BLOCKED).
- R43 — DB dedupe/pre-condition check before shared-data mutations (extends §5.47b dedupe fact + R-NEW-6; C06 `review_reports` pre-check picked clean target `8b65662d` in 1 call, killed an "already reported" dead-end).
- R44 — On pinned-header scrollables, tap only mid-viewport controls (3rd occlusion source after R22 viewport / R31 tab-bar; P2/P3 mis-tap on `toggle-trades-push` at AX-logical y94 hit the fixed header bell → opened Notification Center).
- R45 — Fully AX-exposed screen → ONE full tree parse (Group J + Referrals were the cheapest phases; full tree lives in the session-resource file when inline output truncates).
- R46 — Source-read the exact visual rule before pixel work (B01/B02 earned `#FFF9EC` vs locked `#F7F7F7` — one targeted badge-scan; A08 `QuickReplyChips` `INITIAL_VISIBLE=3`).
- R47 — Two-source corroboration for unobservable UI states / DB-gated renders (extends §6.1 Phase-23; B03 strip via source+OCR, B05 via zero `leaderboard_rank_up` rows).
- R48 — Reuse incidental navigation states for whole-group execution (extends §5.33; P2 Notification Center → I03–I05 ran in-place).
- R49 — Batch same-persona cases before switching (quantified login tax ~3 calls/switch; reaffirms R19/§5.26).
- §5.51b — Forward-looking instrumentation (NOT behavioral rules, queued as a separate dev task): per-tile AX labels/testIDs on image grids + badge-modal Close testID, `qa:ax-tree` header-band occlusion flag, `qa:scroll-to` helper, AX-visible `keyboard-done-button` on multiline inputs, register missing linking paths (Leaderboard/Badges/SpWallet/Referrals/Chat/TradeList) + `/submit-review?tradeId=`, `leaderboard_rank_up` fixture / `qa-badge-award`, schema-cheat-sheet MSG table update. Once they land, update the affected R42–R49 rules to the new tooling rather than keeping manual workarounds as the only documented path.
- Recommend committing these tracked-rule edits in the same turn they are applied (2026-08-18 playbook-loss lesson) — QA reviews/pushes manually per convention.


## 2026-09-02 — QA Task 21 (v2) — Full Real Subscription Lifecycle E2E + SUB closure — Section A PASS end-to-end
- Report: `e2e-test-results/qa-task21-sub-close-2026-09-02/report.md`. **THE subscription money path is now verified end-to-end for the first time on staging** (real Stripe Checkout → webhook → DB → mobile → cancel → test-clock renewal), via disposable UI user qa.alice.17883856626511437 (041e491f, since cleaned up).
- **NEW ENV BLOCKER FOUND + FIXED (user-authorized):** `stripe-webhook-subscriptions` endpoint `we_1T2k3z4I6kCJlvXob2DFTJI9` was subscribed to ONLY customer.subscription.updated/deleted + invoice.payment_failed/succeeded — **missing checkout.session.completed + customer.subscription.created** (the 2 events that bootstrap the subscriptions row). First real checkout succeeded on Stripe but wrote NO DB row (2 webhook 200s = invoice.payment_succeeded + sub.updated, both no-op on missing row). Fix = add the 2 events (now all 6 subscribed). **Do NOT re-report this as a blocker.**
- Verified: real Checkout (4242, $5.99) → subscriptions row active (period 09-02→10-02) + subscription_events web_subscription_upsert (source web_first_subscription_r7 — first ever) + welcome push. Free→active on relaunch (no manual refresh). Cancel: retention → Stripe cancel_at_period_end=true → DB canceled → Manage "Cancelled / Access Until Oct 2 / auto-renew off"; re-enable toggle → active. Renewal: **Stripe CANNOT attach a test clock to an existing Checkout sub** (`parameter_unknown` on test_clock update) → created clock-bound sub (sub_1UBMHq4) metadata-bound to the user; webhook re-bound the single DB row by user_id; +35d clock advance → renewal invoice in_1UBMIB4 paid → current_period_end advanced 10-02→11-02 + billing_history row (599 SUCCEEDED). **DT-89 fix proven on a brand-new sub.** Strict cleanup: 0 Stripe customers/subs/clocks + 0 auth/profiles/rows for the user.
- **Fee config now = transaction_fee_subscriber_cents 149 ($1.49)** — QA Task 20 F-2 (copy $1.49 vs config $1.00) is RESOLVED (config changed to 149).
- Section B decline PASS (4000...0002 → clear "Your card was declined. Please try a different card.", no row, no state). **F-B1: EF `create-checkout-session` 500s "Key length is zero" for emails with no profile** — staging lacks SUBSCRIPTION_BIND_TOKEN_SECRET (bind-token fallback broken; dev/ops set it).
- Section C PASS: TradeSuccess free-buyer + free-seller upsell = "Join Kids Club+" (no trial copy, on-device); TradeOffer CTA source-verified. Price floor: min_listing_price 0→5 → 1243 available unchanged, 236 sub-$5 stayed available (DT-86 forward-only), reverted.
- Section F: L01 + L04 → PASS (real events); L02/L03 stay PARTIAL (failing-renewal fixture + negative-sig probe not driven); L05 payout domain. SUB Group L guide intro is stale (claims money leg blocked) — doc-drift flag.
- Doc-drift (carried): MODULE-15.1.2 + TRADING-FLOW-V2 still quote old trial-upsell wording on TradeSuccess/TradeOffer.
- SUB remaining never-started ledger + next-session batch in report.md §D/§E.
- **DESIGN-SYSTEM DEVIATION (user-confirmed 2026-09-02): Manage Kids Club+ screen is off-brand.** Source-confirmed in ManageKidsClubScreen.tsx + components/subscription/{PaymentMethodSection,AutoRenewToggle,BillingHistoryLink}.tsx: primary "Add Payment Method" button = `#0066CC` system blue (not #5DBB8E pill); status badges use Material palette (Active `#4CAF50`, Grace `#E53935`, Trial `#29B6F6`, Cancelled `#FFA726`) not the Pass It Up semantic tokens (#5DBB8E success / #E85D75 error / #FFA726 warning / #5B8FB9 info); Auto-Renew Switch ON = iOS blue (#93C5FD/#0066CC) not green; warning text `#D97706` (Tailwind) ≠ #FFA726; text grays Tailwind #111827/#6B7280 ≠ canonical #1A1A1A/#6B6B6B. Aligned on same screen: infoBox #EBF4F9+#5B8FB9, warningBox #FFF3E0+#FFA726. Recommend dev remap to doc tokens (separate task).

## 2026-09-01 — QA Task 17 friction-analysis learnings (decision-outcome log) — 3 USER-FLAGGED fix classes + validated patterns
- Full decision-and-outcome log: `e2e-test-results/qa-task17-z-g-dt78-81-2026-09-01/decision-outcome-log.md` (19 PASS/2 FAIL; ~330–400 calls). Ranked friction + concrete fixes there.
- **FIX-1 (bottom-anchor AX unreliable — the "disclaimer opened again" class):** AX tree has no z-order; the floating tab pill overlaps bottom-anchored CTAs, so tree coords mislead taps. Rule: NEVER tap a bottom-anchored button from tree coords — swipe it into the visible band (y 100–800), re-list, OCR the band to confirm label, then tap. Instrumentation ask: `qa:scroll-to?testID=` deep link (returns fresh viewport coords in 1 call) or `qa:ax-tree --pill-top/--pill-bottom` check before tapping.
- **FIX-2 (native modals blank the AX tree — the "Accept All" class):** native "Accept All Offers?" / "Offer Accepted!" / "Request Withdrawn" alerts make `list_elements_on_screen` return an empty tree → OCR + coordinate-guess (~4–6 calls × ~6 occurrences). Dev ask: route these through the branded `GlobalAlertProvider` (AX-exposed `global-alert-button-N`) like DT51 did for accept/decline confirms — then each accept is 2 AX calls (like the already-AX-exposed bundle path `btn-accept-all-confirm`). Interim rule: cache per-device modal anchors (primary ≈ (220,838), cancel ≈ (220,887)) in memory; OCR once to confirm, tap, DB-verify.
- **FIX-3 (server-side-created offers "not in the tree" — stale Needs Action list):** after `ef-repro`/server writes the seller list only refetches on focus/remount. Rule: treat the FIRST list after any server-side write as stale — go straight to nav-away-and-back (or pull-to-refresh), then list (skip the "list→nothing→investigate" step). Dev ask: `qa:refresh` deep link + `ef-repro --notify` flag.
- Validated this run: timeline flingy-scroll gesture = swipe up 700 @y250 → swipe down 140 @(100,850) reveals mid-section approve/decline (reused ~6×); admin sidebar intercepts `locator.click` → use JS `document.querySelector('[data-testid=…]').click()` in ONE batched `run_playwright_code` block; multi-statement `execute_sql` returns only LAST result → one statement per call; deep links queued behind pending native alerts → dismiss alert, then re-fire; `qa-trade-success` H02 needs `spUsed>0` to hit "Got it! You saved…" (Perm-2).
- Defects found (dev, separate follow-ups): Z05 whole-bundle seller-approve cascade leaves sibling Stripe/SP holds unreleased (`respondToCancelRequest('approve')` cancels one trade only) — HIGH money-state; D2 `PaymentMethodSection` never calls attach EF; Z03 timeout cron never notifies buyer; admin Action Center Cancel Requests "Review" is a no-op false toast.

## 2026-09-01 — DEV-TASK-79 — QA Task 16 friction fixes (FIX-AX / FIX-DISABLED / FIX-PARAMS / FIX-ADMIN-2CALL) — app-side instrumentation landed + one tool-schema fact
- **FIX-AX:** "Includes points redemption" tag is now its own accessible element on BOTH bundle-card variants in `TradeListScreen.tsx` — `testID="includes-points-redemption-tag"` + `accessible` + `accessibilityLabel` (BP-53). Verified on-device: buyer "Your Offers" + seller "Needs Action" bundle cards both expose the identifier in the AX tree.
- **FIX-DISABLED (KEY TOOL FACT):** `disclaimer-modal-accept-button` already had `accessibilityState={{ disabled: !accepted }}` since TASK SAFETY-008 (unit-tested) — the QA Task 16 "disabled not exposed" observation was a **mobile-mcp tool-schema limitation**: `list_elements_on_screen` only surfaces `{type,label,name,identifier,coordinates}` and does NOT surface `accessibilityState.disabled` OR `accessibilityHint` on any element. So no app-side change can make disabled appear in that tool's tree; DT79 added the `accessibilityHint` (VoiceOver-visible) and playbook R-16-4 now says: re-list → relaunch once (R-NEW-1) → fall back to ONE functional tap. Don't re-investigate "why isn't disabled in the AX tree".
- **FIX-PARAMS:** `qa-trade-success` params were already fully documented by DT78 (commit 4667101f) — no change.
- **FIX-ADMIN-2CALL:** `run_playwright_code` deferred-result 2-call pattern is a TOOL CONTRACT (mutating scripts return a `deferredResultId`; second call resumes it). Documented as expected behavior in playbook §5.49 R-16-6 (batch to pay the tax once per page interaction).
- Playbook edits: §5.49 R-16-6 (new) + R-16-4 updated (FIX-DISABLED landed). Flow registry: DEV-TASK-79 entry (FLOW-08/FLOW-18).

## 2026-09-01 — QA Task 16 friction-analysis standing rules added (playbook §5.48, R-16-1..5) — zero dev cost, apply from the very next QA session
- Added §5.48 "Standing rules — QA Task 16 friction-analysis fixes (2026-09-01) — R-16-1..5" to `.github/instructions/QA-Test-Agent.instructions.md` (after §5.47b codified facts, before §6). Evidence source: `e2e-test-results/qa-task16-close-trd-2026-08-31/decision-outcome-log.md` (18 PASS / 0 FAIL / 0 BLOCKED; ~38 of ~150–170 calls lost to friction — down from QA Task 15's ~90, confirming R-NEW-1..6 + DT76/DT77 instrumentation). Pure playbook/memory discipline — no dev work.
- R-16-1 — Reset offer/cart fixtures FIRST, always: `npm run qa:reset-offer-fixtures` as the very first action of any session touching offers/bundles/carts (this run's `MAX_PENDING_OFFERS` + reset-and-rebuild cost ~13 calls; a 1-call up-front reset avoids it). Strengthens R26.
- R-16-2 — In-progress bundle trades have NO app-side cancel (bundle Trade Timeline exposes only extension/confirm/report; cancel lives only on single-trade TradeDetailScreen). To free such a fixture, go straight to admin "Force Cancel Entire Bundle" — never attempt the app UI (~11 calls saved). Note: `p2pkidsmarketplace://trade/<id>` now works (DT77) but lands on the bundle Timeline (no cancel).
- R-16-3 — Category-level SP caps (`categories.sp_redemption_cap`) are written via the admin portal `/categories` → Edit → SP Config tab only — NOT `qa:admin-config-set` (which covers `admin_config`). Scope-write-then-revert-verify (R28) when touching.
- R-16-4 — For disabled-state assertions not exposed in the AX tree (no `accessibilityState.disabled`), do ONE bounded functional tap (confirm no-op) then move on — never a color-scan loop. Stopgap until FIX-DISABLED lands app-side (BP-53).
- R-16-5 — Read a deep link's actual param names before firing it with crafted values (e.g. `qa-trade-success`'s "You saved $" figure is `spAmountDollars`, not `spUsed`) — one wrong guess costs a wasted round-trip.
- Also wired a dated pointer into `.github/agents/QA-Test-Agent.agent.md` §4. Recommend committing these tracked-rule edits in the same turn they are applied (2026-08-18 playbook-loss lesson) — QA reviews/pushes manually per convention.



## 2026-08-31 — QA Task 15 friction-analysis standing rules added (playbook §5.47/§5.47b, R-NEW-1..6 + codified facts) — zero dev cost, apply from the very next QA session
- Added §5.47 "Standing rules — QA Task 15 friction-analysis fixes (2026-08-31) — R-NEW-1..6" + §5.47b "Codified facts" to `.github/instructions/QA-Test-Agent.instructions.md` (after §5.46 R39–R41, before §6). Evidence source: `e2e-test-results/qa-task15-dt75-w-t-2026-08-31/decision-outcome-log.md` (~90 of ~250–300 calls lost to avoidable friction). Pure playbook/memory discipline — no dev work.
- R-NEW-1 — Relaunch-first on a blind AX tree: 2 consecutive empty/status-bar-only `mobile_list_elements_on_screen` lists → `terminate` + `launch` (2 calls); still blind → BLOCKED with reason. Turns QA Task 15's worst 20-call stuck-"Offer Declined"-alert loop into a 2-call fix. Distinct from §5.4 native-`Alert.alert` OCR-first (alert genuinely up) vs broken-screen-state (R-NEW-1).
- R-NEW-2 — Deep-link-first navigation with a running cached dead-end list: `p2pkidsmarketplace://trade/<id>` confirmed dead (linking config has no TradeDetail/TradeTimeline path; only the notification service parses `/trade/:id`) — don't retry; navigate via UI.
- R-NEW-3 — Schema-consult before SQL: read `/memories/repo/schema-cheat-sheet.md` first; if table/column missing, run ONE `information_schema.columns` query and ADD the result to the cheat-sheet. Reaffirms §5.34/R7 before-the-first-query.
- R-NEW-4 — Seller-ownership check (`items.seller_id`, one SQL) before persona switch when a case depends on a specific seller (Board Game Set = test-seller-2, not test-seller).
- R-NEW-5 — Batch admin-portal actions into ONE `run_playwright_code` block performing clicks + reads + returning a JSON verdict; never one action per call; prefer inline-JSON over huge snapshots + grep.
- R-NEW-6 — Fixture-feasibility check (1–3 DB queries) before a group: non-terminal bundles (`bool_or(status NOT IN ('completed','cancelled','payment_failed'))`), Accept-SP item counts per seller, 3-item same-seller SP bundle. (W09/W10 + T06 fixture gaps = blocked cases this run.)
- Codified facts (no number): (1) confirm only the FIRST coordinate guess with a screenshot, then trust the state change or relaunch; (2) dedupe guards mean "fresh" fixtures may not be fresh — check DB state first, pivot to an alternate fixture (Q15 already-reported review); (3) disclaimer modal requires the "I have read and understand" checkbox before Accept — do the checkbox first (confirms §5.42 R30).
- Also updated `/memories/repo/schema-cheat-sheet.md` with the QA Task 15 verified drift facts (`sp_wallets.reserved_sp`, `subscriptions.has_used_trial`, `cart_items.listing_id`) and added a dated §4 pointer in `.github/agents/QA-Test-Agent.agent.md`.
- Recommend committing these tracked-rule edits in the same turn they are applied (2026-08-18 playbook-loss lesson) — QA reviews/pushes manually per convention.



## 2026-08-31 — QA Task 16 — Close Out TRD: DT76 + W09/W10 + T06 + Section D — 18 PASS / 0 FAIL / 0 BLOCKED
- Report: `e2e-test-results/qa-task16-close-trd-2026-08-31/report.md`. Full run: A1 (Review Offer bundle +61 SP, not +60 — DT76 T08 fixed on-device), A2 (tag both lists), A3 (admin filter reset), A4 (Sports cap=5 → "up to 5 SP" + SP_CAP_EXCEEDED server reject, reverted), W09/W10 (non-terminal fixture `93e84d1c` Force Cancel 2/2 + DB), T06 (3-item SP counter/caps), I06–I09 (disclaimer gate/cancel/✕/reset), H01–H04 (completion CTAs via `qa-trade-success`), K11 (seller fee = effectivePct × cash, 20% observed).
- **KEY R-NEW-2 CACHE UPDATE:** `p2pkidsmarketplace://trade/<id>` NOW WORKS (DT77 added TradeDetail/TradeTimeline to the linking config) — it is NO LONGER a dead end. It lands on the bundle Trade Timeline for in_progress bundle trades (which has NO cancel button); use `/trade/<id>` for navigation, but expect no cancel there.
- **Buyer has NO cancel for in_progress bundle trades** (Trade Timeline exposes only extension/confirm/report) — QA freed Kids Bicycle by admin force-cancel. Product-decision gap (finding 4).
- **Seller fee on staging:** free 10% / KCP(subscriber) 20% (trial=subscriber); guide K11's 5% is stale; `discount_percentage_kids_club_plus` used as flat subscriber rate (20%>free) — config-intent flag.
- **Fixture state left:** test-buyer pending offers = Kids Bicycle bundle `b6b42db4` (2 trades, sp=45) + Soccer Ball `a45caeb5` (I06 accept); force-cancelled `5b69480b` + `93e84d1c`; Sports cap reverted NULL.
- **Efficiency:** R-NEW-1..6 + DT77 instrumentation worked — single blind-AX incident (stale LogBox) resolved in a 2-call relaunch; `qa-set-sp` collapsed SP entry to 1 call; no QA Task 15 friction classes rediscovered.

## 2026-08-31 — STANDING RULE: Admin-dependent test cases are ALWAYS in scope (recurring-violation fix; supersedes any "admin-scope" / "admin-out-of-scope" default)
- **Verbatim standing rule (mandatory — do not skip):** "Admin-dependent test cases are NEVER out of scope by default. Every QA task that touches a guide with admin-portal-dependent cases (tax admin, moderation, config, reporting, etc.) must execute those cases for real against the live admin portal in the same session, using the same real-admin-session pattern already proven in QA Task 12 (portal login, real config/save actions, DB read-back verification). 'Admin-scope' is not a valid reason to skip or defer a case — if a case requires the admin portal, that is the instruction to use the admin portal, not a signal to exclude it. If a genuine blocker prevents admin execution in a given session (e.g., portal unavailable, credentials issue), state that explicitly as a BLOCKED case with a stated reason — never silently label it 'admin-scope' and skip."
- **Why (escalated — same mistake now 3×):** QA Task 11 originally skipped/mis-scoped admin-dependent cases; QA Task 12 was specifically run to correct that (11 admin cases executed for real against the live portal); QA Task 14 then skipped Group W (12 admin cases) again with an "admin-scope" label. This is now a standing rule so it cannot recur a third time.
- **Immediate action — MANDATORY carry-forward (not optional):** Group W (12 cases) from QA Task 14 must be executed for real in the next QA session.
- Wired into the tracked agent/playbook the same day (no code): `.github/agents/QA-Test-Agent.agent.md` §2 Scope (admin web app now IN scope via the shared admin-portal browser session, per the QA Task 12 pattern) + §4 dated pointer; `.github/instructions/QA-Test-Agent.instructions.md` §5.38 R13 ("admin-portal-scope" removed as a valid BLOCKED category). Recommend committing these tracked-rule edits in the same turn they are applied (2026-08-18 playbook-loss lesson) — QA reviews/pushes manually per convention.


## 2026-08-31 — Standing QA rules from the DT68→DT73 / QA11→QA13 arc (copy-change re-verify; explicit per-case scope lists; multi-account/aged-fixture budget)
- **Rule 1 — Re-verify copy-invalidated cases in the SAME or very next session, not "stale for later".** When a dev-side copy/UI change lands, proactively re-run every case whose expected-copy assertion it invalidates rather than only flagging the guide as stale for a future round. This round's model: DT72's SP-limit phrasing + DT73's authorized-vs-charged copy stayed correctly flagged AND got re-verified on-device in QA Task 13 (T01/T03/T05/T06, S-group banner, A1 accept-alert "Payment authorized") — make that the default, not an exception.
- **Rule 2 — Explicit per-case scope list for deferred/owed batches; require the user to account for EVERY case.** When presenting a batch of deferred/owed cases for a user scope decision, accept only an explicit per-case list (in-scope / out-of-scope / deferred-with-reason per TC-ID), never a general "these are deferred" note; flag mismatches back immediately rather than proceeding on an incomplete list. This round: the user's in-scope/out-of-scope lists (4 + 4) did not cover all 9 previously-deferred cases (Q10 was unaccounted for) — a general acceptance note left a case silently unaccounted.
- **Rule 3 — Multi-account / aged-fixture cases need dedicated fixture-building sessions — budget them explicitly.** The Q05/Q15/Q17/Q10-class cases (second-party review on a profile, 3-distinct-reporter auto-hide, aged reviews for the 24h edit window / 30-day cooldown / 24h post-completion lock) consistently require assembling multi-account or time-aged fixtures that do NOT fit opportunistically inside a broader batch. When these are back in scope, plan a dedicated fixture-building session and say so in the run plan — do not fold them into a general batch and mark deferred again.
- Evidence: `e2e-test-results/qa-task12-close-2026-08-30/report.md` (A4 deferred table — the 9 owed cases; P2 copy finding; Books tax-exempt fixture trap) + `e2e-test-results/qa-task13-dt71-dt72-verify-2026-08-31/report.md` (Section A/B copy re-verifies; C1 guide stale-copy flag). Companion Dev-side standing notes added the same day to `/memories/repo/agent-rule-updates-2026-08-31.md`. WIRED into the tracked agents/docs the same day: §5.46 (R39–R41) added to `.github/instructions/QA-Test-Agent.instructions.md` (before §6), a dated pointer added to `.github/agents/QA-Test-Agent.agent.md` §4, and committed as `08cc8e41` (3 files, 17 insertions, NOT pushed — QA reviews/pushes manually).

## 2026-08-30 — DEV-TASK-65 QA Toolkit Round 3 — playbook §5.44 (R37) + §5.45 (R38) added
- Added §5.44 "Standing rules — Legitimate `admin_config` write paths for QA fixtures (2026-08-30) — R37" (after §5.43, before §6): the `qa:admin-config-set` helper (Item A, preferred), admin settings-page UI (Item B), admin portal API `PATCH :3001/api/admin/config` w/ `x-admin-secret` (Item C), and the blocked paths (MCP SQL → P0001 = DT-56a/DT-59 lockdown working). Evidence: `e2e-test-results/dev-task-65-qa-toolkit-r3-2026-08-30/`; script `p2p-kids-marketplace/scripts/qa/admin-config-set.mjs` (npm `qa:admin-config-set`), verified live set `cart_min_value_cents` 0→7777→0 + DB read-back.
- Added §5.45 "Standing rules — Accepted verdict class for VoiceOver swipe-gesture testing (2026-08-30) — R38": class `AX-tree-exposure-verified, VoiceOver-swipe-interaction-not-directly-testable`. 2a investigated and concluded infeasible (mobile-mcp raw-touch-only, no VoiceOver cursor/utterance observability, VoiceOver enable not reliably drivable on iOS 26.1 sim); 2b chosen. §8.1 item 3 verdict list extended with the class name.
- Simulator left clean: accessibility plist restored (VoiceOverTouchEnabled removed), rebooted, app on Home logged-in.

## 2026-08-30 — QA playbook §5.42 efficiency rules added (R30–R32) from QA Task 8
- Added §5.42 "Standing rules — QA Task 8 cumulative-regression efficiency (2026-08-30) — AX-first modal handling, gated checkboxes, tab-bar occlusion (R30–R32)" to `.github/instructions/QA-Test-Agent.instructions.md` (after §5.41 R29, before §6). Evidence source `e2e-test-results/qa-task8-cumulative-regression-2026-08-30/report.md`.
- R30 — Acknowledgment-gated modal fast path: disabled (pastel) primary + "I have read and understand..." checkbox → DON'T pixel-hunt; `disclaimer-modal-checkbox` is in the AX tree at (16,812,408,24) center (220,824); tap row center (NOT box corner ~x28, NOT y840-853 band), assert value="checked", then `disclaimer-modal-accept-button` center (325,878).
- R31 — Never tap a CTA whose y-center ≥ ~840 on tabbed screens (tab-bar band y≈848–904; e.g. `cancel-trade-button` y854 — tapping center (220,878) hit the Sell tab x192-248). Scroll content UP first so the button's reported y-center < ~790, re-list, then tap.
- R32 — CancellationReasonModal: re-list tree once (rows populate on 2nd listing as `cancellation-reason-<id>`); reason rows ~61px tall incl subtitle — tap row center (Changed mind ~y308, center (220,338); y305 is a gap). Confirm `cancel-trade-confirm-button` (224,896)→(324,917) is red #FF6B6B (enabled) before tapping; pastel #FFE7E7 = reason not registered.
- Meta-rule — AX-tree-first for every modal: list + re-list once before any OCR/band-scan/pixel work; tree coords authoritative; OCR only if 2nd listing still empty.
- Why: QA Task 8 spent 10–15 OCR band-scans + pixel color-inspections on these three interactions; each resolves in 3–4 calls with the AX-first path.


## 2026-08-29 — QA standing rule: Simulator/Device Busy Check added (§5.41 R29)
- Added §5.41 "Standing rules — Simulator/Device Busy Check (2026-08-29) — shared-resource exclusivity (R29)" to `.github/instructions/QA-Test-Agent.instructions.md` (after §5.40 R28, before §6). R29: BEFORE any simulator/emulator interaction (or attaching to the shared admin-portal browser session), check the target UDID/browser session for an in-flight agent task (dev or QA) — session marker, `xcrun simctl list devices booted` + `ps` scan for `expo run:ios`/`maestro`/`run-suite.sh`/Playwright, fresh `e2e-test-results/<run>/` writes, session store, live admin `:3001` session. If busy → wait and poll, NEVER interleave (interleaved agents corrupt persona/login state and produce false verdicts). Applies symmetrically (QA vs Dev Task, and vice versa). Instrumentation gap: no first-class "another task is using this simulator" detector yet — proposed lightweight per-UDID lock file (agent id, start time, UDID) under `e2e-test-results/` written on start / removed on finish; fold into the next QA toolkit round. Until it lands, treat "can't rule out another agent on this UDID" as busy.
- Also wired into `.github/agents/QA-Test-Agent.agent.md` §5 Invocation pattern step 3 (busy check runs before confirming simulator/app state).
- Reason: Dev Task 48/51/52 + QA Task work now share the same iOS Simulator instance and staging Supabase project, sometimes back-to-back or overlapping.

## 2026-08-29 — QA playbook Round 2 rules added (commit 9cfe9b96)
- Added §5.39 "Standing rules — TRD Group C/D/E run (2026-08-28) — Round 2" (R17–R22) to `.github/instructions/QA-Test-Agent.instructions.md`, evidence source `e2e-test-results/qa-trd-b-c-d-e-2026-08-28/decision-outcome-log.md`. R17 OCR-first on suspicious AX coords (tab-pill overlap / y>screen height); R18 source-pre-read for reachability/thresholds/discrepancies; R19 batch-by-persona + SQL-seed preconditions; R20 border/fill color scan + strip-OCR band-first; R21 BLOCKED-with-evidence + DB-state substitution continuation; R22 pre-tap viewport/occlusion gate.
- Items 4 (timer RPC re-run on 0) and 7 (Cmd+A default) MERGED in place into existing Round 1 rules R14 and R3 (§5.38) — not duplicated.
- Committed same-turn (prevents the 2026-08-18 playbook-loss lesson). Not pushed.

## 2026-08-26 — B02 re-verify (happy + security-rejection) + B03 real-SMS re-attempt — 2 PASS / 1 BLOCKED / 1 SKIPPED
- Report: `e2e-test-results/account-file-b02-b03-reverify-2026-08-26/report.md`. Device iPhone 17 Pro Max sim (26.1). Persona test-buyer.
- **B02 PASS (both legs) — the two CRITICAL defects from the prior round are FIXED + on-device verified:** (1) happy path: email → `test-buyer+b02reverify@…` → Verify modal → 123456 → **applied** — `auth.users.email`=`profiles.email` in sync (19:42:34Z), row `a861320d` verified_at+used_at set, orphan `5ec4ee58` sealed. The BP-62 verify/apply (unwrapRpcResult of TABLE-returning RPC) works. (2) security rejection: email → `samer.alzubaidi82@gmail.com` → "Updated with Warning / That email is already used by another account." (GlobalAlertProvider) — **NO new email_change_verifications row, NO new email_logs send**; BP-63 guard via `check_account_exists_by_email` works. Email restored to baseline (row `c83e6994` sealed).
- **B03 STILL BLOCKED (ops):** `send-phone-otp` v10 returned **HTTP 500 EDGE_FUNCTION_ERROR** (function_edge_logs 19:47:43Z) + 2 error logs; code row `e5442cb7` stored PRE-send (v10 inserts before Twilio — row ≠ delivered); client dev-bypassed. **No real SMS.** Exact Twilio text NOT retrievable via `mcp_supabase_query_logs` (function_logs message body not exposed in event_message/metadata). Class = prior 21606. One send only. **Twilio config still the unblock.**
- **KEY TECHNIQUE (standing):** the long-press → Select All native menu did NOT appear for email/phone fields this build (3 attempts). **Cmd+A via osascript (hardware-keyboard select-all) + type REPLACES the whole field value reliably** (worked 4/4). Add to playbook.
- **Verify-modal Cancel AX-frame offset:** element is AX-exposed (after Fix 4) at (20,36,50x27) but tap at center (45,49) MISSES; working tap ~pt (40,58). Flag for dev.
- **Moderate app finding (reproduced):** post-verify `navigation.reset` → Profile stuck "Loading profile..." until navigate-away/back (email + phone paths).
- test-buyer left at baseline (email test-buyer@…, phone 5551234002, auth=profile); no seed restore. App logged-in at Profile.

## 2026-08-25 — F02 valid + G07 3-CTA closures (2 cases) — 2 PASS / 0 FAIL / 0 BLOCKED
- Report: `e2e-test-results/account-file-f02-g07-closures-2026-08-25/report.md`. Device iPhone 17 Pro Max sim. Cumulative Account file A–G now 47 cases (39 PASS / 0 FAIL / 8 BLOCKED).
- **F02 valid leg PASS (CLOSED):** the `generate_unsubscribe_token` `gen_random_bytes` search_path defect is FIXED (RPC now `extensions.gen_random_bytes(32)`, verified via pg_get_functiondef). Read the seeded token via **read-only SQL** (`unsubscribe_tokens` for test-buyer where `used_at IS NULL`) instead of re-running the shared-staging seed (smaller blast radius; token is one-time use, redact in report). Deep link → "You've Been Unsubscribed" + `subscription` category + Go to Home; DB: `used_at` set + `email_enabled=false` + `updated_at` bumped. CDP clean.
- **G07 3-CTA leg PASS (CLOSED):** new standing `test-grace@…` persona (id `…011`, pw `TestGrace123!`, status grace + 1 active draft + id-verif none) stacks 3 CTAs → "Show 1 more action" (MAX_VISIBLE=2) → expand → 3 CTAs + "Show less" → collapse → 2 CTAs. All toggle testIDs AX-exposed. On-device verified via tree + OCR.
- **KEY TOOLING DATA POINT (Home ScrollView):** `mobile_swipe_on_screen` on Home moves content ~950pt per swipe REGARDLESS of requested distance (flingy) — overshoots past the target twice; recover with short controlled swipes + tree re-lists (§5.9/§5.2). Not an app defect.
- **F02 token consumed** (one-time use); `test-grace` fixture left intact. Both personas logged out; Landing clean.

## 2026-08-24 — Account File Groups A+B+C+D (23 cases) — 16 PASS / 0 FAIL / 7 BLOCKED
- Report: `e2e-test-results/account-file-groups-abcd-2026-08-24/report.md`. Device iPhone 17 Pro Max sim. Persona test-buyer; D04 used fresh throwaway `qa.alice.17876099769423378@…` (`167abdc4-…`).
- **KEY TECHNIQUE — full AX tree lives in the resource file:** mobile-mcp `list_elements_on_screen` writes the FULL tree (17KB+) to the session-resource file even though the inline tool output truncates at ~2k chars. Extract specific elements with `grep -o '"name":"<id>"[^]]*' <resource-file>` (or `grep -o '"type":"Switch"[^]]*'`). Used to get exact Switch/TextInput coords (Notification toggles, quiet-hours inputs) that were invisible in the truncated inline output.
- **B04 picker WAS drivable this session (contradicts §5.31):** the expo-image-picker photo grid exposed photos as AX Images and the crop/confirm window's Cancel/Choose buttons WERE AX-exposed and tappable → B04 avatar upload ran end-to-end (storage object `user-avatars/avatars/…-1787609347283.jpg` + `profiles.avatar_url` updated). The §5.31 undrivable note is BUILD-DEPENDENT — verify per run (1 bounded attempt).
- **B03/B09 DEFECT CLUSTER (phone_verification_codes):** (1) the `verified` column DOES NOT EXIST → `EditProfileScreen`/`verifyPhoneCode`'s `.eq('verified',true)` queries always error → `alreadyVerified` always false → B09 "already verified" Info alert unreachable; `requestPhoneVerification`'s insert of `verified:false` also errors → dev-bypass fallback always used. (2) After OTP verify (123456), `auth-update-phone` updates `auth.users.phone` but NOT `profiles.phone`/`phone_verified_at` (DB-proven) → profile stale + user still gated phone-unverified. (3) Profile shows "Loading profile…" indefinitely after the phone-verify `navigation.reset`; recovers by navigating away/back. (4) OTP modal Verify button not AX-exposed + frequently swallows first tap (2-tap). Dev recommended fixes in report.
- **A04 HIGH — legal docs are imported 3rd-party boilerplate:** TOS = Google Cloud Marketplace TOS, Privacy = Walmart Seller Privacy Notice, Liability = Amazon commercial-insurance text (all "Last updated 4/1/2026"). Parents would read unrelated companies' policies. Content-owner task.
- **D04 empty-state is dead code:** fresh signup auto-creates all 5 `notification_preferences` rows (DB: created_at = signup time) AND `getNotificationPreferences` self-heals via `initialize_user_preferences` → "No preferences found" + Initialize Settings never show in normal flows. Guide expectation obsolete.
- **In-app dialogs = GlobalAlertProvider (not native Alert.alert)** — re-confirmed on A02/A03/B07/B10/C02 (`global-alert-button-*`). Guide Dependencies labels = doc drift.
- **B05 observation:** test-buyer subscription is in GRACE (status active, grace_ends_at 2026-09-12) but Profile badge shows "Kid's Club Member / Exclusive perks active" — grace-label nuance vs guide taxonomy.
- **test-buyer state left:** phone restored 5551234001; bio → "QA test bio 0824" (was "Rr"), avatar changed (new file), subscription email notification toggle ON — seed restores.


## 2026-08-24 — Auth final-cleanup batch (8 items) — 2 PASS / 5 BLOCKED / 1 DOC-FIXED
- Report: `e2e-test-results/auth-final-cleanup-batch-2026-08-24/report.md`.
- **AUTH-TC-J10 PASS (real first run):** fresh unverified user → ItemCreate (dev-add-test-photo/dev-set-category Books/dev-fill-item $20) → Publish → phone-gate modal blocks → 123456 verify → publish resumes → item `8a8e1dcb` pending; DB: phone_verified_at 21:09:07 → item 21:09:08. User `qa.alice.17876051981934476@kidsmarketplace.test` (id `ae798427-fac2-46b4-b95b-b4d132e683e4`) now phone-verified + pending item (reuse for L01 anchor or cleanup).
- **AUTH-TC-E04 UNBLOCKED + PASS:** deployed `send-phone-otp` v2 rate-limit (3/hr phone, 5/day user) runs BEFORE dev-bypass; client `phoneService` rethrows `OTPRateLimitError` (never dev-bypassed). 4th send → "Too many attempts. Please try again in 3600 seconds." + resend disabled 3598s. Old Phase 22 "RATE_LIMIT_EXCEEDED never surfaces" is STALE. Induction recipe: same phone, 4 sends within an hour (resend countdown 60s between).
- **AUTH-TC-S01 BLOCKED (NEW regression):** staging Supabase Auth SMTP NOT configured — Forgot Password Send → real GoTrue "Reset Email Failed / Error sending recovery email / SMTP/email provider not configured" (qa_reset_error_simulation absent). Was PASS 2026-08-16. Dev must configure staging SMTP + confirm `p2pkidsmarketplace://reset-password` redirect URL.
- **AUTH-TC-Q04 DOC-FIXED:** guide said `fee = 2.5 (10%)` (old % model). `spCalculatorService.calculateSP` buy mode = FLAT fee (BRD $0.99/$2.99; staging admin_config $1.00 sub / $20.00 non-sub). Applied flat-fee expected-result + model note to the guide (2026-08-24).
- **Re-confirmed blockers:** C05 (no qa_provider_unavailable toggle; banner unit-tested only), C07 (NO social-only/password-less user on staging — DB query empty; SetPasswordModal/LinkedAccounts exist; C04 fixture made kidsp2p@gmail.com password-capable), H03 (qa_avatar_upload_failure still `none`, disarmed 2026-08-18 17:06 — dev ops action to re-arm), P03 (test-buyer has ZERO messages; seed creates none).
- **Design deviation:** phone-gate modal OTP = 6 separate digit boxes vs design doc §4.4 single auto-formatted field (standalone PhoneVerificationScreen uses single `otp-input` — inconsistent). Moderate finding.
- **Dev LogBox:** "Text strings must be rendered within a <Text> component" warning observed during phone-gate flow (source not chased; recommend dev sweep).

## 2026-08-24 — Group J+H closure run — 3 PASS (J05, photo_urls sync, H01 avatar)
- Report: `e2e-test-results/group-j-h-closure-2026-08-24/report.md`. **Group J 14/14 and Group H 7/7 now fully closed.**
- **Check 1 (AUTH-TC-J05) PASS:** `dev-set-other-category` prefills "Board Games"; clearing (long-press → Select All → **Cut**) → submit gray `#C8C8C8` (blocked); re-fill → green `#5DBB8E` (both color-histogram-proven). NOTE: `type_keys("\b")` writes literal backslash-b, NOT a backspace — field-clear must use the native text menu (Cut).
- **Check 2 (post-migration photo_urls sync) PASS:** migration `20260824190740_fix_merge_item_draft_sync_photo_urls` applied + tracked; `merge_item_draft` body has the photo_urls column refresh (pg_get_functiondef); baseline 0 stale rows / 46 in sync (backfill OK). On-device: 3× `dev-add-test-photo-uploaded` → draft `667898b8` → reorder via `move-photo-right-…` → back-nav flush → DB `photo_urls` == `draft_data->'photo_urls'` = [1093661, 1175644, 1129292], `in_sync=true`.
- **Check 3 (AUTH-TC-H01 avatar) PASS:** `dev-set-avatar` (cache-URI fix f0417f5a) → avatar preview rendered (histogram), NO "Profile will be created without avatar" warning ("Success / Your profile has been created!" dialog), DB `avatar_url` non-null (`avatars/…jpg`) for profile `3a98fe7b` (user `qa.alice.17876013524609211@…`).
- **Bonus verified:** J13 reorder controls (`move-photo-left/right-<id>`, `replace-photo-<id>`) now rendered + functional; `dev-add-test-photo-uploaded` drives J02/J11 AI/draft paths (AI analyzing overlay observed); carousel slide 3 typo fix holds.
- **KEY TOOLING FINDING (ItemCreate flaky-scroll):** terminate+relaunch did NOT fix the binary-snap scroll this run. Starting the swipe **over the photo grid (non-button surface)** resolved it — the swipe was being captured by the dev-fixture buttons. New workaround for the §5.9/§9 scroll class.

## 2026-08-23 — H01 dev avatar fixture FIXED (dev task, commit f0417f5a)
- `ProfileSetupScreen.handleDevSetAvatar` previously set `localImageUri` to `Image.resolveAssetSource(...).uri` (Metro URL in Expo Go / file:// bundle path in dev-client) → `uploadProfileAvatar` → `ImageManipulator.manipulateAsync` couldn't read it → non-blocking warning + `avatar_url = null` (the H01 spotcheck's F1 finding).
- Now copies the bundled asset into `FileSystem.cacheDirectory` (`expo-file-system/legacy`: `copyAsync` for file:// sources, `downloadAsync` for remote Metro URLs) and sets `localImageUri` to the cache `file://.../dev-avatar-<ts>.png` path — identical to a real image-picker result. The full avatar pipeline (ImageManipulator → upload → `avatar_url` persistence) now works for QA automation.
- Unit tests updated (2 new cases): copy branch asserts `copyAsync` called + avatar preview source is `file:///...cache.../dev-avatar-*.png` (not the raw asset URL); download branch asserts `downloadAsync` for a Metro `http://localhost:8081/assets/...` source. 4/4 pass; typecheck + scoped lint clean.
- H01 can now be re-run end-to-end (preview AND upload/persistence legs).

# QA Test Agent (created 2026-08-16, Phase 13.5; updated Phase 13.6, 13.8)

## 2026-08-24 — Account file Groups E+F+G + C03 closure (21 cases) — 12 PASS / 2 FAIL / 7 BLOCKED
- Report: `e2e-test-results/account-file-groups-efg-c03-2026-08-24/report.md`. Device iPhone 17 Pro Max sim. Cumulative Account file A–G = 44 cases (28 PASS / 2 FAIL / 14 BLOCKED).
- **C03 FAIL — root cause: GoTrue "Manual linking is disabled"** (staging Supabase Auth setting). CDP-captured `[accountService] unlinkSocialAccount failed: Failed to unlink identity: Manual linking is disabled`. Fixture + UI + confirm dialog all work; the backend toggle blocks `unlinkIdentity`. One-toggle ops fix. (See qa-test-accounts.md C03 section.)
- **Group E PASS 3/3 (destructive on throwaway):** E03 verified all side-effects via DB — `request_account_deletion` (profiles deletion_type='self'), `sp_wallets.state='frozen'`, audit in **`admin_activity_log`** (NOT admin_audit_log), auto-logout. Delete flow works.
- **Group G:** G01 FAIL (greeting `_getGreeting()` is dead code — never called; badge+SP pass), G02 BLOCKED (draft leg PASS on test-seller; grace/payment-fail/trial legs not inducible; **guide banner-priority ≠ source** — Trial/PaymentFail always-shown + Action Items ID>grace>drafts), G03/G04/G05/G06/G08/G10/G11/G12/G13 PASS, G07 BLOCKED (**max 3 CTA types = MAX_VISIBLE 3 → Show-more never renders**), G09 BLOCKED (no-session fallback unreachable, auth guard redirects to Landing).
- **Group F:** F02 error leg PASS (invalid token → "Unable to Unsubscribe"/"Invalid or expired token"/Go to Home; route is auth-gated); F01/F04 BLOCKED (no `test-suspended@…` persona — only suspended user is bobnoemailverficed.demo@example.com, pw unknown); F02 valid leg BLOCKED (unsubscribe_tokens empty; QA can't write); F03 BLOCKED (OfflineScreen orphaned — no trigger/deep link).
- **KEY FRICTION (DeleteAccountScreen):** focusing the password field causes a scroll-stuck state (buttons below fold; swipes give 0 changed px). **Workaround:** swipe from explicit start `(220,350)` up 350 → red #E85D75 button band at y 400–556pt → tap (220,478). Recurred on every field focus; use remount (back→re-enter) to clear.
- **CDP capture recipe (improved):** attaching the Hermes inspector via `/json` can trigger an app RELOAD → single-socket capture catches only a replay burst then dies (new target id). Use a **reconnect-capable** capture (poll `/json` every ~4s, re-attach if socket not OPEN) + retry the action AFTER the app settles (~8s). This captured the live `Manual linking is disabled` error that a plain attach missed. Reference script: `e2e-test-results/account-file-groups-efg-c03-2026-08-24/capture-cdp2.cjs`.


## 2026-08-24 — Group J (Listing Creation, Single Item) run — 8 PASS / 1 FAIL / 5 BLOCKED
- Report: `e2e-test-results/group-j-listing-creation-single-2026-08-24/report.md`. Device iPhone 17 Pro Max sim. Personas: test-seller (DB `sub_status='trial'` — registry "Active" is doc drift; trial counts as subscriber for canAcceptSP) and test-free (DB `node_id NULL`, `phone_verified NULL` — registry inferred otherwise; J07 only opens the form so unaffected).
- **PASS:** J01 (photo-first gating), J03 (disabled submit; title-length error path CDP+DB-proven), J04 (Condition 5+desc / Age 5 / Gender 4 / Color 12), J06 (SP toggle → "✓ SP Eligible"), J07 (free upgrade prompt + Upgrade Now → Kids Club+), J08 (~26 SP Books 1.30×20), J09 (success modal; item `033baae0` pending, NOT in public feed), J14 (Books bonus 1.30x multiplier in preview).
- **FAIL:** J15 — buyer max-SP preview NOT displayed in `SPEarningsPreview` (guide expects it; `categories.sp_spending_cap_percent` exists in DB but is not surfaced). Price-recalc leg passed (26→13 SP).
- **BLOCKED:** J02 (AI analysis gated on real upload; dev fixture sets no uploadedPhotoUrls), J05 (CategorySelectModal undrivable; no Other fixture), J11 (draft auto-save gated on real upload; 0 drafts for test-seller DB-proven), J12 (picker-gated; **10MB code cap vs guide's 5MB = doc drift**), J13 (remove leg PASS 3→2/10; **reorder/replace NOT implemented** — `PhotoUploadManager` `onReorder` destructured unused; persist gated on draft).
- **KEY ON-DEVICE FINDINGS:** (1) ColorPicker swatches `accessibilityRole="checkbox"` do NOT surface in the iOS AX tree on RN 0.81 — same class as `tab`/`adjustable`; recommend `"button"`. (2) ItemCreate ScrollView scroll blocker via mobile-mcp swipe (~0 changed px over 5+ variants) was cleared by a single **terminate + relaunch** (~15-20s) — add relaunch as a first-class unblock step for this class before pivoting to tree-only verification.
- **Verification recipe:** dev fixtures `dev-add-test-photo`/`dev-set-category` (→ Books, first non-Other)/`dev-fill-item` (title "QA Dev Fixture Item", price 20, condition new); reach via deep link `p2pkidsmarketplace://create-item`; submit footer at pt (220,868). dev-fixture path NEVER creates a draft (no uploadedPhotoUrls).


- Location: `.github/agents/QA-Test-Agent.agent.md` — lean persona/role/scope/tools file (mirrors `Kids P2P App Builder.agent.md` frontmatter). Detailed operating playbook moved (Phase 13.8) to `.github/instructions/QA-Test-Agent.instructions.md` per repo convention (agent file carries §1–§3 + §4 pointer + §5 invocation; playbook keeps §4–§9).
- Purpose: execution-only autonomous test runner. Invocation: give it a TC-ID/group (e.g. `AUTH-TC-S01`) and it executes the case from the canonical guide in `cross-checked-and-consolidated/` via `mobile-mcp`.
- Scope: iOS mobile only. Android OUT (Milestone 2 cloud fleet pending). Admin web OUT (Playwright path instead).
- Key playbook rules (full text in `.github/instructions/QA-Test-Agent.instructions.md`): re-list element tree before every tap (no tap-by-id); re-list after keyboard events (primary failure source); poll 1–2s / timeout 15–20s for transitions (timed transitions tighten to ~500ms–1s, §5.7); dialogs per Option B (Stripe PaymentSheet / Alert.alert / browser confirm() handled by text) BUT verify empirically each run — in-app `GlobalAlertProvider`/`ui/Modal` dialogs are locator-instrumentable, don't force Option B (§5.4); deep-link cases need clean-launch verification first — non-dismissible LogBox fatal overlay = environment blocker (BLOCKED, §5.8); WebView = screenshots as primary evidence; **three-layer judgment** = hard assert + UX review (structural AND wording/copy — propose concrete rewrites for parent/guardian audience — AND design-system compliance vs `docx/design-system-passitup.md` per §6.4, concrete deviations only: primary green #5DBB8E, 4px spacing base/16px between inputs, pill buttons/max-one-primary, filled inputs, touch ≥44px; §6.4 now explicitly covers EVERY modal/alert/toast/pop-up — color-token consistency + text/element alignment, same 3-layer review as full screens); **perceived load-time tracking** per transition, ≥3s flagged (§5.7 + §8.2 table); locator gaps flagged as findings, never silently worked around.
- Test personas: check `/memories/repo/qa-test-accounts.md` (standing staging registry) BEFORE creating a throwaway account; reuse persona per `Setup:`; only `new-user` is UI-created per run; missing/stale persona = setup gap (BLOCKED), never unsafe provisioning.
- **QA Session Handoff** (§8.3): per-case AND batch-summary reports close with the adapted dev-agent handoff template — `Test Scope` / `Design-System Compliance` / `Verdict Summary` / `Critical Findings` / `App State Left Behind` / `Why It Matters` / `How to Verify/Reproduce` / `Known Gaps / Not Tested` / `Suggested Next Session` / `Suggested to Improve Agent Rules` (Change Classification/Backward Compatibility/Regression Plan dropped — no code changes).
- Reads conventions from `/memories/repo/locator-conventions.md`, `test-authoring-conventions.md`, `simulator-keyboard-suppression.md`, `manual-testing-guide-canons.md`, `qa-test-accounts.md`, `docs/locator-coverage-tracker.md`, and `docx/design-system-passitup.md`.
- **§5.20 hardened 2026-08-21 (admin screenshots MANDATORY):** every surface must leave screenshot evidence. Playwright's default failure-only behavior saves ZERO screenshots on a passing run → a passing suite produces no admin visual evidence unless the spec has explicit `page.screenshot()` OR the agent runs an evidence-capture script (`capture-admin-evidence.cjs` recipe: CommonJS + `NODE_PATH=$PWD/node_modules`, run from the web-app dir, creds read from `.env.local` inside the script, `ADMIN-<step>.png` naming, run archive dir is 2 levels under workspace root). Verify admin screenshots exist in the evidence folder before finishing a run. (Real gap closed 2026-08-21 Group L: spec had no screenshots; captured post-run.)
- **§5.21 added 2026-08-21 (Phase 13.30, commit bd0c0bea) — stale dev-server diagnostic:** an admin/web dev server 404ing on ALL routes with the correct app+routes in source is usually a STALE dev server (left running across a git checkout/branch switch/code change), NOT an app-side blocker. Restart `npm run dev`, re-verify a known route (e.g., `/auth/login`) returns 200 before declaring an admin-side blocker; also check the listening process's CWD to rule out it serving the wrong app.
- **Phase 13.37 (2026-08-23, commit a228c079) — five playbook additions from the Group A+B+D decision log:** (1) §5.19 Rule 1 elevated to a HARD GATE — never tap AX-tree coords while keyboard visible (two corruption incidents: P17, A02); (2) new §5.28 — batch cases by required form baseline (mutate only the delta field, not full re-fill); (3) new §5.29 — prefer navigate-away-and-back remount over full relaunch for local form clears; (4) §5.9 elevated to HARD RULE — maintain a known-stale-screen list in memory (Profile, WebView screens) and skip straight to screenshot/OCR on those; (5) new §5.30 — reuse verified-unchanged prior-run evidence (must `git show`/mtimes-verify unchanged first; label "cross-referenced from run X, verified unchanged" in report).


## Phase 14 first real run (Group S Password Recovery, 2026-08-16) — verified facts
- **Deep-link redbox blocker (HIGH):** `xcrun simctl openurl booted "p2pkidsmarketplace://reset-password"` (warm or cold-with-pending-link) on the dev build raises a **non-dismissible LogBox fatal overlay**: Uncaught `` `new NativeEventEmitter()` requires a non-null argument. `` + caught Console Error `Error parsing initial URL for reset token: TypeError: Cannot read property 'default' of undefined` (ResetPasswordScreen.handleInitialUrl). LogBox captures all touches; footer Dismiss/Minimize NOT in AX tree; header (365,82) only cycles logs 2↔1. Plain launch (no deep link) = clean Landing, no redbox → redbox is deep-link-specific. Unblocks S07/S08/S09/S10/S11 once fixed.
- Cold `simctl openurl` lands on **Expo dev-launcher home** (dev build); must tap the Metro server entry (`http://localhost:8081`) to load the app, then the pending deep link delivers.
- **Alerts are NOT native** in this build — they render via in-app `GlobalAlertProvider`; buttons surface as `global-alert-button-0/1`. Guide's "native Alert.alert — assert by title" for Group S is outdated (doc drift).
- **S05 400 technique (reusable):** an oversized email (~300 `a` chars + `@test.com`) passes the client regex but exceeds GoTrue's 255-char limit → genuine server 400 → "Reset Email Failed" + appended account-guidance line. Use for future 400-state cases.
- Rate-limit (S03) / SMTP-500 (S04) not inducible on healthy staging (SMTP works — sends succeed). Need rate-limit-exhausted account or staging toggle / SMTP toggle.
- ForgotPassword success path works: `qaparent@kidsmarketplace.test` → Check Your Inbox; Send Another Email clears the field. S02 invalid email (`abc`) → "Invalid Email" exact copy; disabled-on-empty verified (gray).
- Run result: 3 PASS (S01,S02,S05) / 0 FAIL / 5 BLOCKED (S03,S04,S07,S08,S10). Report: `e2e-test-results/phase14-auth-pw-recovery-2026-08-16/report.md`.

## Phase 13.8 (2026-08-16) — restructure + bundled improvements
- Split: agent file → lean persona (§1–§3 + §4 pointer + §5 invocation); full playbook → `.github/instructions/QA-Test-Agent.instructions.md` (§4–§9). All Phase 11–14 content preserved verbatim in the playbook.
- §6.4 expanded to cover EVERY modal/alert/toast/pop-up (color-token consistency + text/element alignment; same 3-layer review as full screens).
- New §5.7 perceived load-time tracking (start/end timestamps, ~500ms–1s poll, "Perceived load time (simulator, wall-clock, ±polling-interval precision) — not a formal performance profile" label, ≥3s = UX finding; §8.2 table).
- New §5.8 environment-blocker detection (clean launch before deep-link cases; non-dismissible LogBox fatal overlay within 3 attempts = BLOCKED environment blocker).
- §5.4 empirical dialog-type verification (verify tree, not `Dependencies:` label; in-app GlobalAlertProvider/ui/Modal dialogs are locator-instrumentable — record doc drift).
- New §7 test-persona registry + `/memories/repo/qa-test-accounts.md` (standing personas + per-run new-user + provisioning = dev-team seed:staging; agent never does unsafe provisioning).

## Phase 15 (2026-08-16) — Group S re-verify + remainder (S07–S11) — verified facts
- **Deep-link redbox is FIXED and verified**: `p2pkidsmarketplace://reset-password` (warm + cold) renders the ResetPassword screen fully interactive — no LogBox overlay, no crash. The fix = static `import` of `Linking` in ResetPasswordScreen (dynamic `import('react-native')` enumerated lazy getters → NativeEventEmitter crash) + try/catch in handleResetUrl.
- **Run result: 4 PASS (S07,S09,S10,S11[1&3]) / 0 FAIL / 1 BLOCKED (S08).** S11 Case-2 (tokenized link) also blocked — same harness gap. Report: `e2e-test-results/phase15-auth-group-s-reverify-2026-08-16/report.md`. Device used: iPhone 17 Pro Max (iOS 26.1) — brief's "iPhone 17 Pro 26.5" not booted.
- **S08 remaining blocker = valid reset session** (real access_token/refresh_token). No mail client; no `generateLink` mint-and-return harness. The `supabase/functions/admin-trigger-password-reset` Edge Function calls `auth.admin.generateLink` but (a) needs admin auth and (b) returns only "email sent" — NOT the token. No in-app dev helper navigates ResetPassword with token params. Needs a dev-gated mint-and-return helper.
- **S07 execution nuance**: submit button is `disabled={!password || !confirmPassword}` → to trigger inline validation errors BOTH fields must be non-empty (guide's "enter X and tap" reads loose; matches Detox test which fills both). Inline errors surface as plain StaticText (assertable by text, no testID).
- **Alert.alert renders in-app via GlobalAlertProvider regardless of import source** — S10 "No active reset session" surfaced OK as `global-alert-button-0` even though ResetPasswordScreen imports Alert from react-native. Group S "native Alert.alert" labels = doc drift (confirmed 2nd surface).
- **Cold fragment-bearing deep links are flaky through the Expo dev launcher** (dropped → Landing, no navigation, no crash); warm delivery from a mounted ResetPassword screen is reliable. Prefer warm for fragment cases on dev builds.
- **Design-system minor**: ResetPassword shows TWO primary CTAs when the Link Error card is visible (Request New Reset Email primary + Reset Password primary). Recommend hiding/disabling submit in link-error state or demoting card button to outline. **FIXED + verified in Phase 16** — submit is now hidden (`{!linkError && …}`) so the link-error state has exactly one primary CTA (`reset-request-new-email-button`).

## Phase 16 (2026-08-16) — Group S closeout: S08 + S11 Case 2 (harness live) — verified facts
- **S08 PASS + S11 Case 2 PASS.** Run result: 2 PASS / 0 FAIL / 0 BLOCKED. Group S fully closed out (S03/S04 remain deliberately deferred backend-toggle gaps). Report: `e2e-test-results/phase16-auth-group-s-closeout-2026-08-16/report.md`.
- **Harness reality (doc-drift vs. memory recipe):** minted `resetLink` is the **OTP form** (`/auth/v1/verify?token=<OTP>&type=recovery&redirect_to=…`), NOT the fragment form. Tokens are issued by the **GET web-redirect exchange** (curl `-D -` → 303 Location carries `#access_token=…&refresh_token=…&type=recovery`). The `/auth/v1/verify` **POST** with `{type,email,token}` returns `otp_expired` — do NOT use it.
- **Admin JWT for the harness = `samer@samer.com`** (staging RBAC admin). `test-admin` has NO RBAC row on staging → `403 Forbidden: Admin access required`. Registry admin-persona row is doc drift.
- **Fragment-bearing deep links are warm-only from a MOUNTED ResetPassword screen.** From Landing/unmounted, a fragment URL is silently ignored (React Navigation matches only the path). Recipe: plain `p2pkidsmarketplace://reset-password` → mount → then deliver the `#access_token=…` URL warm. Tokenized link establishes a real session: submit proceeds with no "No active reset session" alert.
- **Success alert = in-app GlobalAlertProvider** (`global-alert-button-0` = OK) — 3rd confirmed surface of Group S "native Alert.alert" doc drift.
- **Link-error state fix verified:** when the Link Error card shows, `reset-submit-button` is absent from the tree (single primary CTA `reset-request-new-email-button`). Spot-checked via error fragment `#error=otp_expired&error_description=…expired` delivered warm.
- **Test data:** `test-buyer`'s password changed during S08 then **reset back to fixture `TestBuyer123!`** and re-verified by login. Left logged-out (Landing).
- **Friction:** keyboard is per-boot reset to SHOWN on this simulator — re-list after every focus (form shifts up under KeyboardAvoidingView); fix fields by terminate+relaunch if corrupted (none this run).

## Phase 22 (2026-08-17) — Group B cleanup (B10-B12) + Group D (Logout) + Group E (Phone Verification) — verified facts
- **Run result: 8 PASS / 0 FAIL / 2 BLOCKED (E04, E05) / 1 SKIPPED (D02 per user).** Report: `e2e-test-results/phase22-auth-group-b-d-e-2026-08-17/report.md`.
- **B10 back button = BP-53 gap:** `login-back-button` (TouchableOpacity+`←`, testID only, no accessible/role/label) NOT in AX tree — pixel-derived tap (~35,107). Profile utility rows (`profile-logout`, `profile-settings`…), OTP resend link, and Sell-sheet options (`sell-option-list-one-item`/`sell-option-bulk-upload`) have the SAME exposure gap.
- **Below-fold AX coords are LOGICAL, not rendered, when the keyboard is up** — e.g. the signup submit button rendered ~160pt higher than the tree reported (pixel-scan the green #5DBB8E pill). With keyboard DOWN the tree coords were accurate. This is the root cause of most mis-taps this session.
- **Signup form:** fields below the phone field get covered by the number-pad when the keyboard is up → taps hit keys (corrupted the phone field; recovered via §5.10 long-press → Select All → retype, which WORKS for text/phone inputs). Reliable pattern: dismiss keyboard (tap a non-input label) → tap field BOX CENTER (text-line y + ~10) → type. DOB day/month/year = TextInputs (placeholders DD/MM/YYYY, auto-advance); values surface in AX tree once filled.
- **DEV-autofill on signup fills the WHOLE form** (Alice/Bob/Charlie fixed users) — then override email+phone via Select All → fresh values → submit. Big time-saver for repeated signups.
- **QA logout deep link (`p2pkidsmarketplace://qa-logout`) is clean** (no LogBox overlay) and reliably clears any session → Landing.
- **OTP screen:** auto-send "Code Sent (DEV Bypass)" dialog surfaces LATE (async after mount) — don't assume it's absent; poll. Verify button is DISABLED-gray with <6 digits → the "Please enter all 6 digits" alert is unreachable (guide copy = doc drift). Wrong code (111111) → "Verification Failed / Verification code expired or not found" (OTPExpiredError path, since dev-bypass stores no code row — guide's "Invalid code" = doc drift in dev).
- **E03 cooldown verified:** resend fires sends; re-enables ~60s later (taps fired sends #2/#3 ~60s apart). Resend text renders disabled-gray (neutral[300]) during cooldown; not OCR-readable from screenshots.
- **E04 rate limit NOT inducible in dev:** 4 rapid sends → always SEND_FAILED → DEV bypass; RATE_LIMIT_EXCEEDED never surfaced. OTP screen's rate-limit catch is a generic `Alert.alert('Error', err.message)` — friendly "Too many attempts…" copy only in `usePhoneVerification.sendCode`. Recommend screen reuse + a QA rate-limit toggle.
- **E05 gate source-verified + setup achieved:** Sell tab opens a **Sell Options Sheet** (List One Item / Bulk Upload) — must tap "List One Item" to reach ItemCreate "New Item". Unverified user reaches it via signup → don't verify OTP → terminate → relaunch → onboarding carousel → Skip (`skip-button`) → Home. `ItemCreateScreen.handlePublish` → `isPhoneRequired` (true when phone_verified_at null) → `setShowPhoneVerificationModal(true)` (required, no dismiss) → onSuccess retries handlePublish. **BLOCKED on execution:** ItemCreate screen would NOT scroll via mobile-mcp swipe (4 variants, 0 movement, confirmed by pixel-diff) — form could not be filled; also requires a photo (native picker). Treat "0 changed px after swipe" as a scroll blocker/tooling friction, don't retry repeatedly.
- **Test accounts created (throwaway, do not reuse):** `qa.p22.e{1,2,3}.1786964375@kidsmarketplace.test` (+12025551001/002/003); E1 verified, E2/E3 unverified; sessions cleared. `test-buyer` left logged-out.


## Phase 23 (2026-08-17) — Groups F (node/ZIP gating + waitlist), H (profile setup/onboarding), I (subscription choice) + E05 re-attempt — verified facts
- **Run result: 9 PASS / 1 FAIL / 7 BLOCKED / 0 SKIPPED.** Report: `e2e-test-results/phase23-auth-group-f-h-i-e05-2026-08-17/report.md`. Test accounts: users A/B/C/D/E/F created via UI (Alice/Bob/Charlie autofill + unique per-tap email/phone), all sessions cleared at end.
- **ZIP gating (verified):** `06850` (Norwalk CT) → exact-match **Norwalk Central** active node, NO waitlist modal, Home header shows the node name. `07999` (Whippany NJ) → no node → **fallback Little Falls Central** + "We're Coming Soon!" waitlist modal (transparent RN Modal, buttons tappable/assertable). Continue Trading = no waitlist join; Join Waitlist → "Waitlist Confirmed" → Got it. F01–F05 all PASS.
- **F06 FAIL (spec/design drift):** Discover defaults to **ALL items** ("1205 results") — NOT node-scoped; **no "Show All Nodes" toggle exists**. `DiscoverScreen.tsx` L253-254: "do not auto-apply location filter — Requirement: load all discover items by default". Node-scoping only via opt-in ZIP+radius **location filter** (`resolveNodeScopeByLocation`, `nodeIdsInScope`). Guide's "My Node default + Show All toggle" not implemented (source says intentional) — needs product decision.
- **H01/H02/H06/H07 PASS; H03/H04/H05 BLOCKED.** H03: avatar-upload-failure path not cleanly inducible (needs network interruption mid-upload); source-verified non-blocking (`ProfileSetupScreen` L190-192 warning + continue). H04/H05: `WelcomeScreen`/`FeatureHighlightsScreen` + routes EXIST in source but are **orphaned/dead routes** — current onboarding is a single 5-slide carousel that `replace('Home')`, deep links `welcome`/`feature-highlights` don't navigate from Home. H06 (Skip AND Get Started variants) + H07 (relaunch → straight Home) verified on multiple users.
- **I01–I03 BLOCKED (feature removed):** `subscription-choice` route resolves to **JoinKidsClubScreen (R7 web-first)** — no "Start Free Trial"/"Continue Free" onboarding choice exists in the build.
- **E05 re-attempt BLOCKED at category step (toolset limitation, not app defect).** Resolved from Phase 22: (a) ItemCreate "0-movement swipe" was **correct conditional rendering** — with a photo the form renders AND scrolls normally; (b) **`dev-add-test-photo` fixture works** (injects placeholder photo, renders full form); (c) **`dev-fill-test-user-1` autofill now generates UNIQUE email+phone per tap** (no Select All override); (d) deep link `p2pkidsmarketplace://create-item` reliably reaches ItemCreate (bypasses the Sell sheet). NEW blocker: **`CategorySelectModal` is a native `presentationStyle="fullScreen"` RN Modal in a SEPARATE window/layer — invisible to the AX tree AND unresponsive to mobile-mcp taps** (decisive: tapping the modal's × did not close it but DID dismiss the underlying ItemCreate keyboard, kb-px 21994→25; AX tree = 69 elements of underlying content only). Transparent RN Modals (waitlist, GlobalAlert) remain fully tappable — the fullScreen presentation is the difference. `canPublish()` requires category (photo+title+category+condition+price); gate `isPhoneRequired` → modal source-verified. `idb` not installed (no alt tap path). **Fix candidates:** `dev-set-category-<id>` fixture (mirror dev-add-test-photo) or present CategorySelectModal transparent.
- **Unverified-user session persistence is FLAKY in dev:** survived one relaunch (→ carousel), lost on a later one (→ Landing with a stale `sb-*-auth-token` in AsyncStorage). Clear via `qa-logout` deep link (verified clean, wipes auth keys).
- **Phase 23 vs Phase 22 friction:** unique autofill + deep-link nav + photo fixture removed the 3 biggest Phase 22 bottlenecks (signup entry, ItemCreate scroll, per-case signup cost). Remaining dominant blocker = the native-fullScreen-Modal interaction wall (Sell sheet + CategorySelectModal).


## Phase 23 (2026-08-17) — ItemCreate "scroll blocker" RESOLVED (NOT a defect) — verified facts
- **Root cause (E05 blocker from Phase 22):** `ItemCreateScreen` uses a standard `ScrollView` (flex:1, contentContainerStyle padding:16) inside `ScreenLayout` (SafeAreaView+AppHeader, no gesture interceptors); the nested `FlatList` in `PhotoUploadManager` has `scrollEnabled={false}` (correct). **The form fields render ONLY when `photos.length > 0`** (photo-first flow, LISTING-V3-005). With 0 photos there is literally no scrollable overflow → "0 movement after swipe" is CORRECT behavior, not a defect. Real users add a photo via the native picker → the tall form renders → the ScrollView scrolls normally (verified: swipe produced 1.47M changed px; publish button became reachable).
- **New __DEV__-only fixture (2026-08-17):** `ItemCreateScreen` now has a `Dev: Add Test Photo` button, `testID="dev-add-test-photo"` (accessible/role/label), that injects the bundled `assets/adaptive-icon.png` into `photos` + `PHOTOS_ADDED` — bypasses the native image picker (the real Group J/K automation blocker). Does NOT set `uploadedPhotoUrls` → no AI analysis, no draft save side effects. ScrollView now has `testID="item-create-scroll-view"`. `category-select-button` now has accessible/role/label (was a BP-53 gap). Below-fold fields were already AX-exposed once rendered (`title-input`, `manual-price-input`, `publish-button`, brand/color/age/gender, `sp-toggle`).
- **QA recipe to unblock Group J/K:** Sell tab → Sell Options Sheet → tap "List One Item" (sheet options NOT AX-exposed — pixel-locate row ~pt y 700-760; "Sell" title at ~643pt) → New Item → tap `dev-add-test-photo` → form renders → swipe up → fill title/category/price → publish. Evidence: `e2e-test-results/itemcreate-scroll-investigation-2026-08-17/`.

## Phase 24 (2026-08-17) — F06 re-verify (behavioral) + dedicated Discover design-system audit
- **F06 FAIL CONFIRMED behaviorally (Phase 23 ambiguity resolved).** Staging has 5 nodes; available listings: Norwalk 66, Greenwich 13, **Little Falls Central 0**, Buffalo 0, Test Node 1 0. Total available = 1205, of which **1126 (93%) have node_id NULL**. Fresh UI user with ZIP 07424 → **DB-verified node = Little Falls Central (0 available)** → Discover default = **"1205 results · near NJ"** (full set) → NOT node-scoped. Report: `e2e-test-results/phase24-f06-reverify-discover-audit-2026-08-17/`.
- **Location filter is a NO-OP (backend ignores p_node_ids).** Live DB `pg_get_functiondef`: `search_listings` AND `count_listings` accept `p_node_ids` but never reference it ("not node-gated"). Applied ZIP 07424 (0 in-node) → 1205; ZIP 06850 (66 in-node) → 1205 — label-only ("near {zip}, {r} mi"). `get_nodes_within_radius` reads **deprecated `geographic_nodes`** (Buffalo+Greenwich only, DIFFERENT UUIDs than `nodes`). Only SP/category/price filters actually scope (SP toggle 1205→50).
- **NEW UX BUG (first-run onboarding):** fresh user taps **Skip onboarding** → Home renders with **NO persistent tab bar** (no FAB/tabs in tree or pixels, minutes of checks). Tab bar appears only after **app relaunch**. Affects any fresh-signup flow needing the tab bar.
- **Discover design-system vs `design-system-passitup.md`: PARTIAL/FAIL on Discover surface.** Root cause: Discover uses `theme/discoveryTokens.ts` sourced from `design-system.md` (primary #4A7C59) NOT the canonical passitup (#5DBB8E), AND mixes both. Major deviations: **sort dropdown selected = #EEF6FF bg + #007AFF (iOS system blue) text**, border #E5E7EB, unselected text #1F2937; **Filters-sheet Apply button = #4A7C59** (ds.primary[500]) while same sheet's Reset/pills = #5DBB8E (internal inconsistency). Minor: result count #4D4D4D (vs #6B6B6B), non-SP chips/trending #E8F3EC/#3A5F47 (vs #E8F5F0 tint), header icons/border #F5F5F5/#4D4D4D, drag handle #CCCCCC, slider track #E5E7EB, H1 32 vs 28px. CONFIRMED: search bar (#F0F0F0/#999999/#1A1A1A), filter button, favorites bookmark #E85D75, SP toggle/chip/badge (#F59E0B/#FEF3C7), cards (#FFFFFF/#1A1A1A), empty state (#1A1A1A/#6B6B6B/#5DBB8E), modal header/ZIP/category pills (#5DBB8E selected / #F0F0F0 unselected).
- **Tooling:** off-screen tree y-coords are logical not rendered (autofill/submit rendered ~500pt lower when below fold — scroll into view first). `mobile-mcp open_url` rejects non-http (use `xcrun simctl openurl` for qa-logout). Filter-sheet Apply button position varies (y ~612-638 keyboard-up, ~868-922 keyboard-down) — pixel-scan each time.

## Phase 23 wrap-up (2026-08-18) — Vision-OCR helper is now a permanent asset + H03 unblocked
- **Vision-OCR helper canonicalized → `scripts/qa/vision-ocr.swift`** (Apple Vision framework; usage + companion ImageMagick techniques in `scripts/qa/README.md`). When `view_image` fails to deliver pixels (the norm in constrained sessions), use this instead of rebuilding: `swift scripts/qa/vision-ocr.swift <png>` prints OCR'd text lines top-to-bottom. Pair with tight-crops and ImageMagick color/connected-component scans for non-AX controls (see README). Do NOT re-create `temp/ocr.swift` ad-hoc.
- **AUTH-TC-H03 VERIFIED (2026-08-18):** new `qa_avatar_upload_failure` admin_config toggle (`getSimulatedAvatarUploadError` in `devTestingService.ts`, injected at top of `uploadProfileAvatar`) armed on-device → **Warning alert + profile created without avatar** (non-blocking path confirmed). Toggle left **disarmed (`none`)** after the run. See `/memories/repo/qa-test-accounts.md` (H03 toggle section; dev-team arms/disarms it).
- **F06 actor fix:** `seed:staging` now assigns `profiles.node_id` for standard personas even on re-seed (fixes `test-buyer` being node-less). Re-run seed before F06.
- **F07 registration:** AUTH guide F06's dangling "see AUTH-TC-F07" cross-ref repointed to F02/F03/F04 (the inactive-ZIP/waitlist dialog cases) — no new F07 case needed.
- **AUTH-TC-E05 unblocked (2026-08-18):** `dev-set-category` fixture added to `ItemCreateScreen` (mirrors `dev-add-test-photo`; bypasses the native fullScreen `CategorySelectModal` that was unreachable by the toolset). testID **`dev-set-category`** (green button below `dev-add-test-photo`, `__DEV__`-only, accessible/role/label per BP-53). Sets a real category from the loaded list (skips `Other`) → `canPublish()`'s category check passes → the phone-verification gate is reachable. Disabled until categories load; button label shows the picked category name. QA recipe added to the guide's E05 entry.

## 2026-08-18 — playbook §5.14 Terminal approval discipline added
- New §5.14 in `.github/instructions/QA-Test-Agent.instructions.md`: read-only terminal discipline (minimize approval prompts; no mutations without explicit approval). Carve-outs kept for (a) the sanctioned `run-suite.sh` suite (its archive/issue-filing/commit is pre-approved per copilot-instructions), (b) evidence writes confined to `e2e-test-results/<run>/`, (c) `xcrun simctl` device-state/deep-link commands, (d) read-only SQL via `mcp_supabase_execute_sql`, (e) localhost Hermes CDP `/json` reads.


## Phase 25 (2026-08-19) — Group K (Bulk Listing) + bulk phone-gate check — verified facts
- **Run result: 6 PASS (K01–K06) / 0 FAIL / 0 BLOCKED / 0 SKIPPED + 1 FAIL finding (bulk phone-gate).** Report: `e2e-test-results/phase25-auth-group-k-bulk-2026-08-19/report.md`.
- **BULK FLOW HAS NO CLIENT-SIDE PHONE GATE (P1, mirrors E05's class — report first):** `BulkListingCreateScreen.handlePublish` (lines ~1360–1389) has ZERO `isPhoneRequired`/`phone_verified`/`PhoneVerificationModal` refs — goes straight from field validation to `publishBulkDrafts`. Unlike E05 (gate in unreachable branch), this is a wholly ABSENT gate. Empirically: fresh unverified seller (phone_verified=null, DB-verified) drives entry→Group→Review→Submit with NO modal. Server-side still holds: `trg_items_enforce_phone_verified` (BEFORE INSERT on items, `tgenabled='O'` on staging) raises `PHONE_VERIFICATION_REQUIRED` for unverified non-admin sellers; `publishBulkDrafts`→`createListing`→`items` INSERT → trigger fires; unverified seller had 0 items post-attempt. Dev fix: mirror the hoisted `isPhoneRequired` gate + `PhoneVerificationModal` from ItemCreateScreen.
- **Reorder-in-group is NOT in the active Group-step UI (spec gap):** `PhotoGroupingView` (drag-handle reorder) is orphaned — imported/rendered nowhere; active `PhotoSelectGrid` has no reorder affordance. Guide K02 asserts reorder; UI can't do it.
- **AX-tree staleness is SEVERE on `BulkListingCreateScreen`:** `mobile_list_elements_on_screen` returns cached/pre-interaction content after expansion/scroll (e.g., SP-summary-at-top while screen shows expanded form). Screenshot+OCR+ImageMagick became the source of truth. Card-header toggles, photo tiles, selection-bar buttons, step indicator, Submit bar, exclude switches all fail to surface or fail to accept taps via coordinates derived from stale trees.
- **Working interaction recipe on this screen:** type into form TextInputs by tapping the measured input center (gray `#F0F0F0` boxes) — typing lands even with keyboard suppressed; form fields: Title~y345–397, Price~y331–383, Brand~y508–560 (positions shift with scroll; re-scan each time). Condition chips tap ok. **Exclude switches never toggled via toolset** (blocked K05 "Submit 1 Item" + confirm-sheet open — source-verified instead).
- **Apply to All bar:** renders only when a field has a non-blank most-common value (hidden when all blank — correct); chips appear as values are set (Brand Nike + New); non-destructive (Item 1 kept its value). Propagation source/unit-test-corroborated.
- **K06 free-user SP summary:** non-subscriber sees 🔒 "Upgrade to Kids Club+ to earn these points when items sell" + `upgrade-cta` INSTEAD of SP totals (verified on-device with unverified free user).
- **Publish step (K03):** not reachable via fixtures (PUBLISHING state needs a real submit; fixture path hits "Missing bulk session or draft session" before PUBLISH_START).
- **Duplicate-photo perceptual-hash flagging (K01):** not exercisable via `dev-add-test-photos` (fixture skips dup detection; all 5 photos are the same bundled asset) — documented limitation.
- **Test data:** throwaway unverified seller `qa.alice.17871478847129759@kidsmarketplace.test` (user b645cd23-…), phone_verified=null, 0 items; left logged out. test-seller left logged out.

- **Decision-and-outcome log written:** `e2e-test-results/phase25-auth-group-k-bulk-2026-08-19/decision-outcome-log.md` — full action→reasoning→tool-call→outcome trace plus derivation sections: (a) what slows execution, (b) proactive patterns, (c) instrumentation/fixture work that removes friction. This is the reference format for feeding a run to an AI agent for process analysis.

## 2026-08-18 — Phase 13.20/13.21 re-applied + committed (commit 967f9a17)
- **Lesson:** playbook edits in `.github/instructions/QA-Test-Agent.instructions.md` were lost (Phases 13.20/13.21) because they were never committed — a file revert between sessions destroyed them. **Commit agent-definition/playbook edits in the SAME turn they are applied.** Recovery source of last resort: the chronicle session store (`session_store_sql` on `turns.user_message`/`assistant_response` for the originating session id `3fe98aa7-…`) held the verbatim prompts + placement reports; the Phase 23 wrap-up decision log (`e2e-test-results/phase23-wrapup-f06-reverify-f07-h03-2026-08-18/decision-outcome-log.md`) is the underlying evidence.
- Re-applied verbatim: §4 DB-precondition + referenced-case-exists checks; §5.4 measured-bbox-not-estimated-center; §5.9 scroll-anchor, deterministic pixel/OCR fallback, Discover grid coordinate-staleness; §6.1 two-source corroboration; new §5.16 qa-logout-over-Profile-logout technique. Phase 13.22 stale-observation stays §5.15.
