# QA Task 29 — ADM first full real execution round (2026-09-04) — ~88/160 executed, 72 PASS

- Report + full 160-case ledger: `e2e-test-results/qa-task29-adm-first-live-2026-09-04/report.md` + `ledger-FULL-160.md`. Tracker ADM: 72 PASS/8 PARTIAL/7 OPEN/1 SKIP/72 remaining (roll-up + section updated).
- **Group F (restored page) all PASS live** — F01–F10 (F09 on /analytics only; F10 legacy read-only disabled). F05 real round-trip payout_buffer_days 2→5→2 DB-verified; fn_admin_config_int 72/2/7-default; F06 172h client HARD-BLOCK (exact copy) + server P0001.
- **FINDING (MED-HIGH): R2 server guardrail ORDER-DEPENDENT** — per-key non-transactional `upsert_admin_config_setting` loop + BEFORE-UPDATE trigger validating each key vs CURRENTLY-STORED paired value → one-batch offer↑100 + pickup↓67 (valid 167h) FAILS (offer checked vs stale stored pickup 72). Two saves needed. Guide F06 step-3 one-batch expectation doesn't hold. Recommend single-transaction save or deferred constraint.
- **FINDING (MED): /payouts/earnings data 401** "No valid authentication provided" (fetch /api/admin/payouts, page.tsx:64) in logged-in session while sibling admin APIs work (same verifyAdminAuth). Blocks K02 data/K03/Z05. Dev: header-vs-session auth mismatch on that route.
- **FINDING (LOW): /subscriptions defaults per-user** "Provide ?user_id=... No subscriptions found" (M02).
- **Tooling:** prompt() not supported by embedded driver → B03/B06/B07 commit legs blocked (buttons + UI verified). Controlled React input state sync flaky on settings forms (single-field round-trips persist; fill dependent < fields first). Native validation alerts interrupt run_playwright_code → handle_dialog. data-testid=input-<key> + save-button exist.
- Group A 6/6 PASS; corrected J01 (bare /tax 404), Q02/Q03 (confirm copy EXACT), L06 (card-sp-wallet gone), W01 (7 groups) all hold. Clean-pool: B/C/D/G/H (money breakdown DB-verified 4500+99+0=4599¢)/N/O/P/Q/V/W/X/Z/N2/Regression sampled.
- Design: admin active-item #4A7C59, KPI card 16px r Level-1 shadow; A06 label 12px vs 14px doc; X03/Z pills Material (#4CAF50/#FFA726/#E53935).
- Config left clean (timing keys baseline); DT-99 throwaway still active; no moderation/user mutations committed.
