# Guide-Currency Audit — MODULE-ADMIN-PORTAL-MANUAL-TESTING.md

**Date:** 2026-09-02 · **Agent:** Guide-Currency Audit (read-only, Explore research) · **Scope:** Sections 2–4 of the Guide Currency Audit v2 task — **audit + classify only; NO guide rewrite performed.** A rewrite (if needed) becomes its own follow-up task.
**Guide audited:** `cross-checked-and-consolidated/MODULE-ADMIN-PORTAL-MANUAL-TESTING.md`
**Backend audited against:** staging Supabase `drntwgporzabmxdqykrp` (contracts only — source/reachability audit, no live DB reads)
**Audit method:** Read-only. For each referenced surface, verified (a) file existence under `p2p-kids-admin/src/app/**` / `src/components/**`, (b) reachability from the live navigation shell (`src/app/layout.tsx` → `AdminShell` → `Sidebar`/`TopNavbar`; legacy `ProtectedLayout` confirmed unused), (c) quoted copy/testID vs current source. Canonical-source + stale-screen rules from `.github/instructions/QA-Test-Agent.instructions.md` §4 applied. No files written by the audit.

**Layout reality (verified):** Active shell = `src/app/layout.tsx` → `AdminShell` (grouped purple `Sidebar` + `TopNavbar` + `CommandPalette`). `src/app/components/ProtectedLayout.tsx` (flat blue top-nav with `/audit-logs`) is **legacy/dead** (zero importers). Live sidebar sections: OVERVIEW (Action Center pinned + Dashboard), TRADE OPERATIONS (Trades, Trade Pipeline, Disputes→`/trades/disputes`, Flagged Items, Cancel Insights, Reviews), USERS & TRUST (Users, ID Badges, Waitlist, Badges), MONETIZATION (Subscriptions, Payments, Payouts, SP Economy, Referrals, Financial Audit→`/audit`), CATALOG (Categories, Category Mapping, Education, FAQ, Listings), PLATFORM CONFIG (Config, Tax Rules/Reports/Settings/Nodes, Cart Settings, Trade Timing, Policies, Support, Nodes), ANALYTICS (Analytics).

---

## Per-section audit

| Section / TC-IDs | Referenced surface(s) | Existence + reachability | Classification | Reason + evidence |
|---|---|---|---|---|
| **A — Auth & Dashboard** A01–A06 | `/auth/login`, `/` dashboard, protected-route gate | Login, RBAC gate, dashboard all exist | **Current** (credential caveat) | `src/app/auth/login/page.tsx` (RBAC `role='admin'`, red error `You do not have admin access…`); `src/app/page.tsx` (no duplicate nav cards). **Credential doc-drift:** guide says `test-admin@kidsmarketplace.test`; login screen's "Demo Credentials" box hardcodes `admin@example.com / admin123456` — **neither is the verified staging admin `samer@samer.com / samer`**. Point QA at the real creds; treat A01/A02 account rows as stale guidance. |
| **B — User Management** B01–B08 | `/users`, User Detail drawer, suspend/unsuspend/reset/soft-delete, SP adjust/freeze, analytics cards, sort | Page + all four Admin-Action buttons + drawer + DAU/MAU cards + sort + SP wallet actions verified | **Current** | `src/app/users/page.tsx` (`btn-suspend-user` ~L900, `btn-unsuspend-user` ~L909, `btn-reset-password` ~L918, `btn-delete-user-soft` ~L928; DAU/MAU L479–484; Sort By L560; prompts L252–364). Sidebar present. Mobile "Suspended screen" reflection is cross-surface. |
| **C — Listings, Items & Flagged** C01–C12 | `/listings`, `/items/flagged`, `/items/[id]`, ListingSearch | All pages/components exist and are reachable | **Current** | `src/app/listings/page.tsx` (tabs); `ListingSearch.tsx` (testids, actions incl. approve/pause/reject/request-edits); `src/app/items/flagged/page.tsx` ("Moderation Queue" L291, tabs L295, decision-note L468); `/items/[id]` exists. |
| **D — Categories** D01–D11 | `/categories`, CategoryForm (Basic/Icon & Badge/SP Config), suggestions queue, bulk actions, drag reorder, delete guards | All exist | **Current** | `categories/page.tsx` (FilterTab `bonus`); `CategoryForm.tsx` (3-tab); `CategoryTable.tsx` (DnD); `BulkActionsDropdown.tsx` (L71 guard). |
| **E — Nodes & Waitlist** E01–E08 | `/nodes`, `/settings/nodes`, `/waitlist`, per-node KPIs, SQL checks | All exist; E06/E07 SQL-editor cases are by-design DB | **Current** (E06/E07 partly **Fixture-gated/backend** — SQL cases) | `nodes/page.tsx` (deactivate warning L135, N6 KPIs); `settings/nodes/page.tsx`; `waitlist/page.tsx`. `/settings/nodes` not in sidebar (direct URL / `/config` cross-link only). |
| **F — Global Config & Settings** F01–F11 | `/config` tabs, `/settings/cart`, `/settings/trade-timing`, `/trades/pipeline`, fee keys | Pages exist — **but `/settings/trade-timing` no longer renders most of the guide's surface** | **STALE** (F01/F02/F04/F07/F11 current) | **Top Group F finding (largest stale cluster):** `src/app/settings/trade-timing/page.tsx` now renders only Offer Expiry / Offer Limits / Auto-Complete / Buyer Cancel Requests / Swap Points / Transaction Fees (**exactly 2 keys**: `transaction_fee_subscriber_cents` "Kids Club+ Member Fee" + `transaction_fee_non_subscriber_cents` "Free-Tier User Fee"). **Missing from live page:** F03 seller fee % per tier / buyer platform fee fixed+% / Charge One Fee toggle; F05 "Pickup & Payout" (`pickup_window_hours`, `payout_buffer_days`); F06 pickup reminders + 168h guardrail; F08 "Tiered Buyer Fee — R1"; F09 "Buyer Fee-Tier Distribution" (**now on `/analytics`**, `FeeTierDistributionCard` L187); F10 "Legacy fee keys (audit only)" (keys shown live/editable as `transaction_fee_subscriber_cents` vs guide's `transaction_fee_member_cents`). R2 guardrail exists only in the mirror test `settings/trade-timing/__tests__/trade-timing-settings.test.ts` L106–110 — **page.tsx has drifted behind its own test**. |
| **G — Policy Management** G01–G04 | `/settings/policies`, `/new`, `/[id]/edit` | All exist; publish confirm copy matches exactly | **Current** | `settings/policies/page.tsx` (L131), publish confirm verbatim (page L86 / [id] L64). |
| **H — Trades** H01–H06 | `/trades`, `/trades/[id]`, Monetary Breakdown, Subscription Context, External References, Sales Tax | All verified | **Current** | `trades/page.tsx` (h1 L227/407); `trades/[id]/page.tsx` (Monetary Breakdown L185, Sales Tax L206, Subscription Context L351, External References L371); `TradeActions.tsx`. |
| **I — Disputes** I01–I05 | `/trades/disputes`, `/disputes`, `/trades/disputes/[tradeId]`, DisputeFilters | Both queue + both detail pages exist | **Current** | `disputes/page.tsx` (SLA L27+, `SLA!` L117); `DisputeFilters.tsx`; sidebar "Disputes"→`/trades/disputes`. `/disputes` has no sidebar item (reachable via back-links + direct URL) but is live. |
| **J — Tax Admin** J01 | `/tax`, `/tax/nodes`, `/tax/settings`, `/tax/reports`, `/tax/rules` | Sub-pages all exist — **bare `/tax` 404s** | **STALE** (partial — only the "Open /tax" step) | `p2p-kids-admin/src/app/tax/` has **no `page.tsx`** (only `category-mapping/ nodes/ reports/ rules/ settings/`). Tax subpages verified (`/tax/nodes` L166 banner "Global sales-tax settings live in Config → Tax…"). Fix the single `/tax` step to point at a subpage. |
| **K — Payouts** K01–K03 | `/payouts`, `/payouts/earnings` | Config page + earnings list verified | **Current** | `payouts/page.tsx`; `payouts/earnings/page.tsx` (failed deep-link L62–65, retry confirm L150 exact). `/payouts/earnings` no sidebar item (health-strip deep-link + direct URL). |
| **L — SP Economy/Analytics/Wallet** L01–L08 | `/sp-economy`, `/sp-analytics`, `/sp-wallet`, dashboard | Hub + wallet + analytics pages exist | **Current with L06 STALE** | `sp-economy/page.tsx` (tab routing), `sp-wallet/page.tsx`, `sp-analytics/page.tsx`. **L06's dashboard "SP Economy nav card `card-sp-wallet`" no longer exists** — legacy card grid removed (`src/app/page.tsx` comment); grep finds zero `card-sp-wallet`; only the 4-tile `sp-economy-summary` remains. L07 SQL / L08 mobile = **Fixture-gated**. |
| **M — Subscriptions Admin** M01–M06 | `/subscriptions`, `/subscriptions/manage` | Manage page fully present; confirms match guide | **Current** | `subscriptions/manage/page.tsx` (metrics L361–395, filters incl. `free` L502, `btn-extend-trial-<id>` L643). `/subscriptions/manage` no sidebar item (button from `/subscriptions` + direct URL). |
| **N — Referrals Admin** N01–N04 | `/referrals` Config + Analytics | Present | **Current** | `referrals/page.tsx` + tabs. |
| **O — ID Badge Verification** O01–O05 | `/id-badges`, `[requestId]/review|details`, `/messages` | All four routes exist | **Current** | `id-badges/…` + `REJECTION_REASONS` L11 + message templates `{rejection_reason}` L20. |
| **P — Badges & Sandbox** P01–P04 | `/badges`, `/badges/sandbox`, BadgeEditor, ManualAwardModal | All verified | **Current** | `badges/page.tsx` (h1 L120, sandbox link L127–138); `BadgeEditor.tsx` ("Edit Badge" L160); `ManualAwardModal.tsx`. |
| **Q — Review Moderation** Q01–Q06 | `/reviews` | Page exists; structure current — **two confirmation strings differ** | **STALE** (copy drift in Q02/Q03) | `reviews/page.tsx`: Q02 Hide confirm is actually `This will remove the review and notify everyone who reported it. Continue?` (L88), not the guide's `Are you sure you want to hide this review?`; Q03 Approve/unhide confirm is `This will keep the review visible, reject all reports, and notify everyone who reported it. Continue?` (~L113), not the guide's wording. Endpoints are `/api/reviews/*` (not `/api/admin/reviews`). Reason label "Offensive Content" vs guide "Offensive". |
| **R — Education & FAQ CMS** R01–R03 | `/education` (Sections/Examples/Analytics), `/education/faq` (Questions/Categories/Analytics) | Present | **Current** | Both pages + tabs verified. |
| **S — Support Messages** S01–S03 | `/support`, `/support/[id]`, `/api/support/*` | Present | **Current** | Inbox + detail (reply thread) + API routes verified. |
| **T — Analytics** T01–T02 | `/analytics`, `/analytics/notifications` | Present | **Current** | Revenue dashboard + Notification Analytics (testids L225–294). |
| **U — Audit Logs** U01 | `/audit-logs` | Page is a static "Quick SQL" reference block by design — guide caveat holds | **Current** (stub by design) | `audit-logs/page.tsx` = h1 + paragraph + `<pre>` SQL. **No sidebar item** (only dead `ProtectedLayout.tsx` L143 calls it); live audit UI now = `/audit` (Financial Audit). |
| **V — Monitoring & Cron** V01–V02 | `/monitoring`, `/monitoring/cron`, `/api/monitoring/*` | Both pages verified | **Current** (reachability caveat) | `monitoring/page.tsx`; `monitoring/cron/page.tsx` (testids L421–707). **Neither in sidebar** — `/monitoring` via health-strip, `/monitoring/cron` direct URL only. "Run Now"/"Schedule" are real side-effecting mutations (guide already flags). |
| **W — Sidebar Navigation** W01–W07 | Global `Sidebar` (7 sections, collapse/persist/auto-expand/rail) | Verified | **Current** (one stale assertion) | `Sidebar.tsx` (NAV_SECTIONS L64–135, storage key L187, Action Center pinned L225–240). **W01 nit:** guide says "Dashboard sits alone under OVERVIEW" — live has **Action Center pinned + Dashboard** under OVERVIEW (guide's own X09 requires the pinned item). Reword W01. |
| **X — Action Center** X01–X14 | `/action-center`, dashboard embed, `/cancellation-insights` | All 7 sources verified | **Current** (X08/X11/X14 fixture-gated data) | `ActionCenterClient.tsx` (SOURCE_META L78–160); dashboard embed max 5; `cancellation-insights` full page. X08/X11 "reset drifted config" + X14 error-state need fixture state — **Fixture-gated**, not UI gaps. |
| **Y — Command Palette & Global Search** Y01–Y12 | `CommandPalette`, header search pill, `admin_global_search` RPC | Verified | **Current** (Y10 fixture) | `CommandPalette.tsx` (⌘K L98); `TopNavbar.tsx` (`topbar-global-search`); Y10 non-admin session partially **Fixture-gated**. |
| **Z — Dashboard Health Strip** Z01–Z07 | `HealthStatusStrip`, `/config` health keys, deep-links | Six indicators verified 1:1 | **Current** (Z03/Z06 fixture) | `health/HealthStatusStrip.tsx`; `healthStatus.ts` L71–81 registry. Z03/Z06 need config-drift fixture to force warning/critical dots. |
| **Regression** R01–R05 | Cross-surface config/mobile, read-only, audit | Structurally present | **Current** (R03 mobile cross-surface) | `/config` `canWrite===false` Read-Only banner; `admin_audit_logs` writes verified. |
| **N2 — Idempotency & Audit** N2-A01–A08 | `/audit` (Financial Audit), journal, idempotency RPCs | `/audit` page verified | **Current** (A02/A03/A04 fixture-gated/backend) | `audit/page.tsx` (h1 L173, empty state L261); sidebar Financial Audit. A02–A04 are DB-level SQL double-call/webhook-replay checks — not UI-drivable. |

---

## Roll-up

**Counts (per group):**
- **Current:** A, B, C, D, E, G, H, I, K, M, N, O, P, R, S, T, U, V, W, X, Y, Z, Regression, N2 → **24 groups** (many carry per-case fixture/mobile caveats listed in the table).
- **Stale:** **F** (the big one — `/settings/trade-timing` missing most of its surface), plus partial-stale **J** (bare `/tax` step), **L** (L06 dashboard card gone), **Q** (Q02/Q03 confirm copy), **W** (W01 single-item phrasing).
- **Dead:** **0** — no referenced page has zero live callers.
- **Fixture-gated / backend-only / mobile (case-level):** E06, E07, L07, L08, N2-A02, N2-A03, N2-A04, X08, X11, X14, Y10, R03, + the mobile-reflection halves of B06/B07/C06–C10/D05–D07/M04/S03.

**Top stale findings (exact file evidence):**
1. **Group F — `/settings/trade-timing` is missing most of the guide's surface** (F03/F05/F06/F08/F09/F10): no seller-fee/Charge-One-Fee, no Pickup & Payout, no pickup reminders/168h guardrail, no Tiered Buyer Fee R1, no Buyer Fee-Tier Distribution (moved to `/analytics`), no "legacy fee keys" section; transaction-fee keys are live + named `transaction_fee_subscriber_cents`/`transaction_fee_non_subscriber_cents`. Evidence: `src/app/settings/trade-timing/page.tsx` full render block; R2 guardrail only in the mirror test L106–110 — **page vs. its own test have drifted**. Needs a guide rewrite **or a page completion** before QA runs F03/F05/F06/F08/F09/F10.
2. **J01 step "Open /tax" 404s** — `src/app/tax/` has no `page.tsx` (only subfolders). Tax subpages are fine.
3. **Q02/Q03 confirmation copy is stale** — `src/app/reviews/page.tsx` L88 / ~L113 (quotes in table).
4. **L06 dashboard "SP Economy" nav card (`card-sp-wallet`) is gone** — legacy card grid removed; grep finds zero `card-sp-wallet`.
5. **Credential drift** — guide account `test-admin@kidsmarketplace.test` and the login page's hardcoded demo `admin@example.com/admin123456` both diverge from the verified staging admin `samer@samer.com / samer`.

**Surfaces existing in the file tree with NO live sidebar caller (direct URL / cross-link / deep-link only):** `/audit-logs` (stub; superseded by `/audit`), `/monitoring` + `/monitoring/cron`, `/disputes` (redundant queue), `/sp-wallet` + `/sp-analytics`, `/settings/nodes`, `/subscriptions/manage`, `/payouts/earnings`. Legacy `ProtectedLayout.tsx` = zero importers (dead). No leading/trailing-space route issues found (stray `payouts/earnings/page.tsx.bak` noted, harmless).

**QA-round scoping recommendation:** Run groups A–E, G–I, K–P (minus L06), R–Z as written (with the credential fix + fixture caveats). **Do not run Group F (F03/F05/F06/F08/F09/F10), J's bare-`/tax` step, Q02/Q03, L06, or W01 as written** until a guide rewrite (or page fix) lands.
