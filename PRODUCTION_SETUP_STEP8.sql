-- ========================================
-- STEP 8: Verify
-- ========================================

SELECT COUNT(*) as total_config_items FROM admin_config;
SELECT * FROM admin_config LIMIT 10;
